// Database schema exports for crouton-layout — auto-discovered by NuxtHub for
// all apps with this (default-on) layer. The `layout_configs` table moved here
// from crouton-core with the layout engine (#751); the table definition is
// unchanged, so already-migrated apps need no new migration (drizzle diffs by
// structure, not file location).
export { layoutConfigs } from '../database/schema/layoutConfigs'
