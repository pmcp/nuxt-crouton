{
  "provider": {
    "type": "string",
    "meta": {
      "required": true,
      "label": "Provider",
      "description": "Account provider: slack, notion, figma, github, linear",
      "area": "main",
      "group": "basic"
    }
  },
  "label": {
    "type": "string",
    "meta": {
      "required": true,
      "maxLength": 200,
      "label": "Label",
      "description": "User-friendly account name (e.g., 'Design Team Slack')",
      "area": "main",
      "group": "basic"
    }
  },
  "providerAccountId": {
    "type": "string",
    "meta": {
      "required": true,
      "label": "Provider Account ID",
      "description": "Slack team ID, Notion workspace ID, etc. Used for deduplication.",
      "area": "main",
      "group": "basic"
    }
  },
  "accessToken": {
    "type": "string",
    "meta": {
      "required": true,
      "label": "Access Token",
      "description": "Encrypted access token (via encryptSecret())",
      "area": "sidebar",
      "group": "credentials"
    }
  },
  "accessTokenHint": {
    "type": "string",
    "meta": {
      "label": "Access Token Hint",
      "description": "Masked display of the access token (via maskSecret())",
      "area": "sidebar",
      "group": "credentials"
    }
  },
  "refreshToken": {
    "type": "string",
    "meta": {
      "label": "Refresh Token",
      "description": "Encrypted refresh token (optional, for providers that support token refresh)",
      "area": "sidebar",
      "group": "credentials"
    }
  },
  "tokenExpiresAt": {
    "type": "date",
    "meta": {
      "label": "Token Expires At",
      "description": "When the access token expires (if applicable)",
      "area": "sidebar",
      "group": "credentials"
    }
  },
  "scopes": {
    "type": "string",
    "meta": {
      "label": "Scopes",
      "description": "OAuth scopes granted (comma-separated)",
      "area": "sidebar",
      "group": "credentials"
    }
  },
  "providerMetadata": {
    "type": "json",
    "meta": {
      "label": "Provider Metadata",
      "description": "Provider-specific data (workspace name, bot user ID, etc.)",
      "area": "sidebar",
      "group": "metadata"
    }
  },
  "status": {
    "type": "string",
    "meta": {
      "required": true,
      "default": "connected",
      "label": "Status",
      "description": "Account connection status: connected, expired, revoked, error",
      "area": "sidebar",
      "group": "status"
    }
  },
  "lastVerifiedAt": {
    "type": "date",
    "meta": {
      "label": "Last Verified",
      "description": "Last time the token was verified with the provider API",
      "area": "sidebar",
      "group": "status"
    }
  }
}
