{
  "id": { "type": "uuid", "meta": { "primaryKey": true } },
  "eventId": { "type": "uuid", "refTarget": "events", "meta": { "required": true, "label": "Event" } },
  "orderId": { "type": "uuid", "refTarget": "orders", "meta": { "required": true, "label": "Order" } },
  "locationId": { "type": "uuid", "refTarget": "locations", "meta": { "required": true, "label": "Location", "helpText": "One row per (order × location) cleared off a kitchen display — lets the kitchen and bar screens bump the same order independently." } }
}
