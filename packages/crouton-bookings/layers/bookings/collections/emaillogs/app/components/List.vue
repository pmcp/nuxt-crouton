<!--
  @crouton-generated
  @collection emaillogs
  @layer bookings
  @generated 2026-01-06

  ## AI Context
  - List component for emaillogs collection
  - Collection name: bookingsEmaillogs
  - API endpoint: /api/teams/[id]/bookings-emaillogs
  - Columns composable: useBookingsEmaillogs()
  - Column fields: bookingId, templateId, recipientEmail, triggerType, status, sentAt, error

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bookingsEmaillogs"
    :columns="columns"
    :rows="emaillogs || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BookingsEmaillogs"
        :collection="'bookingsEmaillogs'"
        createButton
      />
    </template>
    <template #bookingId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.bookingId"
        :id="row.original.bookingId"
        collection="bookingsBookings"
      />
    </template>
    <template #templateId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.templateId"
        :id="row.original.templateId"
        collection="bookingsEmailTemplates"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBookingsEmaillogs from '../composables/useBookingsEmaillogs'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBookingsEmaillogs()

const { items: emaillogs, pending } = await useCollectionQuery(
  'bookingsEmaillogs'
)
</script>