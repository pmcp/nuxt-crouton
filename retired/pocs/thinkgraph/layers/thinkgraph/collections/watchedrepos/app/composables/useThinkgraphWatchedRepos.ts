/**
 * @crouton-generated
 * @collection watchedrepos
 * @layer thinkgraph
 * @generated 2026-04-07
 *
 * ## AI Context
 * - Composable: useThinkgraphWatchedRepos
 * - Collection name: thinkgraphWatchedRepos
 * - API endpoint: /api/teams/[id]/thinkgraph-watchedrepos
 * - Form component: ThinkgraphWatchedReposForm
 * - List component: ThinkgraphWatchedReposList
 * - Fields: repo, branch, lastCheckedSha, notes, active
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const thinkgraphWatchedRepoSchema = z.object({
  repo: z.string().min(1, 'repo is required'),
  branch: z.string().optional(),
  lastCheckedSha: z.string().optional(),
  notes: z.string().optional(),
  active: z.boolean().optional()
})

export const thinkgraphWatchedReposColumns = [
  { accessorKey: 'repo', header: 'Repo' },
  { accessorKey: 'branch', header: 'Branch' },
  { accessorKey: 'lastCheckedSha', header: 'LastCheckedSha' },
  { accessorKey: 'notes', header: 'Notes' },
  { accessorKey: 'active', header: 'Active' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _thinkgraphWatchedReposConfig = {
  name: 'thinkgraphWatchedRepos',
  layer: 'thinkgraph',
  apiPath: 'thinkgraph-watchedrepos',
  componentName: 'ThinkgraphWatchedReposForm',
  defaultValues: {
    repo: '',
    branch: '',
    lastCheckedSha: '',
    notes: '',
    active: false
  },
  columns: thinkgraphWatchedReposColumns,
  fields: [
      {
          "name": "repo",
          "type": "string",
          "label": "Repository",
          "area": "main"
      },
      {
          "name": "branch",
          "type": "string",
          "label": "Branch",
          "area": "sidebar"
      },
      {
          "name": "lastCheckedSha",
          "type": "string",
          "label": "Last Checked SHA",
          "area": "sidebar"
      },
      {
          "name": "notes",
          "type": "text",
          "label": "Notes",
          "area": "main"
      },
      {
          "name": "active",
          "type": "boolean",
          "label": "Active",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_thinkgraphWatchedReposConfig, 'schema', {
  value: thinkgraphWatchedRepoSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const thinkgraphWatchedReposConfig = _thinkgraphWatchedReposConfig as typeof _thinkgraphWatchedReposConfig & { schema: typeof thinkgraphWatchedRepoSchema }

export const useThinkgraphWatchedRepos = () => thinkgraphWatchedReposConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: thinkgraphWatchedReposConfig.defaultValues,
    schema: thinkgraphWatchedRepoSchema,
    columns: thinkgraphWatchedReposConfig.columns,
    collection: thinkgraphWatchedReposConfig.name
  }
}