<!--
  @crouton-generated
  @collection watchreports
  @layer thinkgraph
  @generated 2026-04-07

  ## AI Context
  - Form component for watchreports collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/thinkgraph-watchreports
  - Zod schema: useThinkgraphWatchReports() composable
  - Fields: repoId, runDate, summary, commitsSinceLast, createdNodeIds

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Summary" name="summary" class="not-last:pb-4">
          <UTextarea v-model="state.summary" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="CommitsSinceLast" name="commitsSinceLast" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.commitsSinceLast === 'string' ? state.commitsSinceLast : JSON.stringify(state.commitsSinceLast, null, 2)"
            @update:model-value="(val) => { try { state.commitsSinceLast = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="RepoId" name="repoId" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.repoId"
            collection="thinkgraphWatchedRepos"
            label="RepoId"
          />
        </UFormField>
        <UFormField label="RunDate" name="runDate" class="not-last:pb-4">
          <UInput v-model="state.runDate" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="CreatedNodeIds" name="createdNodeIds" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.createdNodeIds === 'string' ? state.createdNodeIds : JSON.stringify(state.createdNodeIds, null, 2)"
            @update:model-value="(val) => { try { state.createdNodeIds = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ThinkgraphWatchReportFormProps, ThinkgraphWatchReportFormData } from '../../types'
import useThinkgraphWatchReports from '../composables/useThinkgraphWatchReports'

const props = defineProps<ThinkgraphWatchReportFormProps>()
const { defaultValue, schema, collection } = useThinkgraphWatchReports()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<ThinkgraphWatchReportFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>