<!--
  @crouton-generated
  @collection posts
  @layer blog
  @generated 2026-06-17

  ## AI Context
  - List component for posts collection
  - Collection name: blogPosts
  - API endpoint: /api/teams/[id]/blog-posts
  - Columns composable: useBlogPosts()
  - Column fields: title, slug, body, author, publishedAt, status, tags

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="blogPosts"
    :columns="columns"
    :rows="posts || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BlogPosts"
        :collection="'blogPosts'"
        createButton
      />
    </template>
    <template #tags-cell="{ row }">
      <div v-if="row.original.tags && row.original.tags.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.tags"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #publishedAt-cell="{ row }">
      <CroutonDate :date="row.original.publishedAt"></CroutonDate>
    </template>
    <template #body-cell="{ row }">
      <CroutonEditorPreview :content="row.original.body" mode="thumbnail" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBlogPosts from '../composables/useBlogPosts'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBlogPosts()

const { items: posts, pending } = await useCollectionQuery(
  'blogPosts'
)

</script>