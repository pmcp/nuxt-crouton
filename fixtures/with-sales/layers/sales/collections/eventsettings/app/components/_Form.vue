<!--
  @crouton-generated
  @collection eventsettings
  @layer sales
  @generated 2026-06-24

  ## AI Context
  - Form component for eventsettings collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/sales-eventsettings
  - Zod schema: useSalesEventsettings() composable
  - Fields: eventId, settingKey, settingValue, description

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField :label="t('sales.eventsettings.fields.eventId', 'Event')" name="eventId" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.eventId"
            collection="salesEvents"
            :label="t('sales.eventsettings.fields.eventId', 'Event')"
          />
        </UFormField>
        <UFormField :label="t('sales.eventsettings.fields.settingKey', 'Setting Key')" name="settingKey" class="not-last:pb-4">
          <UInput v-model="state.settingKey" class="w-full" size="xl" />
        </UFormField>
        <UFormField :label="t('sales.eventsettings.fields.settingValue', 'Setting Value')" name="settingValue" class="not-last:pb-4">
          <UTextarea v-model="state.settingValue" class="w-full" size="xl" />
        </UFormField>
        <UFormField :label="t('sales.eventsettings.fields.description', 'Description')" name="description" class="not-last:pb-4">
          <UTextarea v-model="state.description" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { SalesEventsettingFormProps, SalesEventsettingFormData } from '../../types'
import useSalesEventsettings from '../composables/useSalesEventsettings'

const props = defineProps<SalesEventsettingFormProps>()
const { defaultValue, schema, collection } = useSalesEventsettings()

// Field labels resolve through translations (team override → system → fallback)
const { t } = useT()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Draft state: seeded from defaults (required fields may start null/empty until
// the user fills them; the zod schema validates on submit), so cast the initial
// values to the validated shape.
const state = ref<SalesEventsettingFormData & { id?: string | null }>(initialValues as SalesEventsettingFormData & { id?: string | null })

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>