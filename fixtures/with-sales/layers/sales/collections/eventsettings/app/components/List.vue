<!--
  @crouton-generated
  @collection eventsettings
  @layer sales
  @generated 2026-06-24

  ## AI Context
  - List component for eventsettings collection
  - Collection name: salesEventsettings
  - API endpoint: /api/teams/[id]/sales-eventsettings
  - Columns composable: useSalesEventsettings()
  - Column fields: eventId, settingKey, settingValue, description

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="salesEventsettings"
    :columns="columns"
    :rows="eventsettings || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="SalesEventsettings"
        :collection="'salesEventsettings'"
        createButton
      />
    </template>
    <template #eventId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.eventId"
        :id="row.original.eventId"
        collection="salesEvents"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useSalesEventsettings from '../composables/useSalesEventsettings'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useSalesEventsettings()

const { items: eventsettings, pending } = await useCollectionQuery(
  'salesEventsettings'
)

</script>