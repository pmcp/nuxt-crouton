/**
 * @crouton-generated
 * @collection kdsbumps
 * @layer sales
 * @generated 2026-06-24
 *
 * ## AI Context
 * - Composable: useSalesKdsbumps
 * - Collection name: salesKdsbumps
 * - API endpoint: /api/teams/[id]/sales-kdsbumps
 * - Form component: SalesKdsbumpsForm
 * - List component: SalesKdsbumpsList
 * - Fields: id, eventId, orderId, locationId
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const salesKdsbumpSchema = z.object({
  eventId: z.string().min(1, 'eventId is required'),
  orderId: z.string().min(1, 'orderId is required'),
  locationId: z.string().min(1, 'locationId is required')
})

export const salesKdsbumpsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'eventId', header: 'EventId' },
  { accessorKey: 'orderId', header: 'OrderId' },
  { accessorKey: 'locationId', header: 'LocationId' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _salesKdsbumpsConfig = {
  name: 'salesKdsbumps',
  layer: 'sales',
  apiPath: 'sales-kdsbumps',
  componentName: 'SalesKdsbumpsForm',
  defaultValues: {
    eventId: '',
    orderId: '',
    locationId: ''
  },
  columns: salesKdsbumpsColumns,
  fields: [
      {
          "name": "eventId",
          "type": "string",
          "label": "Event",
          "area": "main"
      },
      {
          "name": "orderId",
          "type": "string",
          "label": "Order",
          "area": "main"
      },
      {
          "name": "locationId",
          "type": "string",
          "label": "Location",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_salesKdsbumpsConfig, 'schema', {
  value: salesKdsbumpSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const salesKdsbumpsConfig = _salesKdsbumpsConfig as typeof _salesKdsbumpsConfig & { schema: typeof salesKdsbumpSchema }

export const useSalesKdsbumps = () => salesKdsbumpsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: salesKdsbumpsConfig.defaultValues,
    schema: salesKdsbumpSchema,
    columns: salesKdsbumpsConfig.columns,
    collection: salesKdsbumpsConfig.name
  }
}