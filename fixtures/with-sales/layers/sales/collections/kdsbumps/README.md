# SalesKdsbumps Collection

> Generated by @fyit/crouton-cli on 2026-06-24

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesKdsbumps` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-kdsbumps` |
| Form Component | `<SalesKdsbumpsForm />` |
| List Component | `<SalesKdsbumpsList />` |
| Composable | `useSalesKdsbumps()` |

## Features

- References: eventId → events, orderId → orders, locationId → locations

## File Structure

```
collections/kdsbumps/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesKdsbumps.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-kdsbumps/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [kdsbumpId].patch.ts    # UPDATE
│   │   └── [kdsbumpId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| orderId | string (→ orders) | Yes |
| locationId | string (→ locations) | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-kdsbumps

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-kdsbumps`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-kdsbumps?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-kdsbumps

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-kdsbumps`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-kdsbumps/{kdsbumpId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-kdsbumps/${kdsbumpId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-kdsbumps/{kdsbumpId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-kdsbumps/${kdsbumpId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: kdsbumps, pending } = await useCollectionQuery('salesKdsbumps')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesKdsbumps')
</script>

<template>
  <SalesKdsbumpsList />
</template>
```

### Schema & Validation

```typescript
import { salesKdsbumpSchema } from './app/composables/useSalesKdsbumps'

// Validate data
const result = salesKdsbumpSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only kdsbumps`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesKdsbumps.ts`:
```typescript
export const salesKdsbumpSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-kdsbumps/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
