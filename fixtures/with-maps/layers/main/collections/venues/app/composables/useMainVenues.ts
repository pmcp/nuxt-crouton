/**
 * @crouton-generated
 * @collection venues
 * @layer main
 * @generated 2026-06-21
 *
 * ## AI Context
 * - Composable: useMainVenues
 * - Collection name: mainVenues
 * - API endpoint: /api/teams/[id]/main-venues
 * - Form component: MainVenuesForm
 * - List component: MainVenuesList
 * - Fields: name, street, city, postcode, coordinates
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainVenueSchema = z.object({
  name: z.string().min(1, 'name is required'),
  street: z.string().optional(),
  city: z.string().optional(),
  postcode: z.string().optional(),
  coordinates: z.string().optional()
})

export const mainVenuesColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'street', header: 'Street' },
  { accessorKey: 'city', header: 'City' },
  { accessorKey: 'postcode', header: 'Postcode' },
  { accessorKey: 'coordinates', header: 'Coordinates' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainVenuesConfig = {
  name: 'mainVenues',
  layer: 'main',
  apiPath: 'main-venues',
  componentName: 'MainVenuesForm',
  defaultValues: {
    name: '',
    street: '',
    city: '',
    postcode: '',
    coordinates: ''
  },
  columns: mainVenuesColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "street",
          "type": "string",
          "label": "Street",
          "area": "main"
      },
      {
          "name": "city",
          "type": "string",
          "label": "City",
          "area": "main"
      },
      {
          "name": "postcode",
          "type": "string",
          "label": "Postcode",
          "area": "main"
      },
      {
          "name": "coordinates",
          "type": "string",
          "label": "Coordinates",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainVenuesConfig, 'schema', {
  value: mainVenueSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainVenuesConfig = _mainVenuesConfig as typeof _mainVenuesConfig & { schema: typeof mainVenueSchema }

export const useMainVenues = () => mainVenuesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainVenuesConfig.defaultValues,
    schema: mainVenueSchema,
    columns: mainVenuesConfig.columns,
    collection: mainVenuesConfig.name
  }
}