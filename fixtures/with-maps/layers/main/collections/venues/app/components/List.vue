<!--
  @crouton-generated
  @collection venues
  @layer main
  @generated 2026-06-21

  ## AI Context
  - List component for venues collection
  - Collection name: mainVenues
  - API endpoint: /api/teams/[id]/main-venues
  - Columns composable: useMainVenues()
  - Column fields: name, street, city, postcode, coordinates

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainVenues"
    :columns="columns"
    :rows="venues || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainVenues"
        :collection="'mainVenues'"
        createButton
      />
    </template>
    <template #coordinates-cell="{ row }">
      <CroutonMapsPreview :location="row.original.coordinates" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainVenues from '../composables/useMainVenues'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainVenues()

const { items: venues, pending } = await useCollectionQuery(
  'mainVenues'
)

</script>