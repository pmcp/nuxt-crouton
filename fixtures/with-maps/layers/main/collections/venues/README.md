# MainVenues Collection

> Generated by @fyit/crouton-cli on 2026-06-21

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainVenues` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-venues` |
| Form Component | `<MainVenuesForm />` |
| List Component | `<MainVenuesList />` |
| Composable | `useMainVenues()` |

## File Structure

```
collections/venues/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainVenues.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-venues/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [venueId].patch.ts    # UPDATE
│   │   └── [venueId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| street | string | No |
| city | string | No |
| postcode | string | No |
| coordinates | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-venues

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-venues`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-venues?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-venues

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-venues`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-venues/{venueId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-venues/${venueId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-venues/{venueId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-venues/${venueId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: venues, pending } = await useCollectionQuery('mainVenues')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainVenues')
</script>

<template>
  <MainVenuesList />
</template>
```

### Schema & Validation

```typescript
import { mainVenueSchema } from './app/composables/useMainVenues'

// Validate data
const result = mainVenueSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only venues`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainVenues.ts`:
```typescript
export const mainVenueSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-venues/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
