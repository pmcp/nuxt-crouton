/**
 * @crouton-generated
 * @collection items
 * @layer main
 * @generated 2026-06-21
 *
 * ## AI Context
 * - Composable: useMainItems
 * - Collection name: mainItems
 * - API endpoint: /api/teams/[id]/main-items
 * - Form component: MainItemsForm
 * - List component: MainItemsList
 * - Fields: name, done
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainItemSchema = z.object({
  name: z.string().min(1, 'name is required'),
  done: z.boolean().optional()
})

export const mainItemsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'done', header: 'Done' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainItemsConfig = {
  name: 'mainItems',
  layer: 'main',
  apiPath: 'main-items',
  componentName: 'MainItemsForm',
  defaultValues: {
    name: '',
    done: false
  },
  columns: mainItemsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "done",
          "type": "boolean",
          "label": "Done",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainItemsConfig, 'schema', {
  value: mainItemSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainItemsConfig = _mainItemsConfig as typeof _mainItemsConfig & { schema: typeof mainItemSchema }

export const useMainItems = () => mainItemsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainItemsConfig.defaultValues,
    schema: mainItemSchema,
    columns: mainItemsConfig.columns,
    collection: mainItemsConfig.name
  }
}