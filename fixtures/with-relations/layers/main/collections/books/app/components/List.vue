<!--
  @crouton-generated
  @collection books
  @layer main
  @generated 2026-06-16

  ## AI Context
  - List component for books collection
  - Collection name: mainBooks
  - API endpoint: /api/teams/[id]/main-books
  - Columns composable: useMainBooks()
  - Column fields: title, authorId, year, available

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainBooks"
    :columns="columns"
    :rows="books || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainBooks"
        :collection="'mainBooks'"
        createButton
      />
    </template>
    <template #authorId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.authorId"
        :id="row.original.authorId"
        collection="mainAuthors"
      />
    </template>
    <template #available-cell="{ row }">
      <CroutonBoolean :value="row.original.available" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainBooks from '../composables/useMainBooks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainBooks()

const { items: books, pending } = await useCollectionQuery(
  'mainBooks'
)

</script>