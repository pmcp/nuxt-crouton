/**
 * @crouton-generated
 * @collection books
 * @layer main
 * @generated 2026-06-16
 *
 * ## AI Context
 * - Main interface: MainBook
 * - Form data type: MainBookFormData
 * - New item type: NewMainBook
 * - Form props: MainBookFormProps
 * - Fields: title, authorId, year, available
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { mainBookSchema } from './app/composables/useMainBooks'

export interface MainBook {
  id: string
  teamId: string
  owner: string
  title: string
  authorId?: string
  year?: number
  available?: boolean
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type MainBookFormData = z.infer<typeof mainBookSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewMainBook = Omit<MainBook, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface MainBookFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: MainBook | Record<string, never> // MainBook for update, empty object for create
  collection: string
  loading: string
  action: 'create' | 'update' | 'delete'
}