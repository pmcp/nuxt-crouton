# MainBooks Collection

> Generated by @fyit/crouton-cli on 2026-06-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainBooks` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-books` |
| Form Component | `<MainBooksForm />` |
| List Component | `<MainBooksList />` |
| Composable | `useMainBooks()` |

## Features

- References: authorId → authors

## File Structure

```
collections/books/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainBooks.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-books/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [bookId].patch.ts    # UPDATE
│   │   └── [bookId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| authorId | string (→ authors) | No |
| year | number | No |
| available | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-books

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-books`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-books?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-books

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-books`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-books/{bookId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-books/${bookId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-books/{bookId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-books/${bookId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: books, pending } = await useCollectionQuery('mainBooks')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainBooks')
</script>

<template>
  <MainBooksList />
</template>
```

### Schema & Validation

```typescript
import { mainBookSchema } from './app/composables/useMainBooks'

// Validate data
const result = mainBookSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only books`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainBooks.ts`:
```typescript
export const mainBookSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-books/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
