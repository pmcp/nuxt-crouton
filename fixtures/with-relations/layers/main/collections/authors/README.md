# MainAuthors Collection

> Generated by @fyit/crouton-cli on 2026-06-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainAuthors` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-authors` |
| Form Component | `<MainAuthorsForm />` |
| List Component | `<MainAuthorsList />` |
| Composable | `useMainAuthors()` |

## File Structure

```
collections/authors/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainAuthors.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-authors/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [authorId].patch.ts    # UPDATE
│   │   └── [authorId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| bio | text | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-authors

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-authors`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-authors?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-authors

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-authors`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-authors/{authorId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-authors/${authorId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-authors/{authorId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-authors/${authorId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: authors, pending } = await useCollectionQuery('mainAuthors')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainAuthors')
</script>

<template>
  <MainAuthorsList />
</template>
```

### Schema & Validation

```typescript
import { mainAuthorSchema } from './app/composables/useMainAuthors'

// Validate data
const result = mainAuthorSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only authors`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainAuthors.ts`:
```typescript
export const mainAuthorSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-authors/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
