<!--
  @crouton-generated
  @collection items
  @layer main
  @generated 2026-06-15

  ## AI Context
  - List component for items collection
  - Collection name: mainItems
  - API endpoint: /api/teams/[id]/main-items
  - Columns composable: useMainItems()
  - Column fields: name, done

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainItems"
    :columns="columns"
    :rows="items || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainItems"
        :collection="'mainItems'"
        createButton
      />
    </template>
    <template #done-cell="{ row }">
      <CroutonBoolean :value="row.original.done" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainItems from '../composables/useMainItems'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainItems()

const { items: items, pending } = await useCollectionQuery(
  'mainItems'
)

</script>