/**
 * @crouton-generated
 * @collection collections
 * @layer designer
 * @generated 2026-02-16
 *
 * ## AI Context
 * - Composable: useDesignerCollections
 * - Collection name: designerCollections
 * - API endpoint: /api/teams/[id]/designer-collections
 * - Form component: DesignerCollectionsForm
 * - List component: DesignerCollectionsList
 * - Fields: id, projectId, name, description, sortOrder
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const designerCollectionSchema = z.object({
  projectId: z.string().min(1, 'projectId is required'),
  name: z.string().min(1, 'name is required'),
  description: z.string().optional(),
  sortOrder: z.string().optional()
})

export const designerCollectionsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'projectId', header: 'ProjectId' },
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'description', header: 'Description' },
  { accessorKey: 'sortOrder', header: 'SortOrder' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _designerCollectionsConfig = {
  name: 'designerCollections',
  layer: 'designer',
  apiPath: 'designer-collections',
  componentName: 'DesignerCollectionsForm',
  defaultValues: {
    projectId: '',
    name: '',
    description: '',
    sortOrder: ''
  },
  columns: designerCollectionsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_designerCollectionsConfig, 'schema', {
  value: designerCollectionSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const designerCollectionsConfig = _designerCollectionsConfig as typeof _designerCollectionsConfig & { schema: typeof designerCollectionSchema }

export const useDesignerCollections = () => designerCollectionsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: designerCollectionsConfig.defaultValues,
    schema: designerCollectionSchema,
    columns: designerCollectionsConfig.columns,
    collection: designerCollectionsConfig.name
  }
}