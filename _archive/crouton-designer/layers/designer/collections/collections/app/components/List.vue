<!--
  @crouton-generated
  @collection collections
  @layer designer
  @generated 2026-02-16

  ## AI Context
  - List component for collections collection
  - Collection name: designerCollections
  - API endpoint: /api/teams/[id]/designer-collections
  - Columns composable: useDesignerCollections()
  - Column fields: projectId, name, description, sortOrder

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="designerCollections"
    :columns="columns"
    :rows="collections || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="DesignerCollections"
        :collection="'designerCollections'"
        createButton
      />
    </template>
    <template #projectId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.projectId"
        :id="row.original.projectId"
        collection="designerProjects"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useDesignerCollections from '../composables/useDesignerCollections'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useDesignerCollections()

const { items: collections, pending } = await useCollectionQuery(
  'designerCollections'
)

</script>