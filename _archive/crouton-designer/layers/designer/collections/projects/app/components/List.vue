<!--
  @crouton-generated
  @collection projects
  @layer designer
  @generated 2026-02-16

  ## AI Context
  - List component for projects collection
  - Collection name: designerProjects
  - API endpoint: /api/teams/[id]/designer-projects
  - Columns composable: useDesignerProjects()
  - Column fields: name, currentPhase, config, messages

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="designerProjects"
    :columns="columns"
    :rows="projects || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="DesignerProjects"
        :collection="'designerProjects'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useDesignerProjects from '../composables/useDesignerProjects'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useDesignerProjects()

const { items: projects, pending } = await useCollectionQuery(
  'designerProjects'
)

</script>