<!--
  @crouton-generated
  @collection projects
  @layer designer
  @generated 2026-02-16

  ## AI Context
  - Form component for projects collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/designer-projects
  - Zod schema: useDesignerProjects() composable
  - Fields: name, currentPhase, config, messages

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Name" name="name" class="not-last:pb-4">
          <UInput v-model="state.name" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="CurrentPhase" name="currentPhase" class="not-last:pb-4">
          <UInput v-model="state.currentPhase" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Config" name="config" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.config === 'string' ? state.config : JSON.stringify(state.config, null, 2)"
            @update:model-value="(val) => { try { state.config = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
        <UFormField label="Messages" name="messages" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.messages === 'string' ? state.messages : JSON.stringify(state.messages, null, 2)"
            @update:model-value="(val) => { try { state.messages = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { DesignerProjectFormProps, DesignerProjectFormData } from '../../types'
import useDesignerProjects from '../composables/useDesignerProjects'

const props = defineProps<DesignerProjectFormProps>()
const { defaultValue, schema, collection } = useDesignerProjects()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<DesignerProjectFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>