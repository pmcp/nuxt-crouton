/**
 * @crouton-generated
 * @collection pages
 * @layer pages
 * @generated 2026-01-14
 *
 * ## AI Context
 * - Composable: usePagesPages
 * - Collection name: pagesPages
 * - API endpoint: /api/teams/[id]/pages-pages
 * - Form component: PagesPagesForm
 * - List component: PagesPagesList
 * - Fields: name, label, icon, description, hierarchy, fields
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @friendlyinternet/nuxt-crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const pagesPageSchema = z.object({
  name: z.string().optional(),
  label: z.string().optional(),
  icon: z.string().optional(),
  description: z.string().optional(),
  hierarchy: z.string().optional(),
  fields: z.string().optional()
})

export const pagesPagesColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'label', header: 'Label' },
  { accessorKey: 'icon', header: 'Icon' },
  { accessorKey: 'description', header: 'Description' },
  { accessorKey: 'hierarchy', header: 'Hierarchy' },
  { accessorKey: 'fields', header: 'Fields' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _pagesPagesConfig = {
  name: 'pagesPages',
  layer: 'pages',
  apiPath: 'pages-pages',
  componentName: 'PagesPagesForm',
  defaultValues: {
    name: '',
    label: '',
    icon: '',
    description: '',
    hierarchy: '',
    fields: ''
  },
  columns: pagesPagesColumns,
  sortable: {
    enabled: true,
    orderField: 'order'
  },
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_pagesPagesConfig, 'schema', {
  value: pagesPageSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const pagesPagesConfig = _pagesPagesConfig as typeof _pagesPagesConfig & { schema: typeof pagesPageSchema }

export const usePagesPages = () => pagesPagesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: pagesPagesConfig.defaultValues,
    schema: pagesPageSchema,
    columns: pagesPagesConfig.columns,
    collection: pagesPagesConfig.name
  }
}