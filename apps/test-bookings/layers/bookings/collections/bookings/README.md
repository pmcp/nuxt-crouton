# BookingsBookings Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-22

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsBookings` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-bookings` |
| Form Component | `<BookingsBookingsForm />` |
| List Component | `<BookingsBookingsList />` |
| Composable | `useBookingsBookings()` |

## Features

- References: location → locations

## File Structure

```
collections/bookings/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsBookings.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-bookings/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [bookingId].patch.ts    # UPDATE
│   │   └── [bookingId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| location | string (→ locations) | Yes |
| date | date | Yes |
| slot | string | Yes |
| group | string | No |
| status | string | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-bookings

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-bookings`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-bookings?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-bookings

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-bookings`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-bookings/{bookingId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-bookings/${bookingId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-bookings/{bookingId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-bookings/${bookingId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: bookings, pending } = await useCollectionQuery('bookingsBookings')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsBookings')
</script>

<template>
  <BookingsBookingsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsBookingSchema } from './app/composables/useBookingsBookings'

// Validate data
const result = bookingsBookingSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only bookings`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsBookings.ts`:
```typescript
export const bookingsBookingSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-bookings/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
