<!--
  @crouton-generated
  @collection emailtemplates
  @layer bookings
  @generated 2026-01-06

  ## AI Context
  - List component for emailtemplates collection
  - Collection name: bookingsEmailtemplates
  - API endpoint: /api/teams/[id]/bookings-emailtemplates
  - Columns composable: useBookingsEmailtemplates()
  - Column fields: name, subject, body, fromEmail, triggerType, recipientType, isActive, daysOffset, locationId

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bookingsEmailtemplates"
    :columns="columns"
    :rows="emailtemplates || []"
    :loading="pending"
    :show-collab-presence="{ showSelf: true }"
  >
    <template #header>
      <CroutonTableHeader
        title="BookingsEmailtemplates"
        :collection="'bookingsEmailtemplates'"
        createButton
      />
    </template>
    <template #name-cell="{ row }">
      {{ t(row.original, 'name') }}
    </template>
    <template #subject-cell="{ row }">
      {{ t(row.original, 'subject') }}
    </template>
    <template #body-cell="{ row }">
      {{ t(row.original, 'body') }}
    </template>
    <template #locationId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.locationId"
        :id="row.original.locationId"
        collection="bookingsLocations"
      />
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['name', 'subject', 'body']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBookingsEmailtemplates from '../composables/useBookingsEmailtemplates'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useBookingsEmailtemplates()

const { items: emailtemplates, pending } = await useCollectionQuery(
  'bookingsEmailtemplates'
)
</script>