# BookingsEmailtemplates Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2026-01-06

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsEmailtemplates` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-emailtemplates` |
| Form Component | `<BookingsEmailtemplatesForm />` |
| List Component | `<BookingsEmailtemplatesList />` |
| Composable | `useBookingsEmailtemplates()` |

## Features

- Multi-language support for: name, subject, body
- References: locationId → locations

## File Structure

```
collections/emailtemplates/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsEmailtemplates.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-emailtemplates/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [emailtemplateId].patch.ts    # UPDATE
│   │   └── [emailtemplateId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| subject | string | Yes |
| body | string | Yes |
| fromEmail | string | Yes |
| triggerType | string | Yes |
| recipientType | string | Yes |
| isActive | boolean | No |
| daysOffset | number | No |
| locationId | string (→ locations) | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-emailtemplates

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-emailtemplates`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-emailtemplates?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-emailtemplates

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-emailtemplates`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-emailtemplates/{emailtemplateId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-emailtemplates/${emailtemplateId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-emailtemplates/{emailtemplateId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-emailtemplates/${emailtemplateId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: emailtemplates, pending } = await useCollectionQuery('bookingsEmailtemplates')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsEmailtemplates')
</script>

<template>
  <BookingsEmailtemplatesList />
</template>
```

### Schema & Validation

```typescript
import { bookingsEmailtemplateSchema } from './app/composables/useBookingsEmailtemplates'

// Validate data
const result = bookingsEmailtemplateSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only emailtemplates`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsEmailtemplates.ts`:
```typescript
export const bookingsEmailtemplateSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-emailtemplates/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
