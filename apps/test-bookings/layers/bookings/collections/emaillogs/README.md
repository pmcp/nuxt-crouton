# BookingsEmaillogs Collection

> Generated by @fyit/crouton-cli on 2026-01-06

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsEmaillogs` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-emaillogs` |
| Form Component | `<BookingsEmaillogsForm />` |
| List Component | `<BookingsEmaillogsList />` |
| Composable | `useBookingsEmaillogs()` |

## Features

- References: bookingId → bookings, templateId → emailTemplates

## File Structure

```
collections/emaillogs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsEmaillogs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-emaillogs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [emaillogId].patch.ts    # UPDATE
│   │   └── [emaillogId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| bookingId | string (→ bookings) | No |
| templateId | string (→ emailTemplates) | No |
| recipientEmail | string | Yes |
| triggerType | string | Yes |
| status | string | Yes |
| sentAt | string | No |
| error | text | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-emaillogs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-emaillogs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-emaillogs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-emaillogs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-emaillogs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-emaillogs/{emaillogId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-emaillogs/${emaillogId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-emaillogs/{emaillogId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-emaillogs/${emaillogId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: emaillogs, pending } = await useCollectionQuery('bookingsEmaillogs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsEmaillogs')
</script>

<template>
  <BookingsEmaillogsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsEmaillogSchema } from './app/composables/useBookingsEmaillogs'

// Validate data
const result = bookingsEmaillogSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only emaillogs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsEmaillogs.ts`:
```typescript
export const bookingsEmaillogSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-emaillogs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
