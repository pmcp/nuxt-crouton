<!--
  @crouton-generated
  @collection settings
  @layer bookings
  @generated 2026-01-14

  ## AI Context
  - List component for settings collection
  - Collection name: bookingsSettings
  - API endpoint: /api/teams/[id]/bookings-settings
  - Columns composable: useBookingsSettings()
  - Column fields: statuses, groups

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bookingsSettings"
    :columns="columns"
    :rows="settings || []"
    :loading="pending"
    :show-collab-presence="{ showSelf: true }"
  >
    <template #header>
      <CroutonTableHeader
        title="BookingsSettings"
        :collection="'bookingsSettings'"
        createButton
      />
    </template>
    <template #statuses-cell="{ row }">
      <BookingsSettingsStatuseCardMini :value="row.original.statuses" />
    </template>
    <template #groups-cell="{ row }">
      <BookingsSettingsGroupCardMini :value="row.original.groups" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBookingsSettings from '../composables/useBookingsSettings'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBookingsSettings()

const { items: settings, pending } = await useCollectionQuery(
  'bookingsSettings'
)
</script>