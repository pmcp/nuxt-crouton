/**
 * @crouton-generated
 * @collection settings
 * @layer kvr
 * @generated 2026-04-23
 *
 * ## AI Context
 * - Composable: useKvrSettings
 * - Collection name: kvrSettings
 * - API endpoint: /api/teams/[id]/kvr-settings
 * - Form component: KvrSettingsForm
 * - List component: KvrSettingsList
 * - Fields: recipients
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

export const kvrSettingsRecipientItemSchema = z.object({
  id: z.string(),
  label: z.string(),
  email: z.string(),
  isDefault: z.boolean().optional()
})

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const kvrSettingSchema = z.object({
  recipients: z.array(kvrSettingsRecipientItemSchema).optional()
})

export const kvrSettingsColumns = [
  { accessorKey: 'recipients', header: 'Recipients' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _kvrSettingsConfig = {
  name: 'kvrSettings',
  layer: 'kvr',
  apiPath: 'kvr-settings',
  componentName: 'KvrSettingsForm',
  defaultValues: {
    recipients: []
  },
  columns: kvrSettingsColumns,
  dependentFieldComponents: {
    recipients: 'KvrSettingsRecipientSelect'
  },
  fields: [
      {
          "name": "recipients",
          "type": "repeater",
          "label": "E-mail ontvangers",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_kvrSettingsConfig, 'schema', {
  value: kvrSettingSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const kvrSettingsConfig = _kvrSettingsConfig as typeof _kvrSettingsConfig & { schema: typeof kvrSettingSchema }

export const useKvrSettings = () => kvrSettingsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: kvrSettingsConfig.defaultValues,
    schema: kvrSettingSchema,
    columns: kvrSettingsConfig.columns,
    collection: kvrSettingsConfig.name
  }
}