<!--
  @crouton-generated
  @collection locations
  @layer sales
  @generated 2025-12-25

  ## AI Context
  - List component for locations collection
  - Collection name: salesLocations
  - API endpoint: /api/teams/[id]/sales-locations
  - Columns composable: useSalesLocations()
  - Column fields: eventId, title

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="salesLocations"
    :columns="columns"
    :rows="locations || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="SalesLocations"
        :collection="'salesLocations'"
        createButton
      />
    </template>
    <template #eventId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.eventId"
        :id="row.original.eventId"
        collection="salesEvents"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useSalesLocations from '../composables/useSalesLocations'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useSalesLocations()

const { items: locations, pending } = await useCollectionQuery(
  'salesLocations'
)
</script>