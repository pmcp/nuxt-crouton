/**
 * @crouton-generated
 * @collection locations
 * @layer sales
 * @generated 2025-12-25
 *
 * ## AI Context
 * - Composable: useSalesLocations
 * - Collection name: salesLocations
 * - API endpoint: /api/teams/[id]/sales-locations
 * - Form component: SalesLocationsForm
 * - List component: SalesLocationsList
 * - Fields: id, eventId, title
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const salesLocationSchema = z.object({
  eventId: z.string().min(1, 'eventId is required'),
  title: z.string().min(1, 'title is required')
})

export const salesLocationsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'eventId', header: 'EventId' },
  { accessorKey: 'title', header: 'Title' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _salesLocationsConfig = {
  name: 'salesLocations',
  layer: 'sales',
  apiPath: 'sales-locations',
  componentName: 'SalesLocationsForm',
  defaultValues: {
    eventId: '',
    title: ''
  },
  columns: salesLocationsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_salesLocationsConfig, 'schema', {
  value: salesLocationSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const salesLocationsConfig = _salesLocationsConfig as typeof _salesLocationsConfig & { schema: typeof salesLocationSchema }

export const useSalesLocations = () => salesLocationsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: salesLocationsConfig.defaultValues,
    schema: salesLocationSchema,
    columns: salesLocationsConfig.columns,
    collection: salesLocationsConfig.name
  }
}