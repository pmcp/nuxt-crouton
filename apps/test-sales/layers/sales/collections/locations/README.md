# SalesLocations Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesLocations` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-locations` |
| Form Component | `<SalesLocationsForm />` |
| List Component | `<SalesLocationsList />` |
| Composable | `useSalesLocations()` |

## Features

- References: eventId → events

## File Structure

```
collections/locations/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesLocations.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-locations/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [locationId].patch.ts    # UPDATE
│   │   └── [locationId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| title | string | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-locations

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-locations`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-locations?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-locations

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-locations`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-locations/{locationId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-locations/${locationId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-locations/{locationId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-locations/${locationId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: locations, pending } = await useCollectionQuery('salesLocations')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesLocations')
</script>

<template>
  <SalesLocationsList />
</template>
```

### Schema & Validation

```typescript
import { salesLocationSchema } from './app/composables/useSalesLocations'

// Validate data
const result = salesLocationSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only locations`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesLocations.ts`:
```typescript
export const salesLocationSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-locations/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
