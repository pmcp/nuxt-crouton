# SalesCategories Collection

> Generated by @fyit/crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesCategories` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-categories` |
| Form Component | `<SalesCategoriesForm />` |
| List Component | `<SalesCategoriesList />` |
| Composable | `useSalesCategories()` |

## Features

- References: eventId → events

## File Structure

```
collections/categories/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesCategories.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-categories/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [categorieId].patch.ts    # UPDATE
│   │   └── [categorieId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| title | string | Yes |
| displayOrder | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-categories

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-categories`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-categories?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-categories

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-categories`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-categories/{categorieId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-categories/${categorieId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-categories/{categorieId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-categories/${categorieId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: categories, pending } = await useCollectionQuery('salesCategories')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesCategories')
</script>

<template>
  <SalesCategoriesList />
</template>
```

### Schema & Validation

```typescript
import { salesCategorieSchema } from './app/composables/useSalesCategories'

// Validate data
const result = salesCategorieSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only categories`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesCategories.ts`:
```typescript
export const salesCategorieSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-categories/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
