/**
 * @crouton-generated
 * @collection categories
 * @layer sales
 * @generated 2025-12-25
 *
 * ## AI Context
 * - Composable: useSalesCategories
 * - Collection name: salesCategories
 * - API endpoint: /api/teams/[id]/sales-categories
 * - Form component: SalesCategoriesForm
 * - List component: SalesCategoriesList
 * - Fields: id, eventId, title, displayOrder
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const salesCategorieSchema = z.object({
  eventId: z.string().min(1, 'eventId is required'),
  title: z.string().min(1, 'title is required'),
  displayOrder: z.string().optional()
})

export const salesCategoriesColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'eventId', header: 'EventId' },
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'displayOrder', header: 'DisplayOrder' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _salesCategoriesConfig = {
  name: 'salesCategories',
  layer: 'sales',
  apiPath: 'sales-categories',
  componentName: 'SalesCategoriesForm',
  defaultValues: {
    eventId: '',
    title: '',
    displayOrder: ''
  },
  columns: salesCategoriesColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_salesCategoriesConfig, 'schema', {
  value: salesCategorieSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const salesCategoriesConfig = _salesCategoriesConfig as typeof _salesCategoriesConfig & { schema: typeof salesCategorieSchema }

export const useSalesCategories = () => salesCategoriesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: salesCategoriesConfig.defaultValues,
    schema: salesCategorieSchema,
    columns: salesCategoriesConfig.columns,
    collection: salesCategoriesConfig.name
  }
}