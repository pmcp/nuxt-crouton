<!--
  @crouton-generated
  @collection printqueues
  @layer sales
  @generated 2025-12-25

  ## AI Context
  - List component for printqueues collection
  - Collection name: salesPrintQueues
  - API endpoint: /api/teams/[id]/sales-printqueues
  - Columns composable: useSalesPrintQueues()
  - Column fields: eventId, orderId, printerId, locationId, status, printData, printMode, errorMessage, retryCount, completedAt

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="salesPrintQueues"
    :columns="columns"
    :rows="printqueues || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="SalesPrintQueues"
        :collection="'salesPrintQueues'"
        createButton
      />
    </template>
    <template #eventId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.eventId"
        :id="row.original.eventId"
        collection="salesEvents"
      />
    </template>
    <template #orderId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.orderId"
        :id="row.original.orderId"
        collection="salesOrders"
      />
    </template>
    <template #printerId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.printerId"
        :id="row.original.printerId"
        collection="salesPrinters"
      />
    </template>
    <template #locationId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.locationId"
        :id="row.original.locationId"
        collection="salesLocations"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useSalesPrintQueues from '../composables/useSalesPrintQueues'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useSalesPrintQueues()

const { items: printqueues, pending } = await useCollectionQuery(
  'salesPrintQueues'
)
</script>