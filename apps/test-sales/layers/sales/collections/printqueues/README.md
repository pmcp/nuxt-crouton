# SalesPrintQueues Collection

> Generated by @fyit/crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesPrintQueues` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-printqueues` |
| Form Component | `<SalesPrintQueuesForm />` |
| List Component | `<SalesPrintQueuesList />` |
| Composable | `useSalesPrintQueues()` |

## Features

- References: eventId → events, orderId → orders, printerId → printers, locationId → locations

## File Structure

```
collections/printqueues/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesPrintQueues.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-printqueues/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [printqueueId].patch.ts    # UPDATE
│   │   └── [printqueueId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| orderId | string (→ orders) | Yes |
| printerId | string (→ printers) | Yes |
| locationId | string (→ locations) | No |
| status | string | Yes |
| printData | text | Yes |
| printMode | string | No |
| errorMessage | text | No |
| retryCount | string | No |
| completedAt | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-printqueues

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-printqueues`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-printqueues?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-printqueues

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-printqueues`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-printqueues/{printqueueId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-printqueues/${printqueueId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-printqueues/{printqueueId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-printqueues/${printqueueId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: printqueues, pending } = await useCollectionQuery('salesPrintQueues')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesPrintQueues')
</script>

<template>
  <SalesPrintQueuesList />
</template>
```

### Schema & Validation

```typescript
import { salesPrintQueueSchema } from './app/composables/useSalesPrintQueues'

// Validate data
const result = salesPrintQueueSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only printqueues`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesPrintQueues.ts`:
```typescript
export const salesPrintQueueSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-printqueues/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
