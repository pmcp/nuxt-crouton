# SalesOrders Collection

> Generated by @fyit/crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesOrders` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-orders` |
| Form Component | `<SalesOrdersForm />` |
| List Component | `<SalesOrdersList />` |
| Composable | `useSalesOrders()` |

## Features

- References: eventId → events, clientId → clients

## File Structure

```
collections/orders/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesOrders.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-orders/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [orderId].patch.ts    # UPDATE
│   │   └── [orderId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| clientId | string (→ clients) | No |
| clientName | string | No |
| eventOrderNumber | string | No |
| overallRemarks | text | No |
| isPersonnel | boolean | No |
| status | string | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-orders

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-orders`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-orders?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-orders

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-orders`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-orders/{orderId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-orders/${orderId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-orders/{orderId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-orders/${orderId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: orders, pending } = await useCollectionQuery('salesOrders')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesOrders')
</script>

<template>
  <SalesOrdersList />
</template>
```

### Schema & Validation

```typescript
import { salesOrderSchema } from './app/composables/useSalesOrders'

// Validate data
const result = salesOrderSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only orders`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesOrders.ts`:
```typescript
export const salesOrderSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-orders/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
