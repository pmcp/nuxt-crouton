# SalesPrinters Collection

> Generated by @fyit/crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesPrinters` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-printers` |
| Form Component | `<SalesPrintersForm />` |
| List Component | `<SalesPrintersList />` |
| Composable | `useSalesPrinters()` |

## Features

- References: eventId → events, locationId → locations

## File Structure

```
collections/printers/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesPrinters.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-printers/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [printerId].patch.ts    # UPDATE
│   │   └── [printerId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| locationId | string (→ locations) | Yes |
| title | string | Yes |
| ipAddress | string | Yes |
| port | string | No |
| status | string | No |
| showPrices | boolean | No |
| isActive | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-printers

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-printers`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-printers?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-printers

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-printers`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-printers/{printerId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-printers/${printerId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-printers/{printerId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-printers/${printerId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: printers, pending } = await useCollectionQuery('salesPrinters')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesPrinters')
</script>

<template>
  <SalesPrintersList />
</template>
```

### Schema & Validation

```typescript
import { salesPrinterSchema } from './app/composables/useSalesPrinters'

// Validate data
const result = salesPrinterSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only printers`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesPrinters.ts`:
```typescript
export const salesPrinterSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-printers/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
