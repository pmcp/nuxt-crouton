<!--
  @crouton-generated
  @collection clients
  @layer sales
  @generated 2025-12-25

  ## AI Context
  - List component for clients collection
  - Collection name: salesClients
  - API endpoint: /api/teams/[id]/sales-clients
  - Columns composable: useSalesClients()
  - Column fields: title, isReusable

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="salesClients"
    :columns="columns"
    :rows="clients || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="SalesClients"
        :collection="'salesClients'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useSalesClients from '../composables/useSalesClients'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useSalesClients()

const { items: clients, pending } = await useCollectionQuery(
  'salesClients'
)
</script>