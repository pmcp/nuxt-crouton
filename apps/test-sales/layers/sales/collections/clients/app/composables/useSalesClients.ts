/**
 * @crouton-generated
 * @collection clients
 * @layer sales
 * @generated 2025-12-25
 *
 * ## AI Context
 * - Composable: useSalesClients
 * - Collection name: salesClients
 * - API endpoint: /api/teams/[id]/sales-clients
 * - Form component: SalesClientsForm
 * - List component: SalesClientsList
 * - Fields: id, title, isReusable
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const salesClientSchema = z.object({
  title: z.string().min(1, 'title is required'),
  isReusable: z.boolean().optional()
})

export const salesClientsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'isReusable', header: 'IsReusable' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _salesClientsConfig = {
  name: 'salesClients',
  layer: 'sales',
  apiPath: 'sales-clients',
  componentName: 'SalesClientsForm',
  defaultValues: {
    title: '',
    isReusable: false
  },
  columns: salesClientsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_salesClientsConfig, 'schema', {
  value: salesClientSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const salesClientsConfig = _salesClientsConfig as typeof _salesClientsConfig & { schema: typeof salesClientSchema }

export const useSalesClients = () => salesClientsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: salesClientsConfig.defaultValues,
    schema: salesClientSchema,
    columns: salesClientsConfig.columns,
    collection: salesClientsConfig.name
  }
}