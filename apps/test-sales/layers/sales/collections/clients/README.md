# SalesClients Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesClients` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-clients` |
| Form Component | `<SalesClientsForm />` |
| List Component | `<SalesClientsList />` |
| Composable | `useSalesClients()` |

## File Structure

```
collections/clients/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesClients.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-clients/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [clientId].patch.ts    # UPDATE
│   │   └── [clientId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| isReusable | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-clients

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-clients`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-clients?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-clients

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-clients`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-clients/{clientId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-clients/${clientId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-clients/{clientId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-clients/${clientId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: clients, pending } = await useCollectionQuery('salesClients')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesClients')
</script>

<template>
  <SalesClientsList />
</template>
```

### Schema & Validation

```typescript
import { salesClientSchema } from './app/composables/useSalesClients'

// Validate data
const result = salesClientSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only clients`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesClients.ts`:
```typescript
export const salesClientSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-clients/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
