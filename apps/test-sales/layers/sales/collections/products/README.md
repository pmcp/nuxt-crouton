# SalesProducts Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesProducts` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-products` |
| Form Component | `<SalesProductsForm />` |
| List Component | `<SalesProductsList />` |
| Composable | `useSalesProducts()` |

## Features

- References: eventId → events, categoryId → categories, locationId → locations
- Repeater fields: options

## File Structure

```
collections/products/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesProducts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-products/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [productId].patch.ts    # UPDATE
│   │   └── [productId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| categoryId | string (→ categories) | No |
| locationId | string (→ locations) | No |
| title | string | Yes |
| description | text | No |
| price | decimal | Yes |
| isActive | boolean | No |
| requiresRemark | boolean | No |
| remarkPrompt | string | No |
| hasOptions | boolean | No |
| multipleOptionsAllowed | boolean | No |
| options | repeater | No |
| sortOrder | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-products

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-products`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-products?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-products

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-products`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-products/{productId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-products/${productId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-products/{productId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-products/${productId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: products, pending } = await useCollectionQuery('salesProducts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesProducts')
</script>

<template>
  <SalesProductsList />
</template>
```

### Schema & Validation

```typescript
import { salesProductSchema } from './app/composables/useSalesProducts'

// Validate data
const result = salesProductSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only products`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesProducts.ts`:
```typescript
export const salesProductSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-products/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
