# TriageFlowInputs Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageFlowInputs` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-flowinputs` |
| Form Component | `<TriageFlowInputsForm />` |
| List Component | `<TriageFlowInputsList />` |
| Composable | `useTriageFlowInputs()` |

## Features

- References: flowId → flows

## File Structure

```
collections/flowinputs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageFlowInputs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-flowinputs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [flowinputId].patch.ts    # UPDATE
│   │   └── [flowinputId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| flowId | string (→ flows) | Yes |
| sourceType | string | Yes |
| name | string | Yes |
| apiToken | string | No |
| webhookUrl | string | No |
| webhookSecret | string | No |
| emailAddress | string | No |
| emailSlug | string | No |
| sourceMetadata | json | No |
| active | boolean | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-flowinputs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-flowinputs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-flowinputs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-flowinputs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-flowinputs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-flowinputs/{flowinputId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-flowinputs/${flowinputId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-flowinputs/{flowinputId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-flowinputs/${flowinputId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: flowInputs, pending } = await useCollectionQuery('triageFlowInputs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageFlowInputs')
</script>

<template>
  <TriageFlowInputsList />
</template>
```

### Schema & Validation

```typescript
import { triageFlowInputSchema } from './app/composables/useTriageFlowInputs'

// Validate data
const result = triageFlowInputSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only flowinputs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageFlowInputs.ts`:
```typescript
export const triageFlowInputSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-flowinputs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
