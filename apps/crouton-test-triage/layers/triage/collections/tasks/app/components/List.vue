<!--
  @crouton-generated
  @collection tasks
  @layer triage
  @generated 2026-02-10

  ## AI Context
  - List component for tasks collection
  - Collection name: triageTasks
  - API endpoint: /api/teams/[id]/triage-tasks
  - Columns composable: useTriageTasks()
  - Column fields: discussionId, syncJobId, notionPageId, notionPageUrl, title, description, status, priority, assignee, summary, sourceUrl, isMultiTaskChild, taskIndex, metadata

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageTasks"
    :columns="columns"
    :rows="tasks || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageTasks"
        :collection="'triageTasks'"
        createButton
      />
    </template>
    <template #discussionId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.discussionId"
        :id="row.original.discussionId"
        collection="triageDiscussions"
      />
    </template>
    <template #syncJobId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.syncJobId"
        :id="row.original.syncJobId"
        collection="triageJobs"
      />
    </template>
    <template #isMultiTaskChild-cell="{ row }">
      <CroutonBoolean :value="row.original.isMultiTaskChild" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageTasks from '../composables/useTriageTasks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageTasks()

const { items: tasks, pending } = await useCollectionQuery(
  'triageTasks'
)

</script>