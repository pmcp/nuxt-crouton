# TriageTasks Collection

> Generated by @fyit/crouton-cli on 2026-02-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageTasks` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-tasks` |
| Form Component | `<TriageTasksForm />` |
| List Component | `<TriageTasksList />` |
| Composable | `useTriageTasks()` |

## Features

- References: discussionId → discussions, syncJobId → jobs

## File Structure

```
collections/tasks/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageTasks.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-tasks/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [taskId].patch.ts    # UPDATE
│   │   └── [taskId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| discussionId | string (→ discussions) | Yes |
| syncJobId | string (→ jobs) | Yes |
| notionPageId | string | Yes |
| notionPageUrl | string | Yes |
| title | string | Yes |
| description | text | No |
| status | string | Yes |
| priority | string | No |
| assignee | string | No |
| summary | text | No |
| sourceUrl | string | Yes |
| isMultiTaskChild | boolean | Yes |
| taskIndex | number | No |
| metadata | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-tasks

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-tasks`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-tasks?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-tasks

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-tasks`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-tasks/{taskId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-tasks/${taskId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-tasks/{taskId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-tasks/${taskId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: tasks, pending } = await useCollectionQuery('triageTasks')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageTasks')
</script>

<template>
  <TriageTasksList />
</template>
```

### Schema & Validation

```typescript
import { triageTaskSchema } from './app/composables/useTriageTasks'

// Validate data
const result = triageTaskSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only tasks`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageTasks.ts`:
```typescript
export const triageTaskSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-tasks/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
