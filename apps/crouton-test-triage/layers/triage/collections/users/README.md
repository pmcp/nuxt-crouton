# TriageUsers Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageUsers` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-users` |
| Form Component | `<TriageUsersForm />` |
| List Component | `<TriageUsersList />` |
| Composable | `useTriageUsers()` |

## File Structure

```
collections/users/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageUsers.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-users/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [userId].patch.ts    # UPDATE
│   │   └── [userId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| sourceType | string | Yes |
| sourceWorkspaceId | string | Yes |
| sourceUserId | string | Yes |
| sourceUserEmail | string | No |
| sourceUserName | string | No |
| notionUserId | string | Yes |
| notionUserName | string | No |
| notionUserEmail | string | No |
| mappingType | string | Yes |
| confidence | number | No |
| active | boolean | Yes |
| lastSyncedAt | string | No |
| metadata | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-users

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-users`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-users?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-users

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-users`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-users/{userId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-users/${userId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-users/{userId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-users/${userId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: users, pending } = await useCollectionQuery('triageUsers')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageUsers')
</script>

<template>
  <TriageUsersList />
</template>
```

### Schema & Validation

```typescript
import { triageUserSchema } from './app/composables/useTriageUsers'

// Validate data
const result = triageUserSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only users`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageUsers.ts`:
```typescript
export const triageUserSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-users/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
