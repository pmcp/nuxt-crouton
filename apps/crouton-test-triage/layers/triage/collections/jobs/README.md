# TriageJobs Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageJobs` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-jobs` |
| Form Component | `<TriageJobsForm />` |
| List Component | `<TriageJobsList />` |
| Composable | `useTriageJobs()` |

## Features

- References: discussionId → discussions, flowInputId → inputs

## File Structure

```
collections/jobs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageJobs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-jobs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [jobId].patch.ts    # UPDATE
│   │   └── [jobId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| discussionId | string (→ discussions) | Yes |
| flowInputId | string (→ inputs) | Yes |
| status | string | Yes |
| stage | string | No |
| attempts | number | Yes |
| maxAttempts | number | Yes |
| error | text | No |
| errorStack | text | No |
| startedAt | date | No |
| completedAt | date | No |
| processingTime | number | No |
| taskIds | array | No |
| metadata | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-jobs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-jobs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-jobs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-jobs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-jobs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-jobs/{jobId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-jobs/${jobId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-jobs/{jobId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-jobs/${jobId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: jobs, pending } = await useCollectionQuery('triageJobs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageJobs')
</script>

<template>
  <TriageJobsList />
</template>
```

### Schema & Validation

```typescript
import { triageJobSchema } from './app/composables/useTriageJobs'

// Validate data
const result = triageJobSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only jobs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageJobs.ts`:
```typescript
export const triageJobSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-jobs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
