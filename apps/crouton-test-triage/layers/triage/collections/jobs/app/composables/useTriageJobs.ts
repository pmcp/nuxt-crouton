/**
 * @crouton-generated
 * @collection jobs
 * @layer triage
 * @generated 2026-02-10
 *
 * ## AI Context
 * - Composable: useTriageJobs
 * - Collection name: triageJobs
 * - API endpoint: /api/teams/[id]/triage-jobs
 * - Form component: TriageJobsForm
 * - List component: TriageJobsList
 * - Fields: discussionId, flowInputId, status, stage, attempts, maxAttempts, error, errorStack, startedAt, completedAt, processingTime, taskIds, metadata
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const triageJobSchema = z.object({
  discussionId: z.string().min(1, 'discussionId is required'),
  flowInputId: z.string().min(1, 'flowInputId is required'),
  status: z.string().min(1, 'status is required'),
  stage: z.string().optional(),
  attempts: z.number(),
  maxAttempts: z.number(),
  error: z.string().optional(),
  errorStack: z.string().optional(),
  startedAt: z.date().optional(),
  completedAt: z.date().optional(),
  processingTime: z.number().optional(),
  taskIds: z.array(z.string()).optional(),
  metadata: z.record(z.string(), z.any()).optional()
})

export const triageJobsColumns = [
  { accessorKey: 'discussionId', header: 'DiscussionId' },
  { accessorKey: 'flowInputId', header: 'FlowInputId' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'stage', header: 'Stage' },
  { accessorKey: 'attempts', header: 'Attempts' },
  { accessorKey: 'maxAttempts', header: 'MaxAttempts' },
  { accessorKey: 'error', header: 'Error' },
  { accessorKey: 'errorStack', header: 'ErrorStack' },
  { accessorKey: 'startedAt', header: 'StartedAt' },
  { accessorKey: 'completedAt', header: 'CompletedAt' },
  { accessorKey: 'processingTime', header: 'ProcessingTime' },
  { accessorKey: 'taskIds', header: 'TaskIds' },
  { accessorKey: 'metadata', header: 'Metadata' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _triageJobsConfig = {
  name: 'triageJobs',
  layer: 'triage',
  apiPath: 'triage-jobs',
  componentName: 'TriageJobsForm',
  defaultValues: {
    discussionId: '',
    flowInputId: '',
    status: '',
    stage: '',
    attempts: 0,
    maxAttempts: 0,
    error: '',
    errorStack: '',
    startedAt: null,
    completedAt: null,
    processingTime: 0,
    taskIds: [],
    metadata: {}
  },
  columns: triageJobsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_triageJobsConfig, 'schema', {
  value: triageJobSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const triageJobsConfig = _triageJobsConfig as typeof _triageJobsConfig & { schema: typeof triageJobSchema }

export const useTriageJobs = () => triageJobsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: triageJobsConfig.defaultValues,
    schema: triageJobSchema,
    columns: triageJobsConfig.columns,
    collection: triageJobsConfig.name
  }
}