<!--
  @crouton-generated
  @collection jobs
  @layer triage
  @generated 2026-02-10

  ## AI Context
  - List component for jobs collection
  - Collection name: triageJobs
  - API endpoint: /api/teams/[id]/triage-jobs
  - Columns composable: useTriageJobs()
  - Column fields: discussionId, flowInputId, status, stage, attempts, maxAttempts, error, errorStack, startedAt, completedAt, processingTime, taskIds, metadata

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageJobs"
    :columns="columns"
    :rows="jobs || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageJobs"
        :collection="'triageJobs'"
        createButton
      />
    </template>
    <template #discussionId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.discussionId"
        :id="row.original.discussionId"
        collection="triageDiscussions"
      />
    </template>
    <template #flowInputId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.flowInputId"
        :id="row.original.flowInputId"
        collection="triageFlowInputs"
      />
    </template>
    <template #taskIds-cell="{ row }">
      <div v-if="row.original.taskIds && row.original.taskIds.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.taskIds"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #startedAt-cell="{ row }">
      <CroutonDate :date="row.original.startedAt"></CroutonDate>
    </template>
    <template #completedAt-cell="{ row }">
      <CroutonDate :date="row.original.completedAt"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageJobs from '../composables/useTriageJobs'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageJobs()

const { items: jobs, pending } = await useCollectionQuery(
  'triageJobs'
)

</script>