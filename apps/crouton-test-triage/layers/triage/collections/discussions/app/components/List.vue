<!--
  @crouton-generated
  @collection discussions
  @layer triage
  @generated 2026-02-11

  ## AI Context
  - List component for discussions collection
  - Collection name: triageDiscussions
  - API endpoint: /api/teams/[id]/triage-discussions
  - Columns composable: useTriageDiscussions()
  - Column fields: sourceType, sourceThreadId, sourceUrl, flowInputId, title, content, authorHandle, participants, status, threadData, totalMessages, aiSummary, aiKeyPoints, aiTasks, isMultiTask, syncJobId, notionTaskIds, rawPayload, metadata, processedAt

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageDiscussions"
    :columns="columns"
    :rows="discussions || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageDiscussions"
        :collection="'triageDiscussions'"
        createButton
      />
    </template>
    <template #flowInputId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.flowInputId"
        :id="row.original.flowInputId"
        collection="triageInputs"
      />
    </template>
    <template #syncJobId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.syncJobId"
        :id="row.original.syncJobId"
        collection="triageJobs"
      />
    </template>
    <template #participants-cell="{ row }">
      <div v-if="row.original.participants && row.original.participants.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.participants"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #aiKeyPoints-cell="{ row }">
      <div v-if="row.original.aiKeyPoints && row.original.aiKeyPoints.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.aiKeyPoints"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #notionTaskIds-cell="{ row }">
      <div v-if="row.original.notionTaskIds && row.original.notionTaskIds.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.notionTaskIds"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #processedAt-cell="{ row }">
      <CroutonDate :date="row.original.processedAt"></CroutonDate>
    </template>
    <template #isMultiTask-cell="{ row }">
      <CroutonBoolean :value="row.original.isMultiTask" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageDiscussions from '../composables/useTriageDiscussions'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageDiscussions()

const { items: discussions, pending } = await useCollectionQuery(
  'triageDiscussions'
)

</script>