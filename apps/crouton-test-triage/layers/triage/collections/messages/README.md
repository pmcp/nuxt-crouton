# TriageMessages Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageMessages` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-messages` |
| Form Component | `<TriageMessagesForm />` |
| List Component | `<TriageMessagesList />` |
| Composable | `useTriageMessages()` |

## Features

- References: flowInputId → inputs

## File Structure

```
collections/messages/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageMessages.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-messages/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [messageId].patch.ts    # UPDATE
│   │   └── [messageId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| flowInputId | string (→ inputs) | Yes |
| messageType | string | Yes |
| from | string | Yes |
| to | string | Yes |
| subject | string | Yes |
| htmlBody | text | No |
| textBody | text | No |
| receivedAt | date | Yes |
| read | boolean | No |
| forwardedTo | string | No |
| forwardedAt | date | No |
| resendEmailId | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-messages

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-messages`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-messages?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-messages

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-messages`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-messages/{messageId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-messages/${messageId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-messages/{messageId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-messages/${messageId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: messages, pending } = await useCollectionQuery('triageMessages')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageMessages')
</script>

<template>
  <TriageMessagesList />
</template>
```

### Schema & Validation

```typescript
import { triageMessageSchema } from './app/composables/useTriageMessages'

// Validate data
const result = triageMessageSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only messages`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageMessages.ts`:
```typescript
export const triageMessageSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-messages/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
