# TriageOutputs Collection

> Generated by @fyit/crouton-cli on 2026-02-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageOutputs` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-outputs` |
| Form Component | `<TriageOutputsForm />` |
| List Component | `<TriageOutputsList />` |
| Composable | `useTriageOutputs()` |

## Features

- References: flowId → flows

## File Structure

```
collections/outputs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageOutputs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-outputs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [outputId].patch.ts    # UPDATE
│   │   └── [outputId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| flowId | string (→ flows) | Yes |
| outputType | string | Yes |
| name | string | Yes |
| domainFilter | array | No |
| isDefault | boolean | No |
| outputConfig | json | No |
| active | boolean | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-outputs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-outputs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-outputs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-outputs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-outputs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-outputs/{outputId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-outputs/${outputId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-outputs/{outputId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-outputs/${outputId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: outputs, pending } = await useCollectionQuery('triageOutputs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageOutputs')
</script>

<template>
  <TriageOutputsList />
</template>
```

### Schema & Validation

```typescript
import { triageOutputSchema } from './app/composables/useTriageOutputs'

// Validate data
const result = triageOutputSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only outputs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageOutputs.ts`:
```typescript
export const triageOutputSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-outputs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
