/**
 * @crouton-generated
 * @collection outputs
 * @layer triage
 * @generated 2026-02-11
 *
 * ## AI Context
 * - Composable: useTriageOutputs
 * - Collection name: triageOutputs
 * - API endpoint: /api/teams/[id]/triage-outputs
 * - Form component: TriageOutputsForm
 * - List component: TriageOutputsList
 * - Fields: flowId, outputType, domainFilter, isDefault, outputConfig, accountId, active
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const triageOutputSchema = z.object({
  flowId: z.string().min(1, 'flowId is required'),
  outputType: z.string().min(1, 'outputType is required'),
  domainFilter: z.array(z.string()).optional(),
  isDefault: z.boolean().optional(),
  outputConfig: z.record(z.string(), z.any()).optional(),
  accountId: z.string().optional(),
  active: z.boolean()
})

export const triageOutputsColumns = [
  { accessorKey: 'flowId', header: 'FlowId' },
  { accessorKey: 'outputType', header: 'OutputType' },
  { accessorKey: 'domainFilter', header: 'DomainFilter' },
  { accessorKey: 'isDefault', header: 'IsDefault' },
  { accessorKey: 'outputConfig', header: 'OutputConfig' },
  { accessorKey: 'accountId', header: 'AccountId' },
  { accessorKey: 'active', header: 'Active' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _triageOutputsConfig = {
  name: 'triageOutputs',
  layer: 'triage',
  apiPath: 'triage-outputs',
  componentName: 'TriageOutputsForm',
  defaultValues: {
    flowId: '',
    outputType: '',
    domainFilter: [],
    isDefault: false,
    outputConfig: {},
    accountId: '',
    active: false
  },
  columns: triageOutputsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_triageOutputsConfig, 'schema', {
  value: triageOutputSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const triageOutputsConfig = _triageOutputsConfig as typeof _triageOutputsConfig & { schema: typeof triageOutputSchema }

export const useTriageOutputs = () => triageOutputsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: triageOutputsConfig.defaultValues,
    schema: triageOutputSchema,
    columns: triageOutputsConfig.columns,
    collection: triageOutputsConfig.name
  }
}