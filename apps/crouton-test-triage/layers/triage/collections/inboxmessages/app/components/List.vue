<!--
  @crouton-generated
  @collection inboxmessages
  @layer triage
  @generated 2026-02-10

  ## AI Context
  - List component for inboxmessages collection
  - Collection name: triageInboxMessages
  - API endpoint: /api/teams/[id]/triage-inboxmessages
  - Columns composable: useTriageInboxMessages()
  - Column fields: flowInputId, messageType, from, to, subject, htmlBody, textBody, receivedAt, read, forwardedTo, forwardedAt, resendEmailId

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageInboxMessages"
    :columns="columns"
    :rows="inboxmessages || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageInboxMessages"
        :collection="'triageInboxMessages'"
        createButton
      />
    </template>
    <template #flowInputId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.flowInputId"
        :id="row.original.flowInputId"
        collection="triageFlowInputs"
      />
    </template>
    <template #receivedAt-cell="{ row }">
      <CroutonDate :date="row.original.receivedAt"></CroutonDate>
    </template>
    <template #forwardedAt-cell="{ row }">
      <CroutonDate :date="row.original.forwardedAt"></CroutonDate>
    </template>
    <template #read-cell="{ row }">
      <CroutonBoolean :value="row.original.read" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageInboxMessages from '../composables/useTriageInboxMessages'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageInboxMessages()

const { items: inboxMessages, pending } = await useCollectionQuery(
  'triageInboxMessages'
)

</script>