# TriageInboxMessages Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageInboxMessages` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-inboxmessages` |
| Form Component | `<TriageInboxMessagesForm />` |
| List Component | `<TriageInboxMessagesList />` |
| Composable | `useTriageInboxMessages()` |

## Features

- References: flowInputId → flowInputs

## File Structure

```
collections/inboxmessages/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageInboxMessages.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-inboxmessages/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [inboxmessageId].patch.ts    # UPDATE
│   │   └── [inboxmessageId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| flowInputId | string (→ flowInputs) | Yes |
| messageType | string | Yes |
| from | string | Yes |
| to | string | Yes |
| subject | string | Yes |
| htmlBody | text | No |
| textBody | text | No |
| receivedAt | date | Yes |
| read | boolean | No |
| forwardedTo | string | No |
| forwardedAt | date | No |
| resendEmailId | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-inboxmessages

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-inboxmessages`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-inboxmessages?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-inboxmessages

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-inboxmessages`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-inboxmessages/{inboxmessageId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-inboxmessages/${inboxmessageId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-inboxmessages/{inboxmessageId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-inboxmessages/${inboxmessageId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: inboxMessages, pending } = await useCollectionQuery('triageInboxMessages')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageInboxMessages')
</script>

<template>
  <TriageInboxMessagesList />
</template>
```

### Schema & Validation

```typescript
import { triageInboxMessageSchema } from './app/composables/useTriageInboxMessages'

// Validate data
const result = triageInboxMessageSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only inboxmessages`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageInboxMessages.ts`:
```typescript
export const triageInboxMessageSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-inboxmessages/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
