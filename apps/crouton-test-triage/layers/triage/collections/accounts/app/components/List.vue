<!--
  @crouton-generated
  @collection accounts
  @layer triage
  @generated 2026-02-11

  ## AI Context
  - List component for accounts collection
  - Collection name: triageAccounts
  - API endpoint: /api/teams/[id]/triage-accounts
  - Columns composable: useTriageAccounts()
  - Column fields: provider, label, providerAccountId, accessToken, accessTokenHint, refreshToken, tokenExpiresAt, scopes, providerMetadata, status, lastVerifiedAt

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageAccounts"
    :columns="columns"
    :rows="accounts || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageAccounts"
        :collection="'triageAccounts'"
        createButton
      />
    </template>
    <template #tokenExpiresAt-cell="{ row }">
      <CroutonDate :date="row.original.tokenExpiresAt"></CroutonDate>
    </template>
    <template #lastVerifiedAt-cell="{ row }">
      <CroutonDate :date="row.original.lastVerifiedAt"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageAccounts from '../composables/useTriageAccounts'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageAccounts()

const { items: accounts, pending } = await useCollectionQuery(
  'triageAccounts'
)

</script>