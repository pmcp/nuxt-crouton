# TriageAccounts Collection

> Generated by @fyit/crouton-cli on 2026-02-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageAccounts` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-accounts` |
| Form Component | `<TriageAccountsForm />` |
| List Component | `<TriageAccountsList />` |
| Composable | `useTriageAccounts()` |

## File Structure

```
collections/accounts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageAccounts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-accounts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [accountId].patch.ts    # UPDATE
│   │   └── [accountId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| provider | string | Yes |
| label | string | Yes |
| providerAccountId | string | Yes |
| accessToken | string | Yes |
| accessTokenHint | string | No |
| refreshToken | string | No |
| tokenExpiresAt | date | No |
| scopes | string | No |
| providerMetadata | json | No |
| status | string | Yes |
| lastVerifiedAt | date | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-accounts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-accounts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-accounts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-accounts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-accounts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-accounts/{accountId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-accounts/${accountId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-accounts/{accountId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-accounts/${accountId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: accounts, pending } = await useCollectionQuery('triageAccounts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageAccounts')
</script>

<template>
  <TriageAccountsList />
</template>
```

### Schema & Validation

```typescript
import { triageAccountSchema } from './app/composables/useTriageAccounts'

// Validate data
const result = triageAccountSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only accounts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageAccounts.ts`:
```typescript
export const triageAccountSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-accounts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
