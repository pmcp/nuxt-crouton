<!--
  @crouton-generated
  @collection usermappings
  @layer triage
  @generated 2026-02-10

  ## AI Context
  - List component for usermappings collection
  - Collection name: triageUserMappings
  - API endpoint: /api/teams/[id]/triage-usermappings
  - Columns composable: useTriageUserMappings()
  - Column fields: sourceType, sourceWorkspaceId, sourceUserId, sourceUserEmail, sourceUserName, notionUserId, notionUserName, notionUserEmail, mappingType, confidence, active, lastSyncedAt, metadata

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="triageUserMappings"
    :columns="columns"
    :rows="usermappings || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TriageUserMappings"
        :collection="'triageUserMappings'"
        createButton
      />
    </template>
    <template #active-cell="{ row }">
      <CroutonBoolean :value="row.original.active" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTriageUserMappings from '../composables/useTriageUserMappings'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTriageUserMappings()

const { items: userMappings, pending } = await useCollectionQuery(
  'triageUserMappings'
)

</script>