# PlaygroundPosts Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `playgroundPosts` |
| Layer | `playground` |
| API Base | `/api/teams/[teamId]/playground-posts` |
| Form Component | `<PlaygroundPostsForm />` |
| List Component | `<PlaygroundPostsList />` |
| Composable | `usePlaygroundPosts()` |

## Features

- Multi-language support for: title, excerpt, content
- References: categoryId → categories
- Repeater fields: metadata

## File Structure

```
collections/posts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePlaygroundPosts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/playground-posts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [postId].patch.ts    # UPDATE
│   │   └── [postId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| slug | string | No |
| excerpt | text | No |
| content | text | No |
| status | string | No |
| publishedAt | date | No |
| featuredImage | string | No |
| categoryId | string (→ categories) | No |
| metadata | repeater | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/playground-posts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/playground-posts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/playground-posts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/playground-posts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/playground-posts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/playground-posts/{postId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/playground-posts/${postId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/playground-posts/{postId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/playground-posts/${postId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: posts, pending } = await useCollectionQuery('playgroundPosts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('playgroundPosts')
</script>

<template>
  <PlaygroundPostsList />
</template>
```

### Schema & Validation

```typescript
import { playgroundPostSchema } from './app/composables/usePlaygroundPosts'

// Validate data
const result = playgroundPostSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only posts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePlaygroundPosts.ts`:
```typescript
export const playgroundPostSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/playground-posts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
