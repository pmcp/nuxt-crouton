<!--
  @crouton-generated
  @collection posts
  @layer playground
  @generated 2025-12-19

  ## AI Context
  - List component for posts collection
  - Collection name: playgroundPosts
  - API endpoint: /api/teams/[id]/playground-posts
  - Columns composable: usePlaygroundPosts()
  - Column fields: title, slug, excerpt, content, status, publishedAt, featuredImage, categoryId, metadata

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="playgroundPosts"
    :columns="columns"
    :rows="posts || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PlaygroundPosts"
        :collection="'playgroundPosts'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #excerpt-cell="{ row }">
      {{ t(row.original, 'excerpt') }}
    </template>
    <template #content-cell="{ row }">
      {{ t(row.original, 'content') }}
    </template>
    <template #categoryId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.categoryId"
        :id="row.original.categoryId"
        collection="playgroundCategories"
      />
    </template>
    <template #publishedAt-cell="{ row }">
      <CroutonDate :date="row.original.publishedAt"></CroutonDate>
    </template>
    <template #metadata-cell="{ row }">
      <PlaygroundPostsMetadataCardMini :value="row.original.metadata" />
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title', 'excerpt', 'content']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePlaygroundPosts from '../composables/usePlaygroundPosts'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = usePlaygroundPosts()

const { items: posts, pending } = await useCollectionQuery(
  'playgroundPosts'
)
</script>