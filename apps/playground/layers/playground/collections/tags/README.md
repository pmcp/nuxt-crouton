# PlaygroundTags Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `playgroundTags` |
| Layer | `playground` |
| API Base | `/api/teams/[teamId]/playground-tags` |
| Form Component | `<PlaygroundTagsForm />` |
| List Component | `<PlaygroundTagsList />` |
| Composable | `usePlaygroundTags()` |

## File Structure

```
collections/tags/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePlaygroundTags.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/playground-tags/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [tagId].patch.ts    # UPDATE
│   │   └── [tagId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| slug | string | No |
| color | string | No |
| order | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/playground-tags

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/playground-tags`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/playground-tags?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/playground-tags

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/playground-tags`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/playground-tags/{tagId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/playground-tags/${tagId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/playground-tags/{tagId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/playground-tags/${tagId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: tags, pending } = await useCollectionQuery('playgroundTags')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('playgroundTags')
</script>

<template>
  <PlaygroundTagsList />
</template>
```

### Schema & Validation

```typescript
import { playgroundTagSchema } from './app/composables/usePlaygroundTags'

// Validate data
const result = playgroundTagSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only tags`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePlaygroundTags.ts`:
```typescript
export const playgroundTagSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/playground-tags/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
