<!--
  @crouton-generated
  @collection tags
  @layer playground
  @generated 2025-12-19

  ## AI Context
  - Form component for tags collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/playground-tags
  - Zod schema: usePlaygroundTags() composable
  - Fields: name, slug, color, order

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Name" name="name" class="not-last:pb-4">
          <UInput v-model="state.name" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Slug" name="slug" class="not-last:pb-4">
          <UInput v-model="state.slug" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Color" name="color" class="not-last:pb-4">
          <UInput v-model="state.color" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Order" name="order" class="not-last:pb-4">
          <UInputNumber v-model="state.order" class="w-full" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { PlaygroundTagFormProps, PlaygroundTagFormData } from '../../types'
import usePlaygroundTags from '../composables/usePlaygroundTags'

const props = defineProps<PlaygroundTagFormProps>()
const { defaultValue, schema, collection } = usePlaygroundTags()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<PlaygroundTagFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>