<!--
  @crouton-generated
  @collection tags
  @layer playground
  @generated 2025-12-18

  ## AI Context
  - List component for tags collection
  - Collection name: playgroundTags
  - API endpoint: /api/teams/[id]/playground-tags
  - Columns composable: usePlaygroundTags()
  - Column fields: name, slug, color, order

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="playgroundTags"
    :columns="columns"
    :rows="tags || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PlaygroundTags"
        :collection="'playgroundTags'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePlaygroundTags from '../composables/usePlaygroundTags'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = usePlaygroundTags()

const { items: tags, pending } = await useCollectionQuery(
  'playgroundTags'
)
</script>