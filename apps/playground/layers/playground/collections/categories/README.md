# PlaygroundCategories Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `playgroundCategories` |
| Layer | `playground` |
| API Base | `/api/teams/[teamId]/playground-categories` |
| Form Component | `<PlaygroundCategoriesForm />` |
| List Component | `<PlaygroundCategoriesList />` |
| Composable | `usePlaygroundCategories()` |

## Features

- Tree/hierarchy structure with parent-child relationships
- Multi-language support for: name, description

## File Structure

```
collections/categories/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePlaygroundCategories.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/playground-categories/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [categorieId].patch.ts    # UPDATE
│   │   └── [categorieId].delete.ts   # DELETE
│   │   ├── [categorieId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| description | text | No |
| icon | string | No |
| color | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/playground-categories

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/playground-categories`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/playground-categories?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/playground-categories

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/playground-categories`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/playground-categories/{categorieId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/playground-categories/${categorieId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/playground-categories/{categorieId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/playground-categories/${categorieId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: categories, pending } = await useCollectionQuery('playgroundCategories')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('playgroundCategories')
</script>

<template>
  <PlaygroundCategoriesList />
</template>
```

### Schema & Validation

```typescript
import { playgroundCategorieSchema } from './app/composables/usePlaygroundCategories'

// Validate data
const result = playgroundCategorieSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only categories`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePlaygroundCategories.ts`:
```typescript
export const playgroundCategorieSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/playground-categories/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
