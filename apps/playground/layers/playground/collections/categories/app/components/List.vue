<!--
  @crouton-generated
  @collection categories
  @layer playground
  @generated 2025-12-19

  ## AI Context
  - List component for categories collection
  - Collection name: playgroundCategories
  - API endpoint: /api/teams/[id]/playground-categories
  - Columns composable: usePlaygroundCategories()
  - Column fields: name, description, icon, color

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="playgroundCategories"
    :columns="columns"
    :rows="categories || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PlaygroundCategories"
        :collection="'playgroundCategories'"
        createButton
      />
    </template>
    <template #name-cell="{ row }">
      {{ t(row.original, 'name') }}
    </template>
    <template #description-cell="{ row }">
      {{ t(row.original, 'description') }}
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['name', 'description']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePlaygroundCategories from '../composables/usePlaygroundCategories'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = usePlaygroundCategories()

const { items: categories, pending } = await useCollectionQuery(
  'playgroundCategories'
)
</script>