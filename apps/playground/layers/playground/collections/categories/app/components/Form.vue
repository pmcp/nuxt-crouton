<!--
  @crouton-generated
  @collection categories
  @layer playground
  @generated 2025-12-19

  ## AI Context
  - Form component for categories collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/playground-categories
  - Zod schema: usePlaygroundCategories() composable
  - Fields: name, description, icon, color

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Icon" name="icon" class="not-last:pb-4">
          <UInput v-model="state.icon" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Color" name="color" class="not-last:pb-4">
          <UInput v-model="state.color" class="w-full" size="xl" />
        </UFormField>
      </div>

      <div>


      <CroutonI18nInput
        v-model="state.translations"
        :fields="['name', 'description']"
        :default-values="{
          name: state.name || '',
          description: state.description || ''
        }"
        label="Translations"
      />
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">

        <UFormField label="Parent" name="parentId" class="not-last:pb-4">
          <CroutonFormParentSelect
            v-model="state.parentId"
            collection="playgroundCategories"
            :current-id="state.id"
            label="Parent"
          />
        </UFormField>
      </div>

      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { PlaygroundCategorieFormProps, PlaygroundCategorieFormData } from '../../types'
import usePlaygroundCategories from '../composables/usePlaygroundCategories'

const props = defineProps<PlaygroundCategorieFormProps>()
const { defaultValue, schema, collection } = usePlaygroundCategories()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
// Hierarchy defaults for new items (parentId, path, depth, order)
const hierarchyDefaults = {
  parentId: null,
  path: '/',
  depth: 0,
  order: 0
}
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue, ...hierarchyDefaults }

const state = ref<PlaygroundCategorieFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>