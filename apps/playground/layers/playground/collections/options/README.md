# PlaygroundOptions Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `playgroundOptions` |
| Layer | `playground` |
| API Base | `/api/teams/[teamId]/playground-options` |
| Form Component | `<PlaygroundOptionsForm />` |
| List Component | `<PlaygroundOptionsList />` |
| Composable | `usePlaygroundOptions()` |

## File Structure

```
collections/options/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePlaygroundOptions.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/playground-options/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [optionId].patch.ts    # UPDATE
│   │   └── [optionId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| description | text | No |
| icon | string | No |
| category | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/playground-options

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/playground-options`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/playground-options?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/playground-options

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/playground-options`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/playground-options/{optionId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/playground-options/${optionId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/playground-options/{optionId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/playground-options/${optionId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: options, pending } = await useCollectionQuery('playgroundOptions')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('playgroundOptions')
</script>

<template>
  <PlaygroundOptionsList />
</template>
```

### Schema & Validation

```typescript
import { playgroundOptionSchema } from './app/composables/usePlaygroundOptions'

// Validate data
const result = playgroundOptionSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only options`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePlaygroundOptions.ts`:
```typescript
export const playgroundOptionSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/playground-options/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
