<!--
  @crouton-generated
  @collection options
  @layer playground
  @generated 2025-12-18

  ## AI Context
  - List component for options collection
  - Collection name: playgroundOptions
  - API endpoint: /api/teams/[id]/playground-options
  - Columns composable: usePlaygroundOptions()
  - Column fields: title, description, icon, category

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="playgroundOptions"
    :columns="columns"
    :rows="options || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PlaygroundOptions"
        :collection="'playgroundOptions'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePlaygroundOptions from '../composables/usePlaygroundOptions'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = usePlaygroundOptions()

const { items: options, pending } = await useCollectionQuery(
  'playgroundOptions'
)
</script>