# PlaygroundDecisions Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `playgroundDecisions` |
| Layer | `playground` |
| API Base | `/api/teams/[teamId]/playground-decisions` |
| Form Component | `<PlaygroundDecisionsForm />` |
| List Component | `<PlaygroundDecisionsList />` |
| Composable | `usePlaygroundDecisions()` |

## Features

- Tree/hierarchy structure with parent-child relationships

## File Structure

```
collections/decisions/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePlaygroundDecisions.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/playground-decisions/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [decisionId].patch.ts    # UPDATE
│   │   └── [decisionId].delete.ts   # DELETE
│   │   ├── [decisionId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| description | text | No |
| type | string | No |
| status | string | No |
| position | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/playground-decisions

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/playground-decisions`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/playground-decisions?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/playground-decisions

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/playground-decisions`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/playground-decisions/{decisionId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/playground-decisions/${decisionId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/playground-decisions/{decisionId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/playground-decisions/${decisionId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: decisions, pending } = await useCollectionQuery('playgroundDecisions')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('playgroundDecisions')
</script>

<template>
  <PlaygroundDecisionsList />
</template>
```

### Schema & Validation

```typescript
import { playgroundDecisionSchema } from './app/composables/usePlaygroundDecisions'

// Validate data
const result = playgroundDecisionSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only decisions`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePlaygroundDecisions.ts`:
```typescript
export const playgroundDecisionSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/playground-decisions/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
