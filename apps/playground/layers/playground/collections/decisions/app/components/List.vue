<!--
  @crouton-generated
  @collection decisions
  @layer playground
  @generated 2025-12-18

  ## AI Context
  - List component for decisions collection
  - Collection name: playgroundDecisions
  - API endpoint: /api/teams/[id]/playground-decisions
  - Columns composable: usePlaygroundDecisions()
  - Column fields: title, description, type, status, position

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="playgroundDecisions"
    :columns="columns"
    :rows="decisions || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PlaygroundDecisions"
        :collection="'playgroundDecisions'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePlaygroundDecisions from '../composables/usePlaygroundDecisions'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = usePlaygroundDecisions()

const { items: decisions, pending } = await useCollectionQuery(
  'playgroundDecisions'
)
</script>