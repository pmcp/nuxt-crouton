# ThinkgraphCanvases Collection

> Generated by @fyit/crouton-cli on 2026-03-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphCanvases` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-canvases` |
| Form Component | `<ThinkgraphCanvasesForm />` |
| List Component | `<ThinkgraphCanvasesList />` |
| Composable | `useThinkgraphCanvases()` |

## File Structure

```
collections/canvases/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphCanvases.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-canvases/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [canvaseId].patch.ts    # UPDATE
│   │   └── [canvaseId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| epicId | string | No |
| epicBrief | text | No |
| status | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-canvases

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-canvases`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-canvases?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-canvases

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-canvases`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-canvases/{canvaseId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-canvases/${canvaseId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-canvases/{canvaseId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-canvases/${canvaseId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: canvases, pending } = await useCollectionQuery('thinkgraphCanvases')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphCanvases')
</script>

<template>
  <ThinkgraphCanvasesList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphCanvaseSchema } from './app/composables/useThinkgraphCanvases'

// Validate data
const result = thinkgraphCanvaseSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only canvases`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphCanvases.ts`:
```typescript
export const thinkgraphCanvaseSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-canvases/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
