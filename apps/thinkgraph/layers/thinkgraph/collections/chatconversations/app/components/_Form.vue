<!--
  @crouton-generated
  @collection chatconversations
  @layer thinkgraph
  @generated 2026-03-12

  ## AI Context
  - Form component for chatconversations collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/thinkgraph-chatconversations
  - Zod schema: useThinkgraphChatConversations() composable
  - Fields: nodeId, title, messages, provider, model, systemPrompt, metadata, messageCount, lastMessageAt

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Title" name="title" class="not-last:pb-4">
          <UInput v-model="state.title" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Messages" name="messages" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.messages === 'string' ? state.messages : JSON.stringify(state.messages, null, 2)"
            @update:model-value="(val) => { try { state.messages = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="NodeId" name="nodeId" class="not-last:pb-4">
          <UInput v-model="state.nodeId" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Provider" name="provider" class="not-last:pb-4">
          <UInput v-model="state.provider" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Model" name="model" class="not-last:pb-4">
          <UInput v-model="state.model" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="SystemPrompt" name="systemPrompt" class="not-last:pb-4">
          <UTextarea v-model="state.systemPrompt" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Metadata" name="metadata" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.metadata === 'string' ? state.metadata : JSON.stringify(state.metadata, null, 2)"
            @update:model-value="(val) => { try { state.metadata = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
        <UFormField label="MessageCount" name="messageCount" class="not-last:pb-4">
          <UInputNumber v-model="state.messageCount" class="w-full" />
        </UFormField>
        <UFormField label="LastMessageAt" name="lastMessageAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.lastMessageAt" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ThinkgraphChatConversationFormProps, ThinkgraphChatConversationFormData } from '../../types'
import useThinkgraphChatConversations from '../composables/useThinkgraphChatConversations'

const props = defineProps<ThinkgraphChatConversationFormProps>()
const { defaultValue, schema, collection } = useThinkgraphChatConversations()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Convert date strings to Date objects for date fields during editing
if (props.action === 'update' && props.activeItem?.id) {
  if (initialValues.lastMessageAt) {
    initialValues.lastMessageAt = new Date(initialValues.lastMessageAt)
  }
}

const state = ref<ThinkgraphChatConversationFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    // Serialize Date objects to ISO strings for API submission
    const serializedData = { ...state.value }
    if (serializedData.lastMessageAt instanceof Date) {
      serializedData.lastMessageAt = serializedData.lastMessageAt.toISOString()
    }

    if (props.action === 'create') {
      await create(serializedData)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, serializedData)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>