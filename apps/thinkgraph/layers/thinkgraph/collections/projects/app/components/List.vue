<!--
  @crouton-generated
  @collection projects
  @layer thinkgraph
  @generated 2026-03-19

  ## AI Context
  - List component for projects collection
  - Collection name: thinkgraphProjects
  - API endpoint: /api/teams/[id]/thinkgraph-projects
  - Columns composable: useThinkgraphProjects()
  - Column fields: name, appId, repoUrl, deployUrl, status, clientName, description

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="thinkgraphProjects"
    :columns="columns"
    :rows="projects || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ThinkgraphProjects"
        :collection="'thinkgraphProjects'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useThinkgraphProjects from '../composables/useThinkgraphProjects'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useThinkgraphProjects()

const { items: projects, pending } = await useCollectionQuery(
  'thinkgraphProjects'
)

</script>