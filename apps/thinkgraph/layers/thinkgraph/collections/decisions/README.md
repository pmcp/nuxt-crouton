# ThinkgraphDecisions Collection

> Generated by @fyit/crouton-cli on 2026-03-12

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphDecisions` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-decisions` |
| Form Component | `<ThinkgraphDecisionsForm />` |
| List Component | `<ThinkgraphDecisionsList />` |
| Composable | `useThinkgraphDecisions()` |

## File Structure

```
collections/decisions/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphDecisions.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-decisions/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [decisionId].patch.ts    # UPDATE
│   │   └── [decisionId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| content | text | Yes |
| nodeType | string | Yes |
| pathType | string | No |
| starred | boolean | No |
| branchName | string | No |
| versionTag | string | No |
| parentId | string | No |
| source | string | No |
| model | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-decisions

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-decisions`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-decisions?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-decisions

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-decisions`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-decisions/{decisionId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-decisions/${decisionId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-decisions/{decisionId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-decisions/${decisionId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: decisions, pending } = await useCollectionQuery('thinkgraphDecisions')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphDecisions')
</script>

<template>
  <ThinkgraphDecisionsList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphDecisionSchema } from './app/composables/useThinkgraphDecisions'

// Validate data
const result = thinkgraphDecisionSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only decisions`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphDecisions.ts`:
```typescript
export const thinkgraphDecisionSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-decisions/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
