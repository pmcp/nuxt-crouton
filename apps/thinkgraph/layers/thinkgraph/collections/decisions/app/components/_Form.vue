<!--
  @crouton-generated
  @collection decisions
  @layer thinkgraph
  @generated 2026-03-11

  ## AI Context
  - Form component for decisions collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/thinkgraph-decisions
  - Zod schema: useThinkgraphDecisions() composable
  - Fields: content, nodeType, pathType, starred, branchName, versionTag, parentId, source, model

  ## Customized
  - Simplified for graph UX: main area is just content
  - Metadata fields (nodeType, pathType, starred, branch, version) in collapsible sidebar
  - parentId, source, model are hidden (set programmatically)

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
        <div class="flex flex-col gap-4 p-1">
          <UFormField label="What are you thinking?" name="content">
            <UTextarea
              v-model="state.content"
              class="w-full"
              size="xl"
              autofocus
              :rows="4"
              placeholder="Describe your thought, decision, or idea..."
            />
          </UFormField>
        </div>
      </template>

      <template #sidebar>
        <div class="flex flex-col gap-4 p-1">
          <UFormField label="Type" name="nodeType">
            <USelect
              v-model="state.nodeType"
              :items="nodeTypeOptions"
              class="w-full"
              size="xl"
            />
          </UFormField>
          <UFormField label="Path" name="pathType">
            <USelect
              v-model="state.pathType"
              :items="pathTypeOptions"
              class="w-full"
              size="xl"
            />
          </UFormField>
          <UFormField label="Starred" name="starred">
            <UCheckbox v-model="state.starred" label="Mark as important" />
          </UFormField>
        </div>

        <div class="flex flex-col gap-4 p-1">
          <UFormField label="Branch" name="branchName">
            <UInput
              v-model="state.branchName"
              class="w-full"
              size="xl"
              placeholder="Optional branch name"
            />
          </UFormField>
          <UFormField label="Version" name="versionTag">
            <UInput
              v-model="state.versionTag"
              class="w-full"
              size="xl"
              placeholder="e.g. v1.0"
            />
          </UFormField>
        </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ThinkgraphDecisionFormProps, ThinkgraphDecisionFormData } from '../../types'
import useThinkgraphDecisions from '../composables/useThinkgraphDecisions'

const props = defineProps<ThinkgraphDecisionFormProps>()
const { defaultValue, schema, collection } = useThinkgraphDecisions()

const nodeTypeOptions = [
  { label: 'Decision', value: 'decision' },
  { label: 'Question', value: 'question' },
  { label: 'Idea', value: 'idea' },
  { label: 'Observation', value: 'observation' },
  { label: 'Parked', value: 'parked' },
]

const pathTypeOptions = [
  { label: 'Chosen', value: 'chosen' },
  { label: 'Explored', value: 'explored' },
  { label: 'Rejected', value: 'rejected' },
  { label: 'Pending', value: 'pending' },
]

// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<ThinkgraphDecisionFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
  }
}
</script>
