<!--
  @crouton-generated
  @collection decisions
  @layer thinkgraph
  @generated 2026-03-12

  ## AI Context
  - Custom form for thinkgraph decisions (nodes in the thinking graph)
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/thinkgraph-decisions
  - Node types: idea, insight, decision, question
  - Path types: diverge, deep_dive, prototype, converge, validate, park

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
        <div class="flex flex-col gap-4 p-1">
          <UFormField label="Content" name="content">
            <UTextarea v-model="state.content" class="w-full" size="xl" :rows="4" placeholder="Describe your thought, idea, or decision..." autofocus />
          </UFormField>
        </div>
      </template>

      <template #sidebar>
        <div class="flex flex-col gap-4 p-1">
          <UFormField label="Type" name="nodeType">
            <USelect v-model="state.nodeType" :items="nodeTypeOptions" class="w-full" size="xl" />
          </UFormField>

          <UFormField v-if="action === 'update'" label="Path" name="pathType">
            <USelect v-model="state.pathType" :items="pathTypeOptions" class="w-full" size="xl" />
          </UFormField>

          <UFormField v-if="action === 'update'" label="Starred" name="starred">
            <USwitch v-model="state.starred" />
          </UFormField>

          <UFormField v-if="action === 'update' && state.parentId" label="Parent" name="parentId">
            <UInput v-model="state.parentId" class="w-full" size="xl" disabled />
          </UFormField>
        </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ThinkgraphDecisionFormProps, ThinkgraphDecisionFormData } from '../../types'
import useThinkgraphDecisions from '../composables/useThinkgraphDecisions'

const props = defineProps<ThinkgraphDecisionFormProps>()
const { defaultValue, schema, collection } = useThinkgraphDecisions()

const nodeTypeOptions = [
  { label: 'Idea', value: 'idea' },
  { label: 'Insight', value: 'insight' },
  { label: 'Decision', value: 'decision' },
  { label: 'Question', value: 'question' },
]

const pathTypeOptions = [
  { label: 'None', value: '' },
  { label: 'Diverge', value: 'diverge' },
  { label: 'Deep Dive', value: 'deep_dive' },
  { label: 'Prototype', value: 'prototype' },
  { label: 'Converge', value: 'converge' },
  { label: 'Validate', value: 'validate' },
  { label: 'Park', value: 'park' },
]

const { create, update, deleteItems } = useCollectionMutation(collection)
const { close, loading } = useCrouton()

const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue, nodeType: 'idea', source: 'manual' }

const state = ref<ThinkgraphDecisionFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()
  } catch (error) {
    console.error('Form submission failed:', error)
  }
}
</script>