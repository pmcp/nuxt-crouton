# ThinkgraphInjectRequests Collection

> Generated by @fyit/crouton-cli on 2026-03-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphInjectRequests` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-injectrequests` |
| Form Component | `<ThinkgraphInjectRequestsForm />` |
| List Component | `<ThinkgraphInjectRequestsList />` |
| Composable | `useThinkgraphInjectRequests()` |

## Features

- References: nodeId → nodes

## File Structure

```
collections/injectrequests/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphInjectRequests.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-injectrequests/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [injectrequestId].patch.ts    # UPDATE
│   │   └── [injectrequestId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| nodeId | string (→ nodes) | Yes |
| fromUserId | string | No |
| content | text | Yes |
| status | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-injectrequests

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-injectrequests`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-injectrequests?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-injectrequests

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-injectrequests`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-injectrequests/{injectrequestId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-injectrequests/${injectrequestId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-injectrequests/{injectrequestId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-injectrequests/${injectrequestId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: injectRequests, pending } = await useCollectionQuery('thinkgraphInjectRequests')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphInjectRequests')
</script>

<template>
  <ThinkgraphInjectRequestsList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphInjectRequestSchema } from './app/composables/useThinkgraphInjectRequests'

// Validate data
const result = thinkgraphInjectRequestSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only injectrequests`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphInjectRequests.ts`:
```typescript
export const thinkgraphInjectRequestSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-injectrequests/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
