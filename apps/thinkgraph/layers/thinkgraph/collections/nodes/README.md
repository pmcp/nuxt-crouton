# ThinkgraphNodes Collection

> Generated by @fyit/crouton-cli on 2026-03-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphNodes` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-nodes` |
| Form Component | `<ThinkgraphNodesForm />` |
| List Component | `<ThinkgraphNodesList />` |
| Composable | `useThinkgraphNodes()` |

## Features

- Tree/hierarchy structure with parent-child relationships
- References: canvasId → canvases, parentId → nodes

## File Structure

```
collections/nodes/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphNodes.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-nodes/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [nodeId].patch.ts    # UPDATE
│   │   └── [nodeId].delete.ts   # DELETE
│   │   ├── [nodeId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| canvasId | string (→ canvases) | Yes |
| parentId | string (→ nodes) | No |
| nodeType | string | Yes |
| status | string | Yes |
| title | string | Yes |
| brief | text | No |
| output | text | No |
| handoffType | string | No |
| handoffMeta | json | No |
| contextScope | string | No |
| contextNodeIds | json | No |
| notionTaskId | string | No |
| worktree | string | No |
| sendTarget | string | No |
| sendMode | string | No |
| injectMode | string | No |
| origin | string | No |
| stepIndex | integer | No |
| skillVersion | string | No |
| tokenCount | integer | No |
| userId | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-nodes

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-nodes`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-nodes?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-nodes

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-nodes`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-nodes/{nodeId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-nodes/${nodeId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-nodes/{nodeId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-nodes/${nodeId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: nodes, pending } = await useCollectionQuery('thinkgraphNodes')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphNodes')
</script>

<template>
  <ThinkgraphNodesList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphNodeSchema } from './app/composables/useThinkgraphNodes'

// Validate data
const result = thinkgraphNodeSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only nodes`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphNodes.ts`:
```typescript
export const thinkgraphNodeSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-nodes/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
