<!--
  @crouton-generated
  @collection workitems
  @layer thinkgraph
  @generated 2026-03-19

  ## AI Context
  - Form component for workitems collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/thinkgraph-workitems
  - Zod schema: useThinkgraphWorkItems() composable
  - Fields: projectId, parentId, title, type, status, brief, output, assignee, provider, sessionId, worktree, deployUrl, skill, retrospective, artifacts

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
    @error="handleValidationError"
  >
    <CroutonFormLayout :tabs="tabs" :navigation-items="navigationItems" :tab-errors="tabErrorCounts" v-model="activeSection">
      <template #main="{ activeSection }">
      <div v-show="!tabs || activeSection === 'details'" class="flex flex-col gap-4 p-1">
        <UFormField label="Title" name="title" class="not-last:pb-4">
          <UInput v-model="state.title" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Brief" name="brief" class="not-last:pb-4">
          <UTextarea v-model="state.brief" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Output" name="output" class="not-last:pb-4">
          <UTextarea v-model="state.output" class="w-full" size="xl" />
        </UFormField>
      </div>

      <div v-show="!tabs || activeSection === 'output'" class="flex flex-col gap-4 p-1">
        <UFormField label="Retrospective" name="retrospective" class="not-last:pb-4">
          <UTextarea v-model="state.retrospective" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">

        <UFormField label="Parent" name="parentId" class="not-last:pb-4">
          <CroutonFormParentSelect
            v-model="state.parentId"
            collection="thinkgraphWorkItems"
            :current-id="state.id"
            label="Parent"
          />
        </UFormField>
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="ProjectId" name="projectId" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.projectId"
            collection="thinkgraphProjects"
            label="ProjectId"
          />
        </UFormField>
      </div>

      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Type" name="type" class="not-last:pb-4">
          <UInput v-model="state.type" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Status" name="status" class="not-last:pb-4">
          <UInput v-model="state.status" class="w-full" size="xl" />
        </UFormField>
      </div>

      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Assignee" name="assignee" class="not-last:pb-4">
          <UInput v-model="state.assignee" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Provider" name="provider" class="not-last:pb-4">
          <UInput v-model="state.provider" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="SessionId" name="sessionId" class="not-last:pb-4">
          <UInput v-model="state.sessionId" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Worktree" name="worktree" class="not-last:pb-4">
          <UInput v-model="state.worktree" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="DeployUrl" name="deployUrl" class="not-last:pb-4">
          <UInput v-model="state.deployUrl" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Skill" name="skill" class="not-last:pb-4">
          <UInput v-model="state.skill" class="w-full" size="xl" />
        </UFormField>
      </div>

      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Artifacts" name="artifacts" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.artifacts === 'string' ? state.artifacts : JSON.stringify(state.artifacts, null, 2)"
            @update:model-value="(val) => { try { state.artifacts = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonValidationErrorSummary
          v-if="validationErrors.length > 0"
          :tab-errors="tabErrorCounts"
          :navigation-items="navigationItems"
          @switch-tab="switchToTab"
        />

        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
          :has-validation-errors="validationErrors.length > 0"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ThinkgraphWorkItemFormProps, ThinkgraphWorkItemFormData } from '../../types'
import useThinkgraphWorkItems from '../composables/useThinkgraphWorkItems'

const props = defineProps<ThinkgraphWorkItemFormProps>()
const { defaultValue, schema, collection } = useThinkgraphWorkItems()

// Form layout configuration
const navigationItems = [
  { label: 'Details', value: 'details' },
  { label: 'Output', value: 'output' }
]

const tabs = ref(true)
const activeSection = ref('details')

// Map field names to their tab groups for error tracking
const fieldToGroup: Record<string, string> = {
  'title': 'details',
  'brief': 'details',
  'output': 'details',
  'retrospective': 'output'
}

// Track validation errors for tab indicators
const validationErrors = ref<Array<{ name: string; message: string }>>([])

// Handle form validation errors
const handleValidationError = (event: any) => {
  if (event?.errors) {
    validationErrors.value = event.errors
  }
}

// Compute errors per tab
const tabErrorCounts = computed(() => {
  const counts: Record<string, number> = {}

  validationErrors.value.forEach(error => {
    const tabName = fieldToGroup[error.name] || 'general'
    counts[tabName] = (counts[tabName] || 0) + 1
  })

  return counts
})

// Switch to a specific tab (for clicking error links)
const switchToTab = (tabValue: string) => {
  activeSection.value = tabValue
}

// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
// Hierarchy defaults for new items (parentId, path, depth, order)
const hierarchyDefaults = {
  parentId: null,
  path: '/',
  depth: 0,
  order: 0
}
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue, ...hierarchyDefaults }

const state = ref<ThinkgraphWorkItemFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    // Clear validation errors on successful submission
    validationErrors.value = []

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>