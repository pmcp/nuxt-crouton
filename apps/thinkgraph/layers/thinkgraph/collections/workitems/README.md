# ThinkgraphWorkItems Collection

> Generated by @fyit/crouton-cli on 2026-03-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphWorkItems` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-workitems` |
| Form Component | `<ThinkgraphWorkItemsForm />` |
| List Component | `<ThinkgraphWorkItemsList />` |
| Composable | `useThinkgraphWorkItems()` |

## Features

- Tree/hierarchy structure with parent-child relationships
- References: projectId → projects

## File Structure

```
collections/workitems/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphWorkItems.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-workitems/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [workitemId].patch.ts    # UPDATE
│   │   └── [workitemId].delete.ts   # DELETE
│   │   ├── [workitemId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| projectId | string (→ projects) | Yes |
| parentId | string | No |
| title | string | Yes |
| type | string | Yes |
| status | string | Yes |
| brief | text | No |
| output | text | No |
| assignee | string | No |
| provider | string | No |
| sessionId | string | No |
| worktree | string | No |
| deployUrl | string | No |
| skill | string | No |
| artifacts | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-workitems

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-workitems`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-workitems?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-workitems

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-workitems`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-workitems/{workitemId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-workitems/${workitemId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-workitems/{workitemId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-workitems/${workitemId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: workItems, pending } = await useCollectionQuery('thinkgraphWorkItems')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphWorkItems')
</script>

<template>
  <ThinkgraphWorkItemsList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphWorkItemSchema } from './app/composables/useThinkgraphWorkItems'

// Validate data
const result = thinkgraphWorkItemSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only workitems`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphWorkItems.ts`:
```typescript
export const thinkgraphWorkItemSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-workitems/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
