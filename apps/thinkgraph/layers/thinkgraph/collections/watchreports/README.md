# ThinkgraphWatchReports Collection

> Generated by @fyit/crouton-cli on 2026-04-07

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphWatchReports` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-watchreports` |
| Form Component | `<ThinkgraphWatchReportsForm />` |
| List Component | `<ThinkgraphWatchReportsList />` |
| Composable | `useThinkgraphWatchReports()` |

## Features

- References: repoId → watchedRepos

## File Structure

```
collections/watchreports/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphWatchReports.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-watchreports/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [watchreportId].patch.ts    # UPDATE
│   │   └── [watchreportId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| repoId | string (→ watchedRepos) | Yes |
| runDate | string | Yes |
| summary | text | No |
| commitsSinceLast | json | No |
| createdNodeIds | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-watchreports

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-watchreports`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-watchreports?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-watchreports

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-watchreports`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-watchreports/{watchreportId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-watchreports/${watchreportId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-watchreports/{watchreportId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-watchreports/${watchreportId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: watchReports, pending } = await useCollectionQuery('thinkgraphWatchReports')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphWatchReports')
</script>

<template>
  <ThinkgraphWatchReportsList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphWatchReportSchema } from './app/composables/useThinkgraphWatchReports'

// Validate data
const result = thinkgraphWatchReportSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only watchreports`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphWatchReports.ts`:
```typescript
export const thinkgraphWatchReportSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-watchreports/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
