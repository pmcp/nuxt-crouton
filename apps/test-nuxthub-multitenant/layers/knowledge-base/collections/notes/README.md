# KnowledgeBaseNotes Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-22

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `knowledgeBaseNotes` |
| Layer | `knowledge-base` |
| API Base | `/api/teams/[teamId]/knowledge-base-notes` |
| Form Component | `<KnowledgeBaseNotesForm />` |
| List Component | `<KnowledgeBaseNotesList />` |
| Composable | `useKnowledgeBaseNotes()` |

## File Structure

```
collections/notes/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useKnowledgeBaseNotes.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/knowledge-base-notes/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [noteId].patch.ts    # UPDATE
│   │   └── [noteId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| content | string | No |
| category | string | No |
| tags | json | No |
| isPinned | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/knowledge-base-notes

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/knowledge-base-notes`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/knowledge-base-notes?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/knowledge-base-notes

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/knowledge-base-notes`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/knowledge-base-notes/{noteId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/knowledge-base-notes/${noteId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/knowledge-base-notes/{noteId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/knowledge-base-notes/${noteId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: notes, pending } = await useCollectionQuery('knowledgeBaseNotes')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('knowledgeBaseNotes')
</script>

<template>
  <KnowledgeBaseNotesList />
</template>
```

### Schema & Validation

```typescript
import { knowledgeBaseNoteSchema } from './app/composables/useKnowledgeBaseNotes'

// Validate data
const result = knowledgeBaseNoteSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only notes`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useKnowledgeBaseNotes.ts`:
```typescript
export const knowledgeBaseNoteSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/knowledge-base-notes/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
