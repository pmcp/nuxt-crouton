<!--
  @crouton-generated
  @collection notes
  @layer knowledge-base
  @generated 2025-12-22

  ## AI Context
  - List component for notes collection
  - Collection name: knowledgeBaseNotes
  - API endpoint: /api/teams/[id]/knowledge-base-notes
  - Columns composable: useKnowledgeBaseNotes()
  - Column fields: title, content, category, tags, isPinned

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="knowledgeBaseNotes"
    :columns="columns"
    :rows="notes || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="KnowledgeBaseNotes"
        :collection="'knowledgeBaseNotes'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useKnowledgeBaseNotes from '../composables/useKnowledgeBaseNotes'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useKnowledgeBaseNotes()

const { items: notes, pending } = await useCollectionQuery(
  'knowledgeBaseNotes'
)
</script>