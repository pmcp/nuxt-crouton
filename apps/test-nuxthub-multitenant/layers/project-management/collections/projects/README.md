# ProjectManagementProjects Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-22

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `projectManagementProjects` |
| Layer | `project-management` |
| API Base | `/api/teams/[teamId]/project-management-projects` |
| Form Component | `<ProjectManagementProjectsForm />` |
| List Component | `<ProjectManagementProjectsList />` |
| Composable | `useProjectManagementProjects()` |

## File Structure

```
collections/projects/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useProjectManagementProjects.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/project-management-projects/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [projectId].patch.ts    # UPDATE
│   │   └── [projectId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| description | text | No |
| status | string | No |
| priority | string | No |
| startDate | date | No |
| dueDate | date | No |
| color | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/project-management-projects

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/project-management-projects`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/project-management-projects?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/project-management-projects

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/project-management-projects`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/project-management-projects/{projectId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/project-management-projects/${projectId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/project-management-projects/{projectId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/project-management-projects/${projectId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: projects, pending } = await useCollectionQuery('projectManagementProjects')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('projectManagementProjects')
</script>

<template>
  <ProjectManagementProjectsList />
</template>
```

### Schema & Validation

```typescript
import { projectManagementProjectSchema } from './app/composables/useProjectManagementProjects'

// Validate data
const result = projectManagementProjectSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only projects`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useProjectManagementProjects.ts`:
```typescript
export const projectManagementProjectSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/project-management-projects/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
