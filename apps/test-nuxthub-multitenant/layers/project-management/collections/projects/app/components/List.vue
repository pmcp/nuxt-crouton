<!--
  @crouton-generated
  @collection projects
  @layer project-management
  @generated 2025-12-22

  ## AI Context
  - List component for projects collection
  - Collection name: projectManagementProjects
  - API endpoint: /api/teams/[id]/project-management-projects
  - Columns composable: useProjectManagementProjects()
  - Column fields: name, description, status, priority, startDate, dueDate, color

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="projectManagementProjects"
    :columns="columns"
    :rows="projects || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ProjectManagementProjects"
        :collection="'projectManagementProjects'"
        createButton
      />
    </template>
    <template #startDate-cell="{ row }">
      <CroutonDate :date="row.original.startDate"></CroutonDate>
    </template>
    <template #dueDate-cell="{ row }">
      <CroutonDate :date="row.original.dueDate"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useProjectManagementProjects from '../composables/useProjectManagementProjects'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useProjectManagementProjects()

const { items: projects, pending } = await useCollectionQuery(
  'projectManagementProjects'
)
</script>