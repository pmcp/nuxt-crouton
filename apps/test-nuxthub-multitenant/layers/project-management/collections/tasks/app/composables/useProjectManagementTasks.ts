/**
 * @crouton-generated
 * @collection tasks
 * @layer project-management
 * @generated 2025-12-22
 *
 * ## AI Context
 * - Composable: useProjectManagementTasks
 * - Collection name: projectManagementTasks
 * - API endpoint: /api/teams/[id]/project-management-tasks
 * - Form component: ProjectManagementTasksForm
 * - List component: ProjectManagementTasksList
 * - Fields: id, title, description, status, priority, projectId, dueDate, completedAt, estimatedHours
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @friendlyinternet/nuxt-crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const projectManagementTaskSchema = z.object({
  title: z.string().min(1, 'title is required'),
  description: z.string().optional(),
  status: z.string().optional(),
  priority: z.string().optional(),
  projectId: z.string().optional(),
  dueDate: z.date().optional(),
  completedAt: z.string().optional(),
  estimatedHours: z.number().optional()
})

export const projectManagementTasksColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'description', header: 'Description' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'priority', header: 'Priority' },
  { accessorKey: 'projectId', header: 'ProjectId' },
  { accessorKey: 'dueDate', header: 'DueDate' },
  { accessorKey: 'completedAt', header: 'CompletedAt' },
  { accessorKey: 'estimatedHours', header: 'EstimatedHours' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _projectManagementTasksConfig = {
  name: 'projectManagementTasks',
  layer: 'project-management',
  apiPath: 'project-management-tasks',
  componentName: 'ProjectManagementTasksForm',
  defaultValues: {
    title: '',
    description: '',
    status: '',
    priority: '',
    projectId: '',
    dueDate: null,
    completedAt: '',
    estimatedHours: 0
  },
  columns: projectManagementTasksColumns,
  sortable: {
    enabled: true,
    orderField: 'order'
  },
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_projectManagementTasksConfig, 'schema', {
  value: projectManagementTaskSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const projectManagementTasksConfig = _projectManagementTasksConfig as typeof _projectManagementTasksConfig & { schema: typeof projectManagementTaskSchema }

export const useProjectManagementTasks = () => projectManagementTasksConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: projectManagementTasksConfig.defaultValues,
    schema: projectManagementTaskSchema,
    columns: projectManagementTasksConfig.columns,
    collection: projectManagementTasksConfig.name
  }
}