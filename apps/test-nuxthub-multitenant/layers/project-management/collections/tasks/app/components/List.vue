<!--
  @crouton-generated
  @collection tasks
  @layer project-management
  @generated 2025-12-22

  ## AI Context
  - List component for tasks collection
  - Collection name: projectManagementTasks
  - API endpoint: /api/teams/[id]/project-management-tasks
  - Columns composable: useProjectManagementTasks()
  - Column fields: title, description, status, priority, projectId, dueDate, completedAt, estimatedHours

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="projectManagementTasks"
    :columns="columns"
    :rows="tasks || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ProjectManagementTasks"
        :collection="'projectManagementTasks'"
        createButton
      />
    </template>
    <template #dueDate-cell="{ row }">
      <CroutonDate :date="row.original.dueDate"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useProjectManagementTasks from '../composables/useProjectManagementTasks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useProjectManagementTasks()

const { items: tasks, pending } = await useCollectionQuery(
  'projectManagementTasks'
)
</script>