/**
 * @crouton-generated
 * @collection tags
 * @layer content
 * @generated 2026-03-10
 *
 * ## AI Context
 * - Composable: useContentTags
 * - Collection name: contentTags
 * - API endpoint: /api/teams/[id]/content-tags
 * - Form component: ContentTagsForm
 * - List component: ContentTagsList
 * - Fields: name, color
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const contentTagSchema = z.object({
  name: z.string().min(1, 'name is required'),
  color: z.string().optional()
})

export const contentTagsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'color', header: 'Color' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _contentTagsConfig = {
  name: 'contentTags',
  layer: 'content',
  apiPath: 'content-tags',
  componentName: 'ContentTagsForm',
  defaultValues: {
    name: '',
    color: ''
  },
  columns: contentTagsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "color",
          "type": "string",
          "label": "Color",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_contentTagsConfig, 'schema', {
  value: contentTagSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const contentTagsConfig = _contentTagsConfig as typeof _contentTagsConfig & { schema: typeof contentTagSchema }

export const useContentTags = () => contentTagsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: contentTagsConfig.defaultValues,
    schema: contentTagSchema,
    columns: contentTagsConfig.columns,
    collection: contentTagsConfig.name
  }
}