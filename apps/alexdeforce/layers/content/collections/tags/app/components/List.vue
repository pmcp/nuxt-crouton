<!--
  @crouton-generated
  @collection tags
  @layer content
  @generated 2026-03-10

  ## AI Context
  - List component for tags collection
  - Collection name: contentTags
  - API endpoint: /api/teams/[id]/content-tags
  - Columns composable: useContentTags()
  - Column fields: name, color

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="contentTags"
    :columns="columns"
    :rows="tags || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ContentTags"
        :collection="'contentTags'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useContentTags from '../composables/useContentTags'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useContentTags()

const { items: tags, pending } = await useCollectionQuery(
  'contentTags'
)

</script>