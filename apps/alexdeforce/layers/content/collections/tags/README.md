# ContentTags Collection

> Generated by @fyit/crouton-cli on 2026-03-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentTags` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-tags` |
| Form Component | `<ContentTagsForm />` |
| List Component | `<ContentTagsList />` |
| Composable | `useContentTags()` |

## File Structure

```
collections/tags/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentTags.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-tags/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [tagId].patch.ts    # UPDATE
│   │   └── [tagId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| color | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-tags

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-tags`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-tags?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-tags

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-tags`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-tags/{tagId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-tags/${tagId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-tags/{tagId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-tags/${tagId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: tags, pending } = await useCollectionQuery('contentTags')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentTags')
</script>

<template>
  <ContentTagsList />
</template>
```

### Schema & Validation

```typescript
import { contentTagSchema } from './app/composables/useContentTags'

// Validate data
const result = contentTagSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only tags`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentTags.ts`:
```typescript
export const contentTagSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-tags/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
