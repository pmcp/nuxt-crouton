# ContentAgendas Collection

> Generated by @fyit/crouton-cli on 2026-03-06

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentAgendas` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-agendas` |
| Form Component | `<ContentAgendasForm />` |
| List Component | `<ContentAgendasList />` |
| Composable | `useContentAgendas()` |

## File Structure

```
collections/agendas/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentAgendas.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-agendas/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [agendaId].patch.ts    # UPDATE
│   │   └── [agendaId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| date | date | Yes |
| content | text | No |
| thumbnail | string | No |
| draft | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-agendas

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-agendas`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-agendas?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-agendas

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-agendas`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-agendas/{agendaId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-agendas/${agendaId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-agendas/{agendaId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-agendas/${agendaId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: agendas, pending } = await useCollectionQuery('contentAgendas')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentAgendas')
</script>

<template>
  <ContentAgendasList />
</template>
```

### Schema & Validation

```typescript
import { contentAgendaSchema } from './app/composables/useContentAgendas'

// Validate data
const result = contentAgendaSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only agendas`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentAgendas.ts`:
```typescript
export const contentAgendaSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-agendas/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
