# BlogPosts Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2025-12-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `blogPosts` |
| Layer | `blog` |
| API Base | `/api/teams/[teamId]/blog-posts` |
| Form Component | `<BlogPostsForm />` |
| List Component | `<BlogPostsList />` |
| Composable | `useBlogPosts()` |

## File Structure

```
collections/posts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBlogPosts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/blog-posts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [postId].patch.ts    # UPDATE
│   │   └── [postId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| slug | string | Yes |
| content | text | No |
| excerpt | text | No |
| published | boolean | No |
| publishedAt | string | No |
| sortOrder | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/blog-posts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/blog-posts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/blog-posts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/blog-posts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/blog-posts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/blog-posts/{postId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/blog-posts/${postId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/blog-posts/{postId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/blog-posts/${postId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: posts, pending } = await useCollectionQuery('blogPosts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('blogPosts')
</script>

<template>
  <BlogPostsList />
</template>
```

### Schema & Validation

```typescript
import { blogPostSchema } from './app/composables/useBlogPosts'

// Validate data
const result = blogPostSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only posts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBlogPosts.ts`:
```typescript
export const blogPostSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/blog-posts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
