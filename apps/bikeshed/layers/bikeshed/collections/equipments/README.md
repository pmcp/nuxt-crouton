# BikeshedEquipments Collection

> Generated by @fyit/crouton-cli on 2026-02-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bikeshedEquipments` |
| Layer | `bikeshed` |
| API Base | `/api/teams/[teamId]/bikeshed-equipments` |
| Form Component | `<BikeshedEquipmentsForm />` |
| List Component | `<BikeshedEquipmentsList />` |
| Composable | `useBikeshedEquipments()` |

## File Structure

```
collections/equipments/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBikeshedEquipments.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bikeshed-equipments/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [equipmentId].patch.ts    # UPDATE
│   │   └── [equipmentId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bikeshed-equipments

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bikeshed-equipments`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bikeshed-equipments?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bikeshed-equipments

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bikeshed-equipments`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bikeshed-equipments/{equipmentId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bikeshed-equipments/${equipmentId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bikeshed-equipments/{equipmentId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bikeshed-equipments/${equipmentId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: equipments, pending } = await useCollectionQuery('bikeshedEquipments')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bikeshedEquipments')
</script>

<template>
  <BikeshedEquipmentsList />
</template>
```

### Schema & Validation

```typescript
import { bikeshedEquipmentSchema } from './app/composables/useBikeshedEquipments'

// Validate data
const result = bikeshedEquipmentSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only equipments`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBikeshedEquipments.ts`:
```typescript
export const bikeshedEquipmentSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bikeshed-equipments/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
