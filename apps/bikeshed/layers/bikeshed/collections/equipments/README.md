# Bookingtest3Equipments Collection

> Generated by @fyit/crouton-cli on 2026-02-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingtest3Equipments` |
| Layer | `bookingtest3` |
| API Base | `/api/teams/[teamId]/bookingtest3-equipments` |
| Form Component | `<Bookingtest3EquipmentsForm />` |
| List Component | `<Bookingtest3EquipmentsList />` |
| Composable | `useBookingtest3Equipments()` |

## File Structure

```
collections/equipments/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingtest3Equipments.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookingtest3-equipments/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [equipmentId].patch.ts    # UPDATE
│   │   └── [equipmentId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookingtest3-equipments

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-equipments`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-equipments?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookingtest3-equipments

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookingtest3-equipments`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookingtest3-equipments/{equipmentId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookingtest3-equipments/${equipmentId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookingtest3-equipments/{equipmentId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookingtest3-equipments/${equipmentId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: equipments, pending } = await useCollectionQuery('bookingtest3Equipments')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingtest3Equipments')
</script>

<template>
  <Bookingtest3EquipmentsList />
</template>
```

### Schema & Validation

```typescript
import { bookingtest3EquipmentSchema } from './app/composables/useBookingtest3Equipments'

// Validate data
const result = bookingtest3EquipmentSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only equipments`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingtest3Equipments.ts`:
```typescript
export const bookingtest3EquipmentSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookingtest3-equipments/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
