<!--
  @crouton-generated
  @collection equipments
  @layer bikeshed
  @generated 2026-02-25

  ## AI Context
  - List component for equipments collection
  - Collection name: bikeshedEquipments
  - API endpoint: /api/teams/[id]/bikeshed-equipments
  - Columns composable: useBikeshedEquipments()
  - Column fields: display, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bikeshedEquipments"
    :columns="columns"
    :rows="equipments || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BikeshedEquipments"
        :collection="'bikeshedEquipments'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBikeshedEquipments from '../composables/useBikeshedEquipments'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBikeshedEquipments()

const { items: equipments, pending } = await useCollectionQuery(
  'bikeshedEquipments'
)

</script>