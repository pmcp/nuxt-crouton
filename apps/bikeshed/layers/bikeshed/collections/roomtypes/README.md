# Bookingtest3RoomTypes Collection

> Generated by @fyit/crouton-cli on 2026-02-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingtest3RoomTypes` |
| Layer | `bookingtest3` |
| API Base | `/api/teams/[teamId]/bookingtest3-roomtypes` |
| Form Component | `<Bookingtest3RoomTypesForm />` |
| List Component | `<Bookingtest3RoomTypesList />` |
| Composable | `useBookingtest3RoomTypes()` |

## File Structure

```
collections/roomtypes/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingtest3RoomTypes.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookingtest3-roomtypes/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [roomtypeId].patch.ts    # UPDATE
│   │   └── [roomtypeId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookingtest3-roomtypes

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-roomtypes`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-roomtypes?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookingtest3-roomtypes

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookingtest3-roomtypes`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookingtest3-roomtypes/{roomtypeId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookingtest3-roomtypes/${roomtypeId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookingtest3-roomtypes/{roomtypeId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookingtest3-roomtypes/${roomtypeId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: roomTypes, pending } = await useCollectionQuery('bookingtest3RoomTypes')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingtest3RoomTypes')
</script>

<template>
  <Bookingtest3RoomTypesList />
</template>
```

### Schema & Validation

```typescript
import { bookingtest3RoomTypeSchema } from './app/composables/useBookingtest3RoomTypes'

// Validate data
const result = bookingtest3RoomTypeSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only roomtypes`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingtest3RoomTypes.ts`:
```typescript
export const bookingtest3RoomTypeSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookingtest3-roomtypes/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
