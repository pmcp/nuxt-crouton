# BikeshedRoomTypes Collection

> Generated by @fyit/crouton-cli on 2026-02-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bikeshedRoomTypes` |
| Layer | `bikeshed` |
| API Base | `/api/teams/[teamId]/bikeshed-roomtypes` |
| Form Component | `<BikeshedRoomTypesForm />` |
| List Component | `<BikeshedRoomTypesList />` |
| Composable | `useBikeshedRoomTypes()` |

## File Structure

```
collections/roomtypes/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBikeshedRoomTypes.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bikeshed-roomtypes/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [roomtypeId].patch.ts    # UPDATE
│   │   └── [roomtypeId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bikeshed-roomtypes

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bikeshed-roomtypes`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bikeshed-roomtypes?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bikeshed-roomtypes

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bikeshed-roomtypes`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bikeshed-roomtypes/{roomtypeId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bikeshed-roomtypes/${roomtypeId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bikeshed-roomtypes/{roomtypeId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bikeshed-roomtypes/${roomtypeId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: roomTypes, pending } = await useCollectionQuery('bikeshedRoomTypes')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bikeshedRoomTypes')
</script>

<template>
  <BikeshedRoomTypesList />
</template>
```

### Schema & Validation

```typescript
import { bikeshedRoomTypeSchema } from './app/composables/useBikeshedRoomTypes'

// Validate data
const result = bikeshedRoomTypeSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only roomtypes`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBikeshedRoomTypes.ts`:
```typescript
export const bikeshedRoomTypeSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bikeshed-roomtypes/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
