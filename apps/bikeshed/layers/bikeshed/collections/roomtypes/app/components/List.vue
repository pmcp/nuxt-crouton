<!--
  @crouton-generated
  @collection roomtypes
  @layer bikeshed
  @generated 2026-02-25

  ## AI Context
  - List component for roomtypes collection
  - Collection name: bikeshedRoomTypes
  - API endpoint: /api/teams/[id]/bikeshed-roomtypes
  - Columns composable: useBikeshedRoomTypes()
  - Column fields: display, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bikeshedRoomTypes"
    :columns="columns"
    :rows="roomtypes || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BikeshedRoomTypes"
        :collection="'bikeshedRoomTypes'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBikeshedRoomTypes from '../composables/useBikeshedRoomTypes'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBikeshedRoomTypes()

const { items: roomTypes, pending } = await useCollectionQuery(
  'bikeshedRoomTypes'
)

</script>