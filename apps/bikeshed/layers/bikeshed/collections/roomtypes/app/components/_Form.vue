<!--
  @crouton-generated
  @collection roomtypes
  @layer bookingtest3
  @generated 2026-02-25

  ## AI Context
  - Form component for roomtypes collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/bookingtest3-roomtypes
  - Zod schema: useBookingtest3RoomTypes() composable
  - Fields: display, fields

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Display" name="display" class="not-last:pb-4">
          <UInput v-model="state.display" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Fields" name="fields" class="not-last:pb-4">
          <UInput v-model="state.fields" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { Bookingtest3RoomTypeFormProps, Bookingtest3RoomTypeFormData } from '../../types'
import useBookingtest3RoomTypes from '../composables/useBookingtest3RoomTypes'

const props = defineProps<Bookingtest3RoomTypeFormProps>()
const { defaultValue, schema, collection } = useBookingtest3RoomTypes()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<Bookingtest3RoomTypeFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>