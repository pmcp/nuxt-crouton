<!--
  @crouton-generated
  @collection bookingrequests
  @layer bikeshed
  @generated 2026-02-25

  ## AI Context
  - List component for bookingrequests collection
  - Collection name: bikeshedBookingRequests
  - API endpoint: /api/teams/[id]/bikeshed-bookingrequests
  - Columns composable: useBikeshedBookingRequests()
  - Column fields: display, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bikeshedBookingRequests"
    :columns="columns"
    :rows="bookingrequests || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BikeshedBookingRequests"
        :collection="'bikeshedBookingRequests'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBikeshedBookingRequests from '../composables/useBikeshedBookingRequests'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBikeshedBookingRequests()

const { items: bookingRequests, pending } = await useCollectionQuery(
  'bikeshedBookingRequests'
)

</script>