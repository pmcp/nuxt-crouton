# Bookingtest3Departments Collection

> Generated by @fyit/crouton-cli on 2026-02-25

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingtest3Departments` |
| Layer | `bookingtest3` |
| API Base | `/api/teams/[teamId]/bookingtest3-departments` |
| Form Component | `<Bookingtest3DepartmentsForm />` |
| List Component | `<Bookingtest3DepartmentsList />` |
| Composable | `useBookingtest3Departments()` |

## File Structure

```
collections/departments/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingtest3Departments.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookingtest3-departments/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [departmentId].patch.ts    # UPDATE
│   │   └── [departmentId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookingtest3-departments

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-departments`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookingtest3-departments?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookingtest3-departments

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookingtest3-departments`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookingtest3-departments/{departmentId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookingtest3-departments/${departmentId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookingtest3-departments/{departmentId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookingtest3-departments/${departmentId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: departments, pending } = await useCollectionQuery('bookingtest3Departments')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingtest3Departments')
</script>

<template>
  <Bookingtest3DepartmentsList />
</template>
```

### Schema & Validation

```typescript
import { bookingtest3DepartmentSchema } from './app/composables/useBookingtest3Departments'

// Validate data
const result = bookingtest3DepartmentSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only departments`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingtest3Departments.ts`:
```typescript
export const bookingtest3DepartmentSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookingtest3-departments/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
