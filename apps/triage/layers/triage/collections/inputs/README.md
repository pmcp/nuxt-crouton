# TriageInputs Collection

> Generated by @fyit/crouton-cli on 2026-03-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `triageInputs` |
| Layer | `triage` |
| API Base | `/api/teams/[teamId]/triage-inputs` |
| Form Component | `<TriageInputsForm />` |
| List Component | `<TriageInputsList />` |
| Composable | `useTriageInputs()` |

## Features

- References: flowId → flows

## File Structure

```
collections/inputs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTriageInputs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/triage-inputs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [inputId].patch.ts    # UPDATE
│   │   └── [inputId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| flowId | string (→ flows) | Yes |
| sourceType | string | Yes |
| name | string | Yes |
| apiToken | string | No |
| webhookUrl | string | No |
| webhookSecret | string | No |
| emailAddress | string | No |
| emailSlug | string | No |
| sourceMetadata | json | No |
| active | boolean | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/triage-inputs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/triage-inputs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/triage-inputs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/triage-inputs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/triage-inputs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/triage-inputs/{inputId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/triage-inputs/${inputId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/triage-inputs/{inputId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/triage-inputs/${inputId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: inputs, pending } = await useCollectionQuery('triageInputs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('triageInputs')
</script>

<template>
  <TriageInputsList />
</template>
```

### Schema & Validation

```typescript
import { triageInputSchema } from './app/composables/useTriageInputs'

// Validate data
const result = triageInputSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only inputs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTriageInputs.ts`:
```typescript
export const triageInputSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/triage-inputs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
