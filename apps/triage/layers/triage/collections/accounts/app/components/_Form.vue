<!--
  @crouton-generated
  @collection accounts
  @layer triage
  @generated 2026-02-11

  ## AI Context
  - Form component for accounts collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/triage-accounts
  - Zod schema: useTriageAccounts() composable
  - Fields: provider, label, providerAccountId, accessToken, accessTokenHint, refreshToken, tokenExpiresAt, scopes, providerMetadata, status, lastVerifiedAt

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Provider" name="provider" class="not-last:pb-4">
          <UInput v-model="state.provider" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Label" name="label" class="not-last:pb-4">
          <UInput v-model="state.label" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="ProviderAccountId" name="providerAccountId" class="not-last:pb-4">
          <UInput v-model="state.providerAccountId" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="AccessToken" name="accessToken" class="not-last:pb-4">
          <UInput v-model="state.accessToken" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="AccessTokenHint" name="accessTokenHint" class="not-last:pb-4">
          <UInput v-model="state.accessTokenHint" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="RefreshToken" name="refreshToken" class="not-last:pb-4">
          <UInput v-model="state.refreshToken" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="TokenExpiresAt" name="tokenExpiresAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.tokenExpiresAt" />
        </UFormField>
        <UFormField label="Scopes" name="scopes" class="not-last:pb-4">
          <UInput v-model="state.scopes" class="w-full" size="xl" />
        </UFormField>
      </div>

      <div class="flex flex-col gap-4 p-1">
        <UFormField label="ProviderMetadata" name="providerMetadata" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.providerMetadata === 'string' ? state.providerMetadata : JSON.stringify(state.providerMetadata, null, 2)"
            @update:model-value="(val) => { try { state.providerMetadata = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>

      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Status" name="status" class="not-last:pb-4">
          <UInput v-model="state.status" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="LastVerifiedAt" name="lastVerifiedAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.lastVerifiedAt" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { TriageAccountFormProps, TriageAccountFormData } from '../../types'
import useTriageAccounts from '../composables/useTriageAccounts'

const props = defineProps<TriageAccountFormProps>()
const { defaultValue, schema, collection } = useTriageAccounts()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Convert date strings to Date objects for date fields during editing
if (props.action === 'update' && props.activeItem?.id) {
  if (initialValues.tokenExpiresAt) {
    initialValues.tokenExpiresAt = new Date(initialValues.tokenExpiresAt)
  }
  if (initialValues.lastVerifiedAt) {
    initialValues.lastVerifiedAt = new Date(initialValues.lastVerifiedAt)
  }
}

const state = ref<TriageAccountFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    // Serialize Date objects to ISO strings for API submission
    const serializedData = { ...state.value }
    if (serializedData.tokenExpiresAt instanceof Date) {
      serializedData.tokenExpiresAt = serializedData.tokenExpiresAt.toISOString()
    }
    if (serializedData.lastVerifiedAt instanceof Date) {
      serializedData.lastVerifiedAt = serializedData.lastVerifiedAt.toISOString()
    }

    if (props.action === 'create') {
      await create(serializedData)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, serializedData)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>