/**
 * @crouton-generated
 * @collection accounts
 * @layer triage
 * @generated 2026-02-11
 *
 * ## AI Context
 * - Composable: useTriageAccounts
 * - Collection name: triageAccounts
 * - API endpoint: /api/teams/[id]/triage-accounts
 * - Form component: TriageAccountsForm
 * - List component: TriageAccountsList
 * - Fields: provider, label, providerAccountId, accessToken, accessTokenHint, refreshToken, tokenExpiresAt, scopes, providerMetadata, status, lastVerifiedAt
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const triageAccountSchema = z.object({
  provider: z.string().min(1, 'provider is required'),
  label: z.string().min(1, 'label is required'),
  providerAccountId: z.string().min(1, 'providerAccountId is required'),
  accessToken: z.string().min(1, 'accessToken is required'),
  accessTokenHint: z.string().optional(),
  refreshToken: z.string().optional(),
  tokenExpiresAt: z.date().optional(),
  scopes: z.string().optional(),
  providerMetadata: z.record(z.string(), z.any()).optional(),
  status: z.string().min(1, 'status is required'),
  lastVerifiedAt: z.date().optional()
})

export const triageAccountsColumns = [
  { accessorKey: 'provider', header: 'Provider' },
  { accessorKey: 'label', header: 'Label' },
  { accessorKey: 'providerAccountId', header: 'ProviderAccountId' },
  { accessorKey: 'accessToken', header: 'AccessToken' },
  { accessorKey: 'accessTokenHint', header: 'AccessTokenHint' },
  { accessorKey: 'refreshToken', header: 'RefreshToken' },
  { accessorKey: 'tokenExpiresAt', header: 'TokenExpiresAt' },
  { accessorKey: 'scopes', header: 'Scopes' },
  { accessorKey: 'providerMetadata', header: 'ProviderMetadata' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'lastVerifiedAt', header: 'LastVerifiedAt' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _triageAccountsConfig = {
  name: 'triageAccounts',
  layer: 'triage',
  apiPath: 'triage-accounts',
  componentName: 'TriageAccountsForm',
  defaultValues: {
    provider: '',
    label: '',
    providerAccountId: '',
    accessToken: '',
    accessTokenHint: '',
    refreshToken: '',
    tokenExpiresAt: null,
    scopes: '',
    providerMetadata: {},
    status: '',
    lastVerifiedAt: null
  },
  columns: triageAccountsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_triageAccountsConfig, 'schema', {
  value: triageAccountSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const triageAccountsConfig = _triageAccountsConfig as typeof _triageAccountsConfig & { schema: typeof triageAccountSchema }

export const useTriageAccounts = () => triageAccountsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: triageAccountsConfig.defaultValues,
    schema: triageAccountSchema,
    columns: triageAccountsConfig.columns,
    collection: triageAccountsConfig.name
  }
}