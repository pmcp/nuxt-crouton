# PagesPages Collection

> Generated by @fyit/crouton-cli on 2026-02-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `pagesPages` |
| Layer | `pages` |
| API Base | `/api/teams/[teamId]/pages-pages` |
| Form Component | `<PagesPagesForm />` |
| List Component | `<PagesPagesList />` |
| Composable | `usePagesPages()` |

## Features

- Tree/hierarchy structure with parent-child relationships

## File Structure

```
collections/pages/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePagesPages.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/pages-pages/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [pageId].patch.ts    # UPDATE
│   │   └── [pageId].delete.ts   # DELETE
│   │   ├── [pageId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | No |
| slug | string | No |
| pageType | string | Yes |
| content | text | No |
| config | json | No |
| status | string | Yes |
| visibility | string | Yes |
| publishedAt | date | No |
| showInNavigation | boolean | No |
| layout | string | No |
| seoTitle | string | No |
| seoDescription | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/pages-pages

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/pages-pages`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/pages-pages?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/pages-pages

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/pages-pages`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/pages-pages/{pageId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/pages-pages/${pageId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/pages-pages/{pageId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/pages-pages/${pageId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: pages, pending } = await useCollectionQuery('pagesPages')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('pagesPages')
</script>

<template>
  <PagesPagesList />
</template>
```

### Schema & Validation

```typescript
import { pagesPageSchema } from './app/composables/usePagesPages'

// Validate data
const result = pagesPageSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only pages`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePagesPages.ts`:
```typescript
export const pagesPageSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/pages-pages/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
