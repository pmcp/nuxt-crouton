# CategorizeCategorizeLayouts Collection

> Generated by @fyit/crouton-cli on 2026-03-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `categorizeCategorizeLayouts` |
| Layer | `categorize` |
| API Base | `/api/teams/[teamId]/categorize-categorize-layouts` |
| Form Component | `<CategorizeCategorizeLayoutsForm />` |
| List Component | `<CategorizeCategorizeLayoutsList />` |
| Composable | `useCategorizeCategorizeLayouts()` |

## File Structure

```
collections/categorize-layouts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useCategorizeCategorizeLayouts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/categorize-categorize-layouts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [categorize-layoutId].patch.ts    # UPDATE
│   │   └── [categorize-layoutId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | No |
| databaseId | string | Yes |
| accountId | string | Yes |
| categoryProperty | string | Yes |
| layout | json | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/categorize-categorize-layouts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/categorize-categorize-layouts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/categorize-categorize-layouts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/categorize-categorize-layouts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/categorize-categorize-layouts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/categorize-categorize-layouts/{categorize-layoutId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/categorize-categorize-layouts/${categorize-layoutId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/categorize-categorize-layouts/{categorize-layoutId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/categorize-categorize-layouts/${categorize-layoutId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: categorizeLayouts, pending } = await useCollectionQuery('categorizeCategorizeLayouts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('categorizeCategorizeLayouts')
</script>

<template>
  <CategorizeCategorizeLayoutsList />
</template>
```

### Schema & Validation

```typescript
import { categorizeCategorizeLayoutSchema } from './app/composables/useCategorizeCategorizeLayouts'

// Validate data
const result = categorizeCategorizeLayoutSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only categorize-layouts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useCategorizeCategorizeLayouts.ts`:
```typescript
export const categorizeCategorizeLayoutSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/categorize-categorize-layouts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
