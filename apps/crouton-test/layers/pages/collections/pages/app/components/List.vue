<!--
  @crouton-generated
  @collection pages
  @layer pages
  @generated 2026-01-20

  ## AI Context
  - List component for pages collection
  - Collection name: pagesPages
  - API endpoint: /api/teams/[id]/pages-pages
  - Columns composable: usePagesPages()
  - Column fields: title, slug, pageType, content, config, status, visibility, publishedAt, showInNavigation, layout, seoTitle, seoDescription

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="pagesPages"
    :columns="columns"
    :rows="pages || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="PagesPages"
        :collection="'pagesPages'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #slug-cell="{ row }">
      {{ t(row.original, 'slug') }}
    </template>
    <template #content-cell="{ row }">
      {{ t(row.original, 'content') }}
    </template>
    <template #seoTitle-cell="{ row }">
      {{ t(row.original, 'seoTitle') }}
    </template>
    <template #seoDescription-cell="{ row }">
      {{ t(row.original, 'seoDescription') }}
    </template>
    <template #publishedAt-cell="{ row }">
      <CroutonDate :date="row.original.publishedAt"></CroutonDate>
    </template>
    <template #showInNavigation-cell="{ row }">
      <CroutonBoolean :value="row.original.showInNavigation" />
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title', 'slug', 'content', 'seoTitle', 'seoDescription']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import usePagesPages from '../composables/usePagesPages'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = usePagesPages()

const { items: pages, pending } = await useCollectionQuery(
  'pagesPages'
)

</script>