# BookingsSettings Collection

> Generated by @fyit/crouton-cli on 2026-02-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsSettings` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-settings` |
| Form Component | `<BookingsSettingsForm />` |
| List Component | `<BookingsSettingsList />` |
| Composable | `useBookingsSettings()` |

## Features

- Repeater fields: statuses, groups

## File Structure

```
collections/settings/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsSettings.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-settings/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [settingId].patch.ts    # UPDATE
│   │   └── [settingId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| statuses | repeater | No |
| groups | repeater | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-settings

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-settings`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-settings?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-settings

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-settings`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-settings/{settingId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-settings/${settingId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-settings/{settingId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-settings/${settingId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: settings, pending } = await useCollectionQuery('bookingsSettings')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsSettings')
</script>

<template>
  <BookingsSettingsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsSettingSchema } from './app/composables/useBookingsSettings'

// Validate data
const result = bookingsSettingSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only settings`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsSettings.ts`:
```typescript
export const bookingsSettingSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-settings/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
