# BookingsAssets Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAssets` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-assets` |
| Form Component | `<BookingsAssetsForm />` |
| List Component | `<BookingsAssetsList />` |
| Composable | `useBookingsAssets()` |

## Features

- References: teamId → organisations, userId → users, updatedBy → users

## File Structure

```
collections/assets/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAssets.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-assets/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [assetId].patch.ts    # UPDATE
│   │   └── [assetId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| teamId | string (→ organisations) | Yes |
| userId | string (→ users) | Yes |
| filename | string | Yes |
| pathname | string | Yes |
| contentType | string | No |
| size | number | No |
| category | string | No |
| width | number | No |
| height | number | No |
| alt | string | No |
| uploadedAt | date | No |
| updatedBy | string (→ users) | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-assets

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-assets`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-assets?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-assets

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-assets`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-assets/{assetId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-assets/${assetId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-assets/{assetId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-assets/${assetId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: assets, pending } = await useCollectionQuery('bookingsAssets')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAssets')
</script>

<template>
  <BookingsAssetsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAssetSchema } from './app/composables/useBookingsAssets'

// Validate data
const result = bookingsAssetSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only assets`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAssets.ts`:
```typescript
export const bookingsAssetSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-assets/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
