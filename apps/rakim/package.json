{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt build && wrangler pages deploy dist",
    "deploy:preview": "nuxt build && wrangler pages deploy dist --branch preview",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply rakim-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply rakim-db --remote"
  },
  "dependencies": {
    "@friendlyinternet/nuxt-crouton": "workspace:*",
    "@friendlyinternet/nuxt-crouton-ai": "workspace:*",
    "@libsql/client": "^0.17.0",
    "human-id": "^4.1.1",
    "cheerio": "^1.1.2",
    "drizzle-orm": "^0.45.1",
    "nuxt": "^4.2.2",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@friendlyinternet/nuxt-crouton-cli": "workspace:*",
    "drizzle-kit": "^0.31.0"
  }
}
