/**
 * @crouton-generated
 * @collection flows
 * @layer rakim
 * @generated 2026-01-15
 *
 * ## AI Context
 * - Composable: useRakimFlows
 * - Collection name: rakimFlows
 * - API endpoint: /api/teams/[id]/rakim-flows
 * - Form component: RakimFlowsForm
 * - List component: RakimFlowsList
 * - Fields: name, description, availableDomains, aiEnabled, anthropicApiKey, aiSummaryPrompt, aiTaskPrompt, active, onboardingComplete
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @friendlyinternet/nuxt-crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const rakimFlowSchema = z.object({
  name: z.string().min(1, 'name is required'),
  description: z.string().optional(),
  availableDomains: z.array(z.string()).optional(),
  aiEnabled: z.boolean(),
  anthropicApiKey: z.string().optional(),
  aiSummaryPrompt: z.string().optional(),
  aiTaskPrompt: z.string().optional(),
  active: z.boolean(),
  onboardingComplete: z.boolean()
})

export const rakimFlowsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'description', header: 'Description' },
  { accessorKey: 'availableDomains', header: 'AvailableDomains' },
  { accessorKey: 'aiEnabled', header: 'AiEnabled' },
  { accessorKey: 'anthropicApiKey', header: 'AnthropicApiKey' },
  { accessorKey: 'aiSummaryPrompt', header: 'AiSummaryPrompt' },
  { accessorKey: 'aiTaskPrompt', header: 'AiTaskPrompt' },
  { accessorKey: 'active', header: 'Active' },
  { accessorKey: 'onboardingComplete', header: 'OnboardingComplete' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _rakimFlowsConfig = {
  name: 'rakimFlows',
  layer: 'rakim',
  apiPath: 'rakim-flows',
  componentName: 'RakimFlowsForm',
  defaultValues: {
    name: '',
    description: '',
    availableDomains: [],
    aiEnabled: false,
    anthropicApiKey: '',
    aiSummaryPrompt: '',
    aiTaskPrompt: '',
    active: false,
    onboardingComplete: false
  },
  columns: rakimFlowsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_rakimFlowsConfig, 'schema', {
  value: rakimFlowSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const rakimFlowsConfig = _rakimFlowsConfig as typeof _rakimFlowsConfig & { schema: typeof rakimFlowSchema }

export const useRakimFlows = () => rakimFlowsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: rakimFlowsConfig.defaultValues,
    schema: rakimFlowSchema,
    columns: rakimFlowsConfig.columns,
    collection: rakimFlowsConfig.name
  }
}