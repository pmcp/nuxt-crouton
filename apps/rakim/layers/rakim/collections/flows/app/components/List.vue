<!--
  @crouton-generated
  @collection flows
  @layer rakim
  @generated 2026-01-15

  ## AI Context
  - List component for flows collection
  - Collection name: rakimFlows
  - API endpoint: /api/teams/[id]/rakim-flows
  - Columns composable: useRakimFlows()
  - Column fields: name, description, availableDomains, aiEnabled, anthropicApiKey, aiSummaryPrompt, aiTaskPrompt, active, onboardingComplete

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @friendlyinternet/nuxt-crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="rakimFlows"
    :columns="columns"
    :rows="flows || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="RakimFlows"
        :collection="'rakimFlows'"
        createButton
      />
    </template>
    <template #availableDomains-cell="{ row }">
      <div v-if="row.original.availableDomains && row.original.availableDomains.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.availableDomains"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #aiEnabled-cell="{ row }">
      <CroutonBoolean :value="row.original.aiEnabled" />
    </template>
    <template #active-cell="{ row }">
      <CroutonBoolean :value="row.original.active" />
    </template>
    <template #onboardingComplete-cell="{ row }">
      <CroutonBoolean :value="row.original.onboardingComplete" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useRakimFlows from '../composables/useRakimFlows'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useRakimFlows()

const { items: flows, pending } = await useCollectionQuery(
  'rakimFlows'
)
</script>