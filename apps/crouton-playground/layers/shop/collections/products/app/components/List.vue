<!--
  @crouton-generated
  @collection products
  @layer shop
  @generated 2026-02-20

  ## AI Context
  - List component for products collection
  - Collection name: shopProducts
  - API endpoint: /api/teams/[id]/shop-products
  - Columns composable: useShopProducts()
  - Column fields: name, description, shortDescription, price, stock, featured, inStock, thumbnail, tags

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="shopProducts"
    :columns="columns"
    :rows="products || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ShopProducts"
        :collection="'shopProducts'"
        createButton
      />
    </template>
    <template #tags-cell="{ row }">
      <div v-if="row.original.tags && row.original.tags.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.tags"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #featured-cell="{ row }">
      <CroutonBoolean :value="row.original.featured" />
    </template>
    <template #inStock-cell="{ row }">
      <CroutonBoolean :value="row.original.inStock" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useShopProducts from '../composables/useShopProducts'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useShopProducts()

const { items: products, pending } = await useCollectionQuery(
  'shopProducts'
)

</script>