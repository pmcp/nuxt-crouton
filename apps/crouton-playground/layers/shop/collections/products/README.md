# ShopProducts Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `shopProducts` |
| Layer | `shop` |
| API Base | `/api/teams/[teamId]/shop-products` |
| Form Component | `<ShopProductsForm />` |
| List Component | `<ShopProductsList />` |
| Composable | `useShopProducts()` |

## File Structure

```
collections/products/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useShopProducts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/shop-products/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [productId].patch.ts    # UPDATE
│   │   └── [productId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| description | text | No |
| shortDescription | text | No |
| price | decimal | Yes |
| stock | number | No |
| featured | boolean | No |
| inStock | boolean | No |
| thumbnail | image | No |
| tags | array | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/shop-products

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/shop-products`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/shop-products?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/shop-products

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/shop-products`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/shop-products/{productId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/shop-products/${productId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/shop-products/{productId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/shop-products/${productId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: products, pending } = await useCollectionQuery('shopProducts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('shopProducts')
</script>

<template>
  <ShopProductsList />
</template>
```

### Schema & Validation

```typescript
import { shopProductSchema } from './app/composables/useShopProducts'

// Validate data
const result = shopProductSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only products`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useShopProducts.ts`:
```typescript
export const shopProductSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/shop-products/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
