# ShopCategories Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `shopCategories` |
| Layer | `shop` |
| API Base | `/api/teams/[teamId]/shop-categories` |
| Form Component | `<ShopCategoriesForm />` |
| List Component | `<ShopCategoriesList />` |
| Composable | `useShopCategories()` |

## Features

- Tree/hierarchy structure with parent-child relationships

## File Structure

```
collections/categories/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useShopCategories.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/shop-categories/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [categorieId].patch.ts    # UPDATE
│   │   └── [categorieId].delete.ts   # DELETE
│   │   ├── [categorieId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| slug | string | No |
| description | text | No |
| icon | image | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/shop-categories

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/shop-categories`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/shop-categories?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/shop-categories

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/shop-categories`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/shop-categories/{categorieId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/shop-categories/${categorieId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/shop-categories/{categorieId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/shop-categories/${categorieId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: categories, pending } = await useCollectionQuery('shopCategories')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('shopCategories')
</script>

<template>
  <ShopCategoriesList />
</template>
```

### Schema & Validation

```typescript
import { shopCategorieSchema } from './app/composables/useShopCategories'

// Validate data
const result = shopCategorieSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only categories`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useShopCategories.ts`:
```typescript
export const shopCategorieSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/shop-categories/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
