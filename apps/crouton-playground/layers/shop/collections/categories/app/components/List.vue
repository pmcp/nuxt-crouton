<!--
  @crouton-generated
  @collection categories
  @layer shop
  @generated 2026-02-20

  ## AI Context
  - List component for categories collection
  - Collection name: shopCategories
  - API endpoint: /api/teams/[id]/shop-categories
  - Columns composable: useShopCategories()
  - Column fields: name, slug, description, icon

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="shopCategories"
    :columns="columns"
    :rows="categories || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ShopCategories"
        :collection="'shopCategories'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useShopCategories from '../composables/useShopCategories'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useShopCategories()

const { items: categories, pending } = await useCollectionQuery(
  'shopCategories'
)

</script>