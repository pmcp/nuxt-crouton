# ShopOrders Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `shopOrders` |
| Layer | `shop` |
| API Base | `/api/teams/[teamId]/shop-orders` |
| Form Component | `<ShopOrdersForm />` |
| List Component | `<ShopOrdersList />` |
| Composable | `useShopOrders()` |

## Features

- References: productId → products

## File Structure

```
collections/orders/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useShopOrders.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/shop-orders/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [orderId].patch.ts    # UPDATE
│   │   └── [orderId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| orderNumber | string | Yes |
| customerName | string | Yes |
| customerEmail | string | Yes |
| productId | string (→ products) | No |
| quantity | number | Yes |
| total | decimal | Yes |
| isPaid | boolean | No |
| orderedAt | date | No |
| shippingAddress | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/shop-orders

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/shop-orders`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/shop-orders?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/shop-orders

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/shop-orders`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/shop-orders/{orderId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/shop-orders/${orderId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/shop-orders/{orderId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/shop-orders/${orderId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: orders, pending } = await useCollectionQuery('shopOrders')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('shopOrders')
</script>

<template>
  <ShopOrdersList />
</template>
```

### Schema & Validation

```typescript
import { shopOrderSchema } from './app/composables/useShopOrders'

// Validate data
const result = shopOrderSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only orders`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useShopOrders.ts`:
```typescript
export const shopOrderSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/shop-orders/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
