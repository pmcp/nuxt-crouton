<!--
  @crouton-generated
  @collection orders
  @layer shop
  @generated 2026-02-20

  ## AI Context
  - Form component for orders collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/shop-orders
  - Zod schema: useShopOrders() composable
  - Fields: orderNumber, customerName, customerEmail, productId, quantity, total, isPaid, orderedAt, shippingAddress

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="OrderNumber" name="orderNumber" class="not-last:pb-4">
          <UInput v-model="state.orderNumber" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="CustomerName" name="customerName" class="not-last:pb-4">
          <UInput v-model="state.customerName" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="CustomerEmail" name="customerEmail" class="not-last:pb-4">
          <UInput v-model="state.customerEmail" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="ProductId" name="productId" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.productId"
            collection="shopProducts"
            label="ProductId"
          />
        </UFormField>
        <UFormField label="Quantity" name="quantity" class="not-last:pb-4">
          <UInputNumber v-model="state.quantity" class="w-full" />
        </UFormField>
        <UFormField label="ShippingAddress" name="shippingAddress" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.shippingAddress === 'string' ? state.shippingAddress : JSON.stringify(state.shippingAddress, null, 2)"
            @update:model-value="(val) => { try { state.shippingAddress = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Total" name="total" class="not-last:pb-4">
          <UInputNumber v-model="state.total" class="w-full" />
        </UFormField>
        <UFormField label="IsPaid" name="isPaid" class="not-last:pb-4">
          <UCheckbox v-model="state.isPaid" />
        </UFormField>
        <UFormField label="OrderedAt" name="orderedAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.orderedAt" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ShopOrderFormProps, ShopOrderFormData } from '../../types'
import useShopOrders from '../composables/useShopOrders'

const props = defineProps<ShopOrderFormProps>()
const { defaultValue, schema, collection } = useShopOrders()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Convert date strings to Date objects for date fields during editing
if (props.action === 'update' && props.activeItem?.id) {
  if (initialValues.orderedAt) {
    initialValues.orderedAt = new Date(initialValues.orderedAt)
  }
}

const state = ref<ShopOrderFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    // Serialize Date objects to ISO strings for API submission
    const serializedData = { ...state.value }
    if (serializedData.orderedAt instanceof Date) {
      serializedData.orderedAt = serializedData.orderedAt.toISOString()
    }

    if (props.action === 'create') {
      await create(serializedData)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, serializedData)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>