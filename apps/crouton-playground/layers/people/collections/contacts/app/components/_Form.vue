<!--
  @crouton-generated
  @collection contacts
  @layer people
  @generated 2026-02-20

  ## AI Context
  - Form component for contacts collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/people-contacts
  - Zod schema: usePeopleContacts() composable
  - Fields: name, email, phone, website, bio, avatar, resume, active, birthday, socialLinks, skills

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Name" name="name" class="not-last:pb-4">
          <UInput v-model="state.name" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Email" name="email" class="not-last:pb-4">
          <UInput v-model="state.email" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Phone" name="phone" class="not-last:pb-4">
          <UInput v-model="state.phone" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Website" name="website" class="not-last:pb-4">
          <UInput v-model="state.website" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Bio" name="bio" class="not-last:pb-4">
          <UTextarea v-model="state.bio" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="SocialLinks" name="socialLinks" class="not-last:pb-4">
          <UTextarea
            :model-value="typeof state.socialLinks === 'string' ? state.socialLinks : JSON.stringify(state.socialLinks, null, 2)"
            @update:model-value="(val) => { try { state.socialLinks = val ? JSON.parse(val) : {} } catch (e) { console.error('Invalid JSON:', e) } }"
            class="w-full font-mono text-sm"
            :rows="8"
            placeholder="Enter JSON object"
          />
        </UFormField>
        <UFormField label="Skills" name="skills" class="not-last:pb-4">
          <UTextarea
            :model-value="Array.isArray(state.skills) ? state.skills.join('\n') : ''"
            @update:model-value="(val) => state.skills = val ? val.split('\n').filter(Boolean) : []"
            class="w-full"
            :rows="6"
            placeholder="Enter one value per line"
          />
          <p class="text-sm text-gray-500 mt-1">Enter one value per line</p>
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Avatar" name="avatar" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.avatar"
            :crop="true"
          />
        </UFormField>
        <UFormField label="Resume" name="resume" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.resume"
          />
        </UFormField>
        <UFormField label="Active" name="active" class="not-last:pb-4">
          <UCheckbox v-model="state.active" />
        </UFormField>
        <UFormField label="Birthday" name="birthday" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.birthday" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { PeopleContactFormProps, PeopleContactFormData } from '../../types'
import usePeopleContacts from '../composables/usePeopleContacts'

const props = defineProps<PeopleContactFormProps>()
const { defaultValue, schema, collection } = usePeopleContacts()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Convert date strings to Date objects for date fields during editing
if (props.action === 'update' && props.activeItem?.id) {
  if (initialValues.birthday) {
    initialValues.birthday = new Date(initialValues.birthday)
  }
}

const state = ref<PeopleContactFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    // Serialize Date objects to ISO strings for API submission
    const serializedData = { ...state.value }
    if (serializedData.birthday instanceof Date) {
      serializedData.birthday = serializedData.birthday.toISOString()
    }

    if (props.action === 'create') {
      await create(serializedData)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, serializedData)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>