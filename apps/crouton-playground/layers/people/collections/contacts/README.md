# PeopleContacts Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `peopleContacts` |
| Layer | `people` |
| API Base | `/api/teams/[teamId]/people-contacts` |
| Form Component | `<PeopleContactsForm />` |
| List Component | `<PeopleContactsList />` |
| Composable | `usePeopleContacts()` |

## File Structure

```
collections/contacts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── usePeopleContacts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/people-contacts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [contactId].patch.ts    # UPDATE
│   │   └── [contactId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| email | string | Yes |
| phone | string | No |
| website | string | No |
| bio | text | No |
| avatar | image | No |
| resume | file | No |
| active | boolean | No |
| birthday | date | No |
| socialLinks | json | No |
| skills | array | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/people-contacts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/people-contacts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/people-contacts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/people-contacts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/people-contacts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/people-contacts/{contactId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/people-contacts/${contactId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/people-contacts/{contactId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/people-contacts/${contactId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: contacts, pending } = await useCollectionQuery('peopleContacts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('peopleContacts')
</script>

<template>
  <PeopleContactsList />
</template>
```

### Schema & Validation

```typescript
import { peopleContactSchema } from './app/composables/usePeopleContacts'

// Validate data
const result = peopleContactSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only contacts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/usePeopleContacts.ts`:
```typescript
export const peopleContactSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/people-contacts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
