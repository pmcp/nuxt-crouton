# ProjectsTasks Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `projectsTasks` |
| Layer | `projects` |
| API Base | `/api/teams/[teamId]/projects-tasks` |
| Form Component | `<ProjectsTasksForm />` |
| List Component | `<ProjectsTasksList />` |
| Composable | `useProjectsTasks()` |

## Features

- References: assigneeId → contacts
- Repeater fields: subtasks

## File Structure

```
collections/tasks/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useProjectsTasks.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/projects-tasks/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [taskId].patch.ts    # UPDATE
│   │   └── [taskId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| description | text | No |
| status | string | No |
| dueDate | date | No |
| completed | boolean | No |
| priority | number | No |
| assigneeId | string (→ contacts) | No |
| subtasks | repeater | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/projects-tasks

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/projects-tasks`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/projects-tasks?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/projects-tasks

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/projects-tasks`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/projects-tasks/{taskId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/projects-tasks/${taskId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/projects-tasks/{taskId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/projects-tasks/${taskId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: tasks, pending } = await useCollectionQuery('projectsTasks')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('projectsTasks')
</script>

<template>
  <ProjectsTasksList />
</template>
```

### Schema & Validation

```typescript
import { projectsTaskSchema } from './app/composables/useProjectsTasks'

// Validate data
const result = projectsTaskSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only tasks`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useProjectsTasks.ts`:
```typescript
export const projectsTaskSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/projects-tasks/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
