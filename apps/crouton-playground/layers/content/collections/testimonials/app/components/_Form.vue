<!--
  @crouton-generated
  @collection testimonials
  @layer content
  @generated 2026-02-20

  ## AI Context
  - Form component for testimonials collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/content-testimonials
  - Zod schema: useContentTestimonials() composable
  - Fields: author, company, role, quote, rating, featured, avatar

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Author" name="author" class="not-last:pb-4">
          <UInput v-model="state.author" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Company" name="company" class="not-last:pb-4">
          <UInput v-model="state.company" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Role" name="role" class="not-last:pb-4">
          <UInput v-model="state.role" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Quote" name="quote" class="not-last:pb-4">
          <UTextarea v-model="state.quote" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Rating" name="rating" class="not-last:pb-4">
          <UInputNumber v-model="state.rating" class="w-full" />
        </UFormField>
        <UFormField label="Featured" name="featured" class="not-last:pb-4">
          <UCheckbox v-model="state.featured" />
        </UFormField>
        <UFormField label="Avatar" name="avatar" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.avatar"
            :crop="true"
          />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ContentTestimonialFormProps, ContentTestimonialFormData } from '../../types'
import useContentTestimonials from '../composables/useContentTestimonials'

const props = defineProps<ContentTestimonialFormProps>()
const { defaultValue, schema, collection } = useContentTestimonials()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<ContentTestimonialFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>