# ContentTestimonials Collection

> Generated by @fyit/crouton-cli on 2026-02-20

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentTestimonials` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-testimonials` |
| Form Component | `<ContentTestimonialsForm />` |
| List Component | `<ContentTestimonialsList />` |
| Composable | `useContentTestimonials()` |

## File Structure

```
collections/testimonials/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentTestimonials.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-testimonials/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [testimonialId].patch.ts    # UPDATE
│   │   └── [testimonialId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| author | string | Yes |
| company | string | No |
| role | string | No |
| quote | text | Yes |
| rating | number | No |
| featured | boolean | No |
| avatar | image | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-testimonials

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-testimonials`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-testimonials?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-testimonials

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-testimonials`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-testimonials/{testimonialId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-testimonials/${testimonialId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-testimonials/{testimonialId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-testimonials/${testimonialId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: testimonials, pending } = await useCollectionQuery('contentTestimonials')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentTestimonials')
</script>

<template>
  <ContentTestimonialsList />
</template>
```

### Schema & Validation

```typescript
import { contentTestimonialSchema } from './app/composables/useContentTestimonials'

// Validate data
const result = contentTestimonialSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only testimonials`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentTestimonials.ts`:
```typescript
export const contentTestimonialSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-testimonials/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
