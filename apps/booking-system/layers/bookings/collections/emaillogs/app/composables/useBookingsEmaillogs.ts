/**
 * @crouton-generated
 * @collection emaillogs
 * @layer bookings
 * @generated 2026-01-14
 *
 * ## AI Context
 * - Composable: useBookingsEmaillogs
 * - Collection name: bookingsEmaillogs
 * - API endpoint: /api/teams/[id]/bookings-emaillogs
 * - Form component: BookingsEmaillogsForm
 * - List component: BookingsEmaillogsList
 * - Fields: id, bookingId, templateId, recipientEmail, triggerType, status, sentAt, error
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @friendlyinternet/nuxt-crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const bookingsEmaillogSchema = z.object({
  bookingId: z.string().optional(),
  templateId: z.string().optional(),
  recipientEmail: z.string().min(1, 'recipientEmail is required'),
  triggerType: z.string().min(1, 'triggerType is required'),
  status: z.string().min(1, 'status is required'),
  sentAt: z.string().optional(),
  error: z.string().optional()
})

export const bookingsEmaillogsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'bookingId', header: 'BookingId' },
  { accessorKey: 'templateId', header: 'TemplateId' },
  { accessorKey: 'recipientEmail', header: 'RecipientEmail' },
  { accessorKey: 'triggerType', header: 'TriggerType' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'sentAt', header: 'SentAt' },
  { accessorKey: 'error', header: 'Error' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _bookingsEmaillogsConfig = {
  name: 'bookingsEmaillogs',
  layer: 'bookings',
  apiPath: 'bookings-emaillogs',
  componentName: 'BookingsEmaillogsForm',
  defaultValues: {
    bookingId: '',
    templateId: '',
    recipientEmail: '',
    triggerType: '',
    status: '',
    sentAt: '',
    error: ''
  },
  columns: bookingsEmaillogsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_bookingsEmaillogsConfig, 'schema', {
  value: bookingsEmaillogSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const bookingsEmaillogsConfig = _bookingsEmaillogsConfig as typeof _bookingsEmaillogsConfig & { schema: typeof bookingsEmaillogSchema }

export const useBookingsEmaillogs = () => bookingsEmaillogsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: bookingsEmaillogsConfig.defaultValues,
    schema: bookingsEmaillogSchema,
    columns: bookingsEmaillogsConfig.columns,
    collection: bookingsEmaillogsConfig.name
  }
}