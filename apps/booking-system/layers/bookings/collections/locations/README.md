# BookingsLocations Collection

> Generated by @friendlyinternet/nuxt-crouton-cli on 2026-01-14

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsLocations` |
| Layer | `bookings` |
| API Base | `/api/teams/[teamId]/bookings-locations` |
| Form Component | `<BookingsLocationsForm />` |
| List Component | `<BookingsLocationsList />` |
| Composable | `useBookingsLocations()` |

## Features

- Multi-language support for: title, street, zip, city, content
- Repeater fields: slots

## File Structure

```
collections/locations/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsLocations.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-locations/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [locationId].patch.ts    # UPDATE
│   │   └── [locationId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| color | string | No |
| street | string | No |
| zip | string | No |
| city | string | No |
| location | text | No |
| content | text | No |
| allowedMemberIds | array | No |
| slots | repeater | No |
| inventoryMode | boolean | No |
| quantity | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-locations

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-locations`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-locations?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-locations

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-locations`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-locations/{locationId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-locations/${locationId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-locations/{locationId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-locations/${locationId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: locations, pending } = await useCollectionQuery('bookingsLocations')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsLocations')
</script>

<template>
  <BookingsLocationsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsLocationSchema } from './app/composables/useBookingsLocations'

// Validate data
const result = bookingsLocationSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only locations`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsLocations.ts`:
```typescript
export const bookingsLocationSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-locations/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
