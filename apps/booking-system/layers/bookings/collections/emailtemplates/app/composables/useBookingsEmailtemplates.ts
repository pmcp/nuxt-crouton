/**
 * @crouton-generated
 * @collection emailtemplates
 * @layer bookings
 * @generated 2026-01-14
 *
 * ## AI Context
 * - Composable: useBookingsEmailtemplates
 * - Collection name: bookingsEmailtemplates
 * - API endpoint: /api/teams/[id]/bookings-emailtemplates
 * - Form component: BookingsEmailtemplatesForm
 * - List component: BookingsEmailtemplatesList
 * - Fields: id, name, subject, body, fromEmail, triggerType, recipientType, isActive, daysOffset, locationId
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @friendlyinternet/nuxt-crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const bookingsEmailtemplateSchema = z.object({
  fromEmail: z.string().min(1, 'fromEmail is required'),
  triggerType: z.string().min(1, 'triggerType is required'),
  recipientType: z.string().min(1, 'recipientType is required'),
  isActive: z.boolean().optional(),
  daysOffset: z.number().optional(),
  locationId: z.string().optional(),
  name: z.string().optional(),
  subject: z.string().optional(),
  body: z.string().optional(),
  translations: z.record(
    z.string(),
    z.object({
      name: z.string().min(1, 'Name is required'),
      subject: z.string().min(1, 'Subject is required'),
      body: z.string().min(1, 'Body is required')
    })
  ).refine(
    (translations) => translations.en && translations.en.name && translations.en.subject && translations.en.body,
    { message: 'English translations for name, subject, body are required' }
  )
})

export const bookingsEmailtemplatesColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'subject', header: 'Subject' },
  { accessorKey: 'body', header: 'Body' },
  { accessorKey: 'fromEmail', header: 'FromEmail' },
  { accessorKey: 'triggerType', header: 'TriggerType' },
  { accessorKey: 'recipientType', header: 'RecipientType' },
  { accessorKey: 'isActive', header: 'IsActive' },
  { accessorKey: 'daysOffset', header: 'DaysOffset' },
  { accessorKey: 'locationId', header: 'LocationId' },
  { accessorKey: 'translations', header: 'Translations' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _bookingsEmailtemplatesConfig = {
  name: 'bookingsEmailtemplates',
  layer: 'bookings',
  apiPath: 'bookings-emailtemplates',
  componentName: 'BookingsEmailtemplatesForm',
  defaultValues: {
    name: '',
    subject: '',
    body: '',
    fromEmail: '',
    triggerType: '',
    recipientType: '',
    isActive: false,
    daysOffset: 0,
    locationId: '',
    translations: {}
  },
  columns: bookingsEmailtemplatesColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_bookingsEmailtemplatesConfig, 'schema', {
  value: bookingsEmailtemplateSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const bookingsEmailtemplatesConfig = _bookingsEmailtemplatesConfig as typeof _bookingsEmailtemplatesConfig & { schema: typeof bookingsEmailtemplateSchema }

export const useBookingsEmailtemplates = () => bookingsEmailtemplatesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: bookingsEmailtemplatesConfig.defaultValues,
    schema: bookingsEmailtemplateSchema,
    columns: bookingsEmailtemplatesConfig.columns,
    collection: bookingsEmailtemplatesConfig.name
  }
}