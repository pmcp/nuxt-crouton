# BookingsAppServices Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAppServices` |
| Layer | `bookings-app` |
| API Base | `/api/teams/[teamId]/bookings-app-services` |
| Form Component | `<BookingsAppServicesForm />` |
| List Component | `<BookingsAppServicesList />` |
| Composable | `useBookingsAppServices()` |

## File Structure

```
collections/services/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAppServices.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-app-services/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [serviceId].patch.ts    # UPDATE
│   │   └── [serviceId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-app-services

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-app-services`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-app-services?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-app-services

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-app-services`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-app-services/{serviceId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-app-services/${serviceId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-app-services/{serviceId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-app-services/${serviceId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: services, pending } = await useCollectionQuery('bookingsAppServices')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAppServices')
</script>

<template>
  <BookingsAppServicesList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAppServiceSchema } from './app/composables/useBookingsAppServices'

// Validate data
const result = bookingsAppServiceSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only services`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAppServices.ts`:
```typescript
export const bookingsAppServiceSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-app-services/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
