# BookingsAppReviews Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAppReviews` |
| Layer | `bookings-app` |
| API Base | `/api/teams/[teamId]/bookings-app-reviews` |
| Form Component | `<BookingsAppReviewsForm />` |
| List Component | `<BookingsAppReviewsList />` |
| Composable | `useBookingsAppReviews()` |

## File Structure

```
collections/reviews/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAppReviews.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-app-reviews/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [reviewId].patch.ts    # UPDATE
│   │   └── [reviewId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-app-reviews

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-app-reviews`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-app-reviews?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-app-reviews

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-app-reviews`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-app-reviews/{reviewId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-app-reviews/${reviewId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-app-reviews/{reviewId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-app-reviews/${reviewId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: reviews, pending } = await useCollectionQuery('bookingsAppReviews')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAppReviews')
</script>

<template>
  <BookingsAppReviewsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAppReviewSchema } from './app/composables/useBookingsAppReviews'

// Validate data
const result = bookingsAppReviewSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only reviews`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAppReviews.ts`:
```typescript
export const bookingsAppReviewSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-app-reviews/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
