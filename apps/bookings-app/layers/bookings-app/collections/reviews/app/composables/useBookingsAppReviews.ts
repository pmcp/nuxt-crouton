/**
 * @crouton-generated
 * @collection reviews
 * @layer bookings-app
 * @generated 2026-02-18
 *
 * ## AI Context
 * - Composable: useBookingsAppReviews
 * - Collection name: bookingsAppReviews
 * - API endpoint: /api/teams/[id]/bookings-app-reviews
 * - Form component: BookingsAppReviewsForm
 * - List component: BookingsAppReviewsList
 * - Fields: display, fields
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const bookingsAppReviewSchema = z.object({
  display: z.string().optional(),
  fields: z.string().optional()
})

export const bookingsAppReviewsColumns = [
  { accessorKey: 'display', header: 'Display' },
  { accessorKey: 'fields', header: 'Fields' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _bookingsAppReviewsConfig = {
  name: 'bookingsAppReviews',
  layer: 'bookings-app',
  apiPath: 'bookings-app-reviews',
  componentName: 'BookingsAppReviewsForm',
  defaultValues: {
    display: '',
    fields: ''
  },
  columns: bookingsAppReviewsColumns,
  fields: [
      {
          "name": "display",
          "type": "string",
          "label": "Display",
          "area": "main"
      },
      {
          "name": "fields",
          "type": "string",
          "label": "Fields",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_bookingsAppReviewsConfig, 'schema', {
  value: bookingsAppReviewSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const bookingsAppReviewsConfig = _bookingsAppReviewsConfig as typeof _bookingsAppReviewsConfig & { schema: typeof bookingsAppReviewSchema }

export const useBookingsAppReviews = () => bookingsAppReviewsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: bookingsAppReviewsConfig.defaultValues,
    schema: bookingsAppReviewSchema,
    columns: bookingsAppReviewsConfig.columns,
    collection: bookingsAppReviewsConfig.name
  }
}