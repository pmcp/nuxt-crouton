<!--
  @crouton-generated
  @collection customers
  @layer bookings-app
  @generated 2026-02-18

  ## AI Context
  - List component for customers collection
  - Collection name: bookingsAppCustomers
  - API endpoint: /api/teams/[id]/bookings-app-customers
  - Columns composable: useBookingsAppCustomers()
  - Column fields: display, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="bookingsAppCustomers"
    :columns="columns"
    :rows="customers || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BookingsAppCustomers"
        :collection="'bookingsAppCustomers'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBookingsAppCustomers from '../composables/useBookingsAppCustomers'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBookingsAppCustomers()

const { items: customers, pending } = await useCollectionQuery(
  'bookingsAppCustomers'
)

</script>