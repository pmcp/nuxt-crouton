# BookingsAppCustomers Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAppCustomers` |
| Layer | `bookings-app` |
| API Base | `/api/teams/[teamId]/bookings-app-customers` |
| Form Component | `<BookingsAppCustomersForm />` |
| List Component | `<BookingsAppCustomersList />` |
| Composable | `useBookingsAppCustomers()` |

## File Structure

```
collections/customers/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAppCustomers.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-app-customers/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [customerId].patch.ts    # UPDATE
│   │   └── [customerId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-app-customers

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-app-customers`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-app-customers?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-app-customers

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-app-customers`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-app-customers/{customerId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-app-customers/${customerId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-app-customers/{customerId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-app-customers/${customerId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: customers, pending } = await useCollectionQuery('bookingsAppCustomers')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAppCustomers')
</script>

<template>
  <BookingsAppCustomersList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAppCustomerSchema } from './app/composables/useBookingsAppCustomers'

// Validate data
const result = bookingsAppCustomerSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only customers`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAppCustomers.ts`:
```typescript
export const bookingsAppCustomerSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-app-customers/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
