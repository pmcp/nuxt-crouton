# BookingsAppExt:bookings:bookings Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAppExt:bookings:bookings` |
| Layer | `bookings-app` |
| API Base | `/api/teams/[teamId]/bookings-app-__ext:bookings:bookings` |
| Form Component | `<BookingsAppExt:bookings:bookingsForm />` |
| List Component | `<BookingsAppExt:bookings:bookingsList />` |
| Composable | `useBookingsAppExt:bookings:bookings()` |

## File Structure

```
collections/__ext:bookings:bookings/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAppExt:bookings:bookings.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-app-__ext:bookings:bookings/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [__ext:bookings:bookingId].patch.ts    # UPDATE
│   │   └── [__ext:bookings:bookingId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| testcustomdata | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-app-__ext:bookings:bookings

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-app-__ext:bookings:bookings`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-app-__ext:bookings:bookings?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-app-__ext:bookings:bookings

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-app-__ext:bookings:bookings`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-app-__ext:bookings:bookings/{__ext:bookings:bookingId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-app-__ext:bookings:bookings/${__ext:bookings:bookingId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-app-__ext:bookings:bookings/{__ext:bookings:bookingId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-app-__ext:bookings:bookings/${__ext:bookings:bookingId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: ext:bookings:bookings, pending } = await useCollectionQuery('bookingsAppExt:bookings:bookings')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAppExt:bookings:bookings')
</script>

<template>
  <BookingsAppExt:bookings:bookingsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAppExt:bookings:bookingSchema } from './app/composables/useBookingsAppExt:bookings:bookings'

// Validate data
const result = bookingsAppExt:bookings:bookingSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only __ext:bookings:bookings`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAppExt:bookings:bookings.ts`:
```typescript
export const bookingsAppExt:bookings:bookingSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-app-__ext:bookings:bookings/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
