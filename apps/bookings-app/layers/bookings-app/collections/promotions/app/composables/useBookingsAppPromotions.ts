/**
 * @crouton-generated
 * @collection promotions
 * @layer bookings-app
 * @generated 2026-02-18
 *
 * ## AI Context
 * - Composable: useBookingsAppPromotions
 * - Collection name: bookingsAppPromotions
 * - API endpoint: /api/teams/[id]/bookings-app-promotions
 * - Form component: BookingsAppPromotionsForm
 * - List component: BookingsAppPromotionsList
 * - Fields: display, fields
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const bookingsAppPromotionSchema = z.object({
  display: z.string().optional(),
  fields: z.string().optional()
})

export const bookingsAppPromotionsColumns = [
  { accessorKey: 'display', header: 'Display' },
  { accessorKey: 'fields', header: 'Fields' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _bookingsAppPromotionsConfig = {
  name: 'bookingsAppPromotions',
  layer: 'bookings-app',
  apiPath: 'bookings-app-promotions',
  componentName: 'BookingsAppPromotionsForm',
  defaultValues: {
    display: '',
    fields: ''
  },
  columns: bookingsAppPromotionsColumns,
  fields: [
      {
          "name": "display",
          "type": "string",
          "label": "Display",
          "area": "main"
      },
      {
          "name": "fields",
          "type": "string",
          "label": "Fields",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_bookingsAppPromotionsConfig, 'schema', {
  value: bookingsAppPromotionSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const bookingsAppPromotionsConfig = _bookingsAppPromotionsConfig as typeof _bookingsAppPromotionsConfig & { schema: typeof bookingsAppPromotionSchema }

export const useBookingsAppPromotions = () => bookingsAppPromotionsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: bookingsAppPromotionsConfig.defaultValues,
    schema: bookingsAppPromotionSchema,
    columns: bookingsAppPromotionsConfig.columns,
    collection: bookingsAppPromotionsConfig.name
  }
}