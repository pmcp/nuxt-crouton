# BookingsAppPromotions Collection

> Generated by @fyit/crouton-cli on 2026-02-18

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `bookingsAppPromotions` |
| Layer | `bookings-app` |
| API Base | `/api/teams/[teamId]/bookings-app-promotions` |
| Form Component | `<BookingsAppPromotionsForm />` |
| List Component | `<BookingsAppPromotionsList />` |
| Composable | `useBookingsAppPromotions()` |

## File Structure

```
collections/promotions/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBookingsAppPromotions.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/bookings-app-promotions/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [promotionId].patch.ts    # UPDATE
│   │   └── [promotionId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| display | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/bookings-app-promotions

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/bookings-app-promotions`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/bookings-app-promotions?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/bookings-app-promotions

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/bookings-app-promotions`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/bookings-app-promotions/{promotionId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/bookings-app-promotions/${promotionId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/bookings-app-promotions/{promotionId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/bookings-app-promotions/${promotionId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: promotions, pending } = await useCollectionQuery('bookingsAppPromotions')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('bookingsAppPromotions')
</script>

<template>
  <BookingsAppPromotionsList />
</template>
```

### Schema & Validation

```typescript
import { bookingsAppPromotionSchema } from './app/composables/useBookingsAppPromotions'

// Validate data
const result = bookingsAppPromotionSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only promotions`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBookingsAppPromotions.ts`:
```typescript
export const bookingsAppPromotionSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/bookings-app-promotions/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
