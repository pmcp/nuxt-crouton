/**
 * @crouton-generated
 * @collection news
 * @layer content
 * @generated 2026-03-11
 *
 * ## AI Context
 * - Composable: useContentNews
 * - Collection name: contentNews
 * - API endpoint: /api/teams/[id]/content-news
 * - Form component: ContentNewsForm
 * - List component: ContentNewsList
 * - Fields: title, date, image, text, link, status
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const contentNewSchema = z.object({
  date: z.date({ required_error: 'date is required' }),
  image: z.string().optional(),
  link: z.string().optional(),
  status: z.string().min(1, 'status is required'),
  title: z.string().optional(),
  text: z.string().optional(),
  translations: z.record(
    z.string(),
    z.object({
      title: z.string().min(1, 'Title is required'),
      text: z.string().optional()
    })
  ).refine(
    (translations) => translations.en && translations.en.title,
    { message: 'English translations for title are required' }
  )
})

export const contentNewsColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'date', header: 'Date' },
  { accessorKey: 'image', header: 'Image' },
  { accessorKey: 'text', header: 'Text' },
  { accessorKey: 'link', header: 'Link' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'translations', header: 'Translations' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _contentNewsConfig = {
  name: 'contentNews',
  layer: 'content',
  apiPath: 'content-news',
  componentName: 'ContentNewsForm',
  defaultValues: {
    title: '',
    date: null,
    image: '',
    text: '',
    link: '',
    status: '',
    translations: {}
  },
  columns: contentNewsColumns,
  kind: 'content',
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "date",
          "type": "date",
          "label": "Date",
          "area": "sidebar"
      },
      {
          "name": "image",
          "type": "image",
          "label": "Image",
          "area": "sidebar"
      },
      {
          "name": "text",
          "type": "string",
          "label": "Summary",
          "area": "main"
      },
      {
          "name": "link",
          "type": "string",
          "label": "External Link",
          "area": "sidebar"
      },
      {
          "name": "status",
          "type": "string",
          "label": "Status",
          "area": "sidebar",
          "displayAs": "optionsSelect"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_contentNewsConfig, 'schema', {
  value: contentNewSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const contentNewsConfig = _contentNewsConfig as typeof _contentNewsConfig & { schema: typeof contentNewSchema }

export const useContentNews = () => contentNewsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: contentNewsConfig.defaultValues,
    schema: contentNewSchema,
    columns: contentNewsConfig.columns,
    collection: contentNewsConfig.name
  }
}