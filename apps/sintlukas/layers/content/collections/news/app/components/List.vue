<!--
  @crouton-generated
  @collection news
  @layer content
  @generated 2026-03-11

  ## AI Context
  - List component for news collection
  - Collection name: contentNews
  - API endpoint: /api/teams/[id]/content-news
  - Columns composable: useContentNews()
  - Column fields: title, date, image, text, link, status

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="contentNews"
    :columns="columns"
    :rows="news || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ContentNews"
        :collection="'contentNews'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #text-cell="{ row }">
      {{ t(row.original, 'text') }}
    </template>
    <template #date-cell="{ row }">
      <CroutonDate :date="row.original.date"></CroutonDate>
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title', 'text']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useContentNews from '../composables/useContentNews'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useContentNews()

const { items: news, pending } = await useCollectionQuery(
  'contentNews'
)

</script>