# ContentDownloads Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentDownloads` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-downloads` |
| Form Component | `<ContentDownloadsForm />` |
| List Component | `<ContentDownloadsList />` |
| Composable | `useContentDownloads()` |

## Features

- Multi-language support for: title

## File Structure

```
collections/downloads/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentDownloads.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-downloads/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [downloadId].patch.ts    # UPDATE
│   │   └── [downloadId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| internalName | string | No |
| file | file | Yes |
| order | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-downloads

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-downloads`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-downloads?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-downloads

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-downloads`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-downloads/{downloadId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-downloads/${downloadId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-downloads/{downloadId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-downloads/${downloadId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: downloads, pending } = await useCollectionQuery('contentDownloads')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentDownloads')
</script>

<template>
  <ContentDownloadsList />
</template>
```

### Schema & Validation

```typescript
import { contentDownloadSchema } from './app/composables/useContentDownloads'

// Validate data
const result = contentDownloadSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only downloads`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentDownloads.ts`:
```typescript
export const contentDownloadSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-downloads/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
