<!--
  @crouton-generated
  @collection ateliers
  @layer content
  @generated 2026-03-11

  ## AI Context
  - List component for ateliers collection
  - Collection name: contentAteliers
  - API endpoint: /api/teams/[id]/content-ateliers
  - Columns composable: useContentAteliers()
  - Column fields: title, category, age, mainImage, cardImage, content, sidebarContent, persons, images, order, status

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="contentAteliers"
    :columns="columns"
    :rows="ateliers || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ContentAteliers"
        :collection="'contentAteliers'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #age-cell="{ row }">
      {{ t(row.original, 'age') }}
    </template>
    <template #category-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.category"
        :id="row.original.category"
        collection="contentCategories"
      />
    </template>
    <template #persons-cell="{ row }">
      <div v-if="row.original.persons && row.original.persons.length > 0" class="flex flex-wrap gap-1">
        <CroutonItemCardMini
          v-for="itemId in row.original.persons"
          :key="itemId"
          :id="itemId"
          collection="contentPersons"
        />
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #images-cell="{ row }">
      <div v-if="row.original.images && row.original.images.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.images"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
    <template #content-cell="{ row }">
      <CroutonEditorPreview :content="t(row.original, 'content')" mode="thumbnail" />
    </template>
    <template #sidebarContent-cell="{ row }">
      <CroutonEditorPreview :content="t(row.original, 'sidebarContent')" mode="thumbnail" />
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title', 'age', 'content', 'sidebarContent']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useContentAteliers from '../composables/useContentAteliers'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useContentAteliers()

const { items: ateliers, pending } = await useCollectionQuery(
  'contentAteliers'
)

</script>