# ContentAteliers Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentAteliers` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-ateliers` |
| Form Component | `<ContentAteliersForm />` |
| List Component | `<ContentAteliersList />` |
| Composable | `useContentAteliers()` |

## Features

- Multi-language support for: title, age, content, sidebarContent
- References: category → categories, persons → persons

## File Structure

```
collections/ateliers/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentAteliers.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-ateliers/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [atelierId].patch.ts    # UPDATE
│   │   └── [atelierId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| category | string (→ categories) | Yes |
| age | string | No |
| mainImage | image | No |
| cardImage | image | No |
| content | text | No |
| sidebarContent | text | No |
| persons | array (→ persons) | No |
| images | array | No |
| order | number | No |
| status | string | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-ateliers

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-ateliers`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-ateliers?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-ateliers

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-ateliers`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-ateliers/{atelierId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-ateliers/${atelierId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-ateliers/{atelierId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-ateliers/${atelierId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: ateliers, pending } = await useCollectionQuery('contentAteliers')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentAteliers')
</script>

<template>
  <ContentAteliersList />
</template>
```

### Schema & Validation

```typescript
import { contentAtelierSchema } from './app/composables/useContentAteliers'

// Validate data
const result = contentAtelierSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only ateliers`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentAteliers.ts`:
```typescript
export const contentAtelierSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-ateliers/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
