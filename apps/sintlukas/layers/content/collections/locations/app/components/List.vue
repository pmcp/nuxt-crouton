<!--
  @crouton-generated
  @collection locations
  @layer content
  @generated 2026-03-11

  ## AI Context
  - List component for locations collection
  - Collection name: contentLocations
  - API endpoint: /api/teams/[id]/content-locations
  - Columns composable: useContentLocations()
  - Column fields: title, street, zip, city, location, info, isMain, order

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="contentLocations"
    :columns="columns"
    :rows="locations || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ContentLocations"
        :collection="'contentLocations'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #isMain-cell="{ row }">
      <CroutonBoolean :value="row.original.isMain" />
    </template>
    <template #info-cell="{ row }">
      <CroutonEditorPreview :content="t(row.original, 'info')" mode="thumbnail" />
    </template>
    <template #location-cell="{ row }">
      <CroutonMapsPreview :location="row.original.location" />
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title', 'info']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useContentLocations from '../composables/useContentLocations'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useContentLocations()

const { items: locations, pending } = await useCollectionQuery(
  'contentLocations'
)

</script>