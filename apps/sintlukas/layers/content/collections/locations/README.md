# ContentLocations Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentLocations` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-locations` |
| Form Component | `<ContentLocationsForm />` |
| List Component | `<ContentLocationsList />` |
| Composable | `useContentLocations()` |

## Features

- Multi-language support for: title, info

## File Structure

```
collections/locations/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentLocations.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-locations/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [locationId].patch.ts    # UPDATE
│   │   └── [locationId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| street | string | Yes |
| zip | string | Yes |
| city | string | Yes |
| location | string | No |
| info | text | No |
| isMain | boolean | No |
| order | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-locations

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-locations`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-locations?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-locations

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-locations`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-locations/{locationId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-locations/${locationId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-locations/{locationId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-locations/${locationId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: locations, pending } = await useCollectionQuery('contentLocations')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentLocations')
</script>

<template>
  <ContentLocationsList />
</template>
```

### Schema & Validation

```typescript
import { contentLocationSchema } from './app/composables/useContentLocations'

// Validate data
const result = contentLocationSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only locations`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentLocations.ts`:
```typescript
export const contentLocationSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-locations/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
