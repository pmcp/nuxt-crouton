# DesignerFields Collection

> Generated by @fyit/crouton-cli on 2026-02-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `designerFields` |
| Layer | `designer` |
| API Base | `/api/teams/[teamId]/designer-fields` |
| Form Component | `<DesignerFieldsForm />` |
| List Component | `<DesignerFieldsList />` |
| Composable | `useDesignerFields()` |

## Features

- References: collectionId → collections

## File Structure

```
collections/fields/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useDesignerFields.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/designer-fields/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [fieldId].patch.ts    # UPDATE
│   │   └── [fieldId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| collectionId | string (→ collections) | Yes |
| name | string | Yes |
| type | string | Yes |
| meta | json | No |
| refTarget | string | No |
| sortOrder | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/designer-fields

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/designer-fields`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/designer-fields?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/designer-fields

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/designer-fields`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/designer-fields/{fieldId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/designer-fields/${fieldId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/designer-fields/{fieldId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/designer-fields/${fieldId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: fields, pending } = await useCollectionQuery('designerFields')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('designerFields')
</script>

<template>
  <DesignerFieldsList />
</template>
```

### Schema & Validation

```typescript
import { designerFieldSchema } from './app/composables/useDesignerFields'

// Validate data
const result = designerFieldSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only fields`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useDesignerFields.ts`:
```typescript
export const designerFieldSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/designer-fields/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
