# DesignerCollections Collection

> Generated by @fyit/crouton-cli on 2026-02-16

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `designerCollections` |
| Layer | `designer` |
| API Base | `/api/teams/[teamId]/designer-collections` |
| Form Component | `<DesignerCollectionsForm />` |
| List Component | `<DesignerCollectionsList />` |
| Composable | `useDesignerCollections()` |

## Features

- References: projectId → projects

## File Structure

```
collections/collections/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useDesignerCollections.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/designer-collections/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [collectionId].patch.ts    # UPDATE
│   │   └── [collectionId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| projectId | string (→ projects) | Yes |
| name | string | Yes |
| description | text | No |
| sortOrder | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/designer-collections

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/designer-collections`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/designer-collections?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/designer-collections

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/designer-collections`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/designer-collections/{collectionId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/designer-collections/${collectionId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/designer-collections/{collectionId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/designer-collections/${collectionId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: collections, pending } = await useCollectionQuery('designerCollections')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('designerCollections')
</script>

<template>
  <DesignerCollectionsList />
</template>
```

### Schema & Validation

```typescript
import { designerCollectionSchema } from './app/composables/useDesignerCollections'

// Validate data
const result = designerCollectionSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only collections`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useDesignerCollections.ts`:
```typescript
export const designerCollectionSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/designer-collections/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
