/**
 * @crouton-generated
 * @collection projects
 * @layer designer
 * @generated 2026-02-16
 *
 * ## AI Context
 * - Composable: useDesignerProjects
 * - Collection name: designerProjects
 * - API endpoint: /api/teams/[id]/designer-projects
 * - Form component: DesignerProjectsForm
 * - List component: DesignerProjectsList
 * - Fields: id, name, currentPhase, config, messages
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const designerProjectSchema = z.object({
  name: z.string().min(1, 'name is required'),
  currentPhase: z.string().min(1, 'currentPhase is required'),
  config: z.record(z.string(), z.any()).optional(),
  messages: z.record(z.string(), z.any()).optional()
})

export const designerProjectsColumns = [
  { accessorKey: 'id', header: 'Id' },
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'currentPhase', header: 'CurrentPhase' },
  { accessorKey: 'config', header: 'Config' },
  { accessorKey: 'messages', header: 'Messages' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _designerProjectsConfig = {
  name: 'designerProjects',
  layer: 'designer',
  apiPath: 'designer-projects',
  componentName: 'DesignerProjectsForm',
  defaultValues: {
    name: '',
    currentPhase: '',
    config: {},
    messages: {}
  },
  columns: designerProjectsColumns,
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_designerProjectsConfig, 'schema', {
  value: designerProjectSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const designerProjectsConfig = _designerProjectsConfig as typeof _designerProjectsConfig & { schema: typeof designerProjectSchema }

export const useDesignerProjects = () => designerProjectsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: designerProjectsConfig.defaultValues,
    schema: designerProjectSchema,
    columns: designerProjectsConfig.columns,
    collection: designerProjectsConfig.name
  }
}