<!--
  @crouton-generated
  @collection downloads
  @layer content
  @generated 2026-03-11

  ## AI Context
  - List component for downloads collection
  - Collection name: contentDownloads
  - API endpoint: /api/teams/[id]/content-downloads
  - Columns composable: useContentDownloads()
  - Column fields: title, internalName, file, order

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="contentDownloads"
    :columns="columns"
    :rows="downloads || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ContentDownloads"
        :collection="'contentDownloads'"
        createButton
      />
    </template>
    <template #title-cell="{ row }">
      {{ t(row.original, 'title') }}
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['title']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useContentDownloads from '../composables/useContentDownloads'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useContentDownloads()

const { items: downloads, pending } = await useCollectionQuery(
  'contentDownloads'
)

</script>