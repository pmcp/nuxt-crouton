<!--
  @crouton-generated
  @collection downloads
  @layer content
  @generated 2026-03-11

  ## AI Context
  - Form component for downloads collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/content-downloads
  - Zod schema: useContentDownloads() composable
  - Fields: title, internalName, file, order

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="File" name="file" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.file"
          />
        </UFormField>
      </div>

      <div>


      <CroutonI18nInput
        v-model="state.translations"
        :fields="['title']"
        :default-values="{
          title: state.title || ''
        }"
        show-ai-translate
        field-type="downloads"
        label="Translations"
      />
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="InternalName" name="internalName" class="not-last:pb-4">
          <UInput v-model="state.internalName" class="w-full" size="xl" />
        </UFormField>
        <UFormField label="Order" name="order" class="not-last:pb-4">
          <UInputNumber v-model="state.order" class="w-full" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ContentDownloadFormProps, ContentDownloadFormData } from '../../types'
import useContentDownloads from '../composables/useContentDownloads'

const props = defineProps<ContentDownloadFormProps>()
const { defaultValue, schema, collection } = useContentDownloads()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<ContentDownloadFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>