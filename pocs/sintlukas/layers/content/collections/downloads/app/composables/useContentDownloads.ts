/**
 * @crouton-generated
 * @collection downloads
 * @layer content
 * @generated 2026-03-11
 *
 * ## AI Context
 * - Composable: useContentDownloads
 * - Collection name: contentDownloads
 * - API endpoint: /api/teams/[id]/content-downloads
 * - Form component: ContentDownloadsForm
 * - List component: ContentDownloadsList
 * - Fields: title, internalName, file, order
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const contentDownloadSchema = z.object({
  internalName: z.string().optional(),
  file: z.string(),
  order: z.number().optional(),
  title: z.string().optional(),
  translations: z.record(
    z.string(),
    z.object({
      title: z.string().min(1, 'Title is required')
    })
  ).refine(
    (translations) => translations.en && translations.en.title,
    { message: 'English translations for title are required' }
  )
})

export const contentDownloadsColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'internalName', header: 'InternalName' },
  { accessorKey: 'file', header: 'File' },
  { accessorKey: 'order', header: 'Order' },
  { accessorKey: 'translations', header: 'Translations' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _contentDownloadsConfig = {
  name: 'contentDownloads',
  layer: 'content',
  apiPath: 'content-downloads',
  componentName: 'ContentDownloadsForm',
  defaultValues: {
    title: '',
    internalName: '',
    file: '',
    order: 0,
    translations: {}
  },
  columns: contentDownloadsColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Label",
          "area": "main"
      },
      {
          "name": "internalName",
          "type": "string",
          "label": "Internal Name",
          "area": "sidebar"
      },
      {
          "name": "file",
          "type": "file",
          "label": "File",
          "area": "main"
      },
      {
          "name": "order",
          "type": "number",
          "label": "Sort Order",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_contentDownloadsConfig, 'schema', {
  value: contentDownloadSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const contentDownloadsConfig = _contentDownloadsConfig as typeof _contentDownloadsConfig & { schema: typeof contentDownloadSchema }

export const useContentDownloads = () => contentDownloadsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: contentDownloadsConfig.defaultValues,
    schema: contentDownloadSchema,
    columns: contentDownloadsConfig.columns,
    collection: contentDownloadsConfig.name
  }
}