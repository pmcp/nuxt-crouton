/**
 * @crouton-generated
 * @collection persons
 * @layer content
 * @generated 2026-03-11
 *
 * ## AI Context
 * - Composable: useContentPersons
 * - Collection name: contentPersons
 * - API endpoint: /api/teams/[id]/content-persons
 * - Form component: ContentPersonsForm
 * - List component: ContentPersonsList
 * - Fields: firstName, lastName, role, image, order
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const contentPersonSchema = z.object({
  firstName: z.string().min(1, 'firstName is required'),
  lastName: z.string().min(1, 'lastName is required'),
  role: z.string().optional(),
  image: z.string().optional(),
  order: z.number().optional()
})

export const contentPersonsColumns = [
  { accessorKey: 'firstName', header: 'FirstName' },
  { accessorKey: 'lastName', header: 'LastName' },
  { accessorKey: 'role', header: 'Role' },
  { accessorKey: 'image', header: 'Image' },
  { accessorKey: 'order', header: 'Order' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _contentPersonsConfig = {
  name: 'contentPersons',
  layer: 'content',
  apiPath: 'content-persons',
  componentName: 'ContentPersonsForm',
  defaultValues: {
    firstName: '',
    lastName: '',
    role: '',
    image: '',
    order: 0
  },
  columns: contentPersonsColumns,
  fields: [
      {
          "name": "firstName",
          "type": "string",
          "label": "First Name",
          "area": "main"
      },
      {
          "name": "lastName",
          "type": "string",
          "label": "Last Name",
          "area": "main"
      },
      {
          "name": "role",
          "type": "string",
          "label": "Role",
          "area": "sidebar"
      },
      {
          "name": "image",
          "type": "image",
          "label": "Portrait",
          "area": "sidebar"
      },
      {
          "name": "order",
          "type": "number",
          "label": "Sort Order",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_contentPersonsConfig, 'schema', {
  value: contentPersonSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const contentPersonsConfig = _contentPersonsConfig as typeof _contentPersonsConfig & { schema: typeof contentPersonSchema }

export const useContentPersons = () => contentPersonsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: contentPersonsConfig.defaultValues,
    schema: contentPersonSchema,
    columns: contentPersonsConfig.columns,
    collection: contentPersonsConfig.name
  }
}