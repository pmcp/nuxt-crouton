# ContentPersons Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentPersons` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-persons` |
| Form Component | `<ContentPersonsForm />` |
| List Component | `<ContentPersonsList />` |
| Composable | `useContentPersons()` |

## File Structure

```
collections/persons/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentPersons.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-persons/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [personId].patch.ts    # UPDATE
│   │   └── [personId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| firstName | string | Yes |
| lastName | string | Yes |
| role | string | No |
| image | image | No |
| order | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-persons

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-persons`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-persons?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-persons

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-persons`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-persons/{personId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-persons/${personId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-persons/{personId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-persons/${personId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: persons, pending } = await useCollectionQuery('contentPersons')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentPersons')
</script>

<template>
  <ContentPersonsList />
</template>
```

### Schema & Validation

```typescript
import { contentPersonSchema } from './app/composables/useContentPersons'

// Validate data
const result = contentPersonSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only persons`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentPersons.ts`:
```typescript
export const contentPersonSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-persons/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
