# ContentNews Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentNews` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-news` |
| Form Component | `<ContentNewsForm />` |
| List Component | `<ContentNewsList />` |
| Composable | `useContentNews()` |

## Features

- Multi-language support for: title, text

## File Structure

```
collections/news/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentNews.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-news/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [newId].patch.ts    # UPDATE
│   │   └── [newId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| date | date | Yes |
| image | image | No |
| text | string | No |
| link | string | No |
| status | string | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-news

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-news`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-news?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-news

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-news`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-news/{newId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-news/${newId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-news/{newId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-news/${newId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: news, pending } = await useCollectionQuery('contentNews')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentNews')
</script>

<template>
  <ContentNewsList />
</template>
```

### Schema & Validation

```typescript
import { contentNewSchema } from './app/composables/useContentNews'

// Validate data
const result = contentNewSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only news`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentNews.ts`:
```typescript
export const contentNewSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-news/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
