<!--
  @crouton-generated
  @collection ateliers
  @layer content
  @generated 2026-03-11

  ## AI Context
  - Form component for ateliers collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/content-ateliers
  - Zod schema: useContentAteliers() composable
  - Fields: title, category, age, mainImage, cardImage, content, sidebarContent, persons, images, order, status

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>


      <div>


      <CroutonI18nInput
        v-model="state.translations"
        :fields="['title', 'age', 'content', 'sidebarContent']"
        :default-values="{
          title: state.title || '',
          age: state.age || '',
          content: state.content || '',
          sidebarContent: state.sidebarContent || ''
        }"
        :field-components="{
          content: 'CroutonPagesEditorBlockEditor',
          sidebarContent: 'CroutonPagesEditorBlockEditor'
        }"
        show-ai-translate
        field-type="ateliers"
        label="Translations"
      />
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Category" name="category" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.category"
            collection="contentCategories"
            label="Category"
          />
        </UFormField>
        <UFormField label="MainImage" name="mainImage" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.mainImage"
            :crop="true"
          />
        </UFormField>
        <UFormField label="CardImage" name="cardImage" class="not-last:pb-4">
          <CroutonAssetsPicker
            v-model="state.cardImage"
            :crop="true"
          />
        </UFormField>
        <UFormField label="Persons" name="persons" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.persons"
            collection="contentPersons"
            label="Persons"
            multiple
          />
        </UFormField>
        <UFormField label="Images" name="images" class="not-last:pb-4">
          <UTextarea
            :model-value="Array.isArray(state.images) ? state.images.join('\n') : ''"
            @update:model-value="(val) => state.images = val ? val.split('\n').filter(Boolean) : []"
            class="w-full"
            :rows="6"
            placeholder="Enter one value per line"
          />
          <p class="text-sm text-gray-500 mt-1">Enter one value per line</p>
        </UFormField>
        <UFormField label="Order" name="order" class="not-last:pb-4">
          <UInputNumber v-model="state.order" class="w-full" />
        </UFormField>
        <UFormField label="Status" name="status" class="not-last:pb-4">
          <USelect
            v-model="state.status"
            :items="statusOptions"
            class="w-full"
            size="xl"
          />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { ContentAtelierFormProps, ContentAtelierFormData } from '../../types'
import useContentAteliers from '../composables/useContentAteliers'

const props = defineProps<ContentAtelierFormProps>()
const { defaultValue, schema, collection } = useContentAteliers()

// Form layout configuration
const tabs = ref(false)

// Static select options
const statusOptions = [
  { label: 'Draft', value: 'draft' },
  { label: 'Published', value: 'published' },
  { label: 'Archived', value: 'archived' }
]



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

const state = ref<ContentAtelierFormData & { id?: string | null }>(initialValues)

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>