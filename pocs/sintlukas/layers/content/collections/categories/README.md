# ContentCategories Collection

> Generated by @fyit/crouton-cli on 2026-03-11

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentCategories` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-categories` |
| Form Component | `<ContentCategoriesForm />` |
| List Component | `<ContentCategoriesList />` |
| Composable | `useContentCategories()` |

## Features

- Multi-language support for: title

## File Structure

```
collections/categories/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentCategories.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-categories/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [categorieId].patch.ts    # UPDATE
│   │   └── [categorieId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| order | number | Yes |
| color | string | Yes |
| thumbnail | image | No |
| background | image | No |
| isSidebar | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-categories

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-categories`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-categories?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-categories

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-categories`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-categories/{categorieId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-categories/${categorieId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-categories/{categorieId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-categories/${categorieId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: categories, pending } = await useCollectionQuery('contentCategories')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentCategories')
</script>

<template>
  <ContentCategoriesList />
</template>
```

### Schema & Validation

```typescript
import { contentCategorieSchema } from './app/composables/useContentCategories'

// Validate data
const result = contentCategorieSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only categories`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentCategories.ts`:
```typescript
export const contentCategorieSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-categories/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
