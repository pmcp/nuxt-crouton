# KvrSettings Collection

> Generated by @fyit/crouton-cli on 2026-04-23

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `kvrSettings` |
| Layer | `kvr` |
| API Base | `/api/teams/[teamId]/kvr-settings` |
| Form Component | `<KvrSettingsForm />` |
| List Component | `<KvrSettingsList />` |
| Composable | `useKvrSettings()` |

## Features

- Repeater fields: recipients

## File Structure

```
collections/settings/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useKvrSettings.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/kvr-settings/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [settingId].patch.ts    # UPDATE
│   │   └── [settingId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| recipients | repeater | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/kvr-settings

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/kvr-settings`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/kvr-settings?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/kvr-settings

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/kvr-settings`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/kvr-settings/{settingId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/kvr-settings/${settingId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/kvr-settings/{settingId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/kvr-settings/${settingId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: settings, pending } = await useCollectionQuery('kvrSettings')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('kvrSettings')
</script>

<template>
  <KvrSettingsList />
</template>
```

### Schema & Validation

```typescript
import { kvrSettingSchema } from './app/composables/useKvrSettings'

// Validate data
const result = kvrSettingSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only settings`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useKvrSettings.ts`:
```typescript
export const kvrSettingSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/kvr-settings/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
