<!--
  @crouton-generated
  @collection settings
  @layer kvr
  @generated 2026-04-23

  ## AI Context
  - List component for settings collection
  - Collection name: kvrSettings
  - API endpoint: /api/teams/[id]/kvr-settings
  - Columns composable: useKvrSettings()
  - Column fields: recipients

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="kvrSettings"
    :columns="columns"
    :rows="settings || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="KvrSettings"
        :collection="'kvrSettings'"
        createButton
      />
    </template>
    <template #recipients-cell="{ row }">
      <KvrSettingsRecipientCardMini :value="row.original.recipients" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useKvrSettings from '../composables/useKvrSettings'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useKvrSettings()

const { items: settings, pending } = await useCollectionQuery(
  'kvrSettings'
)

</script>