# LibraryCatalogGenres Collection

> Generated by @fyit/crouton-cli on 2026-06-21

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `libraryCatalogGenres` |
| Layer | `library-catalog` |
| API Base | `/api/teams/[teamId]/library-catalog-genres` |
| Form Component | `<LibraryCatalogGenresForm />` |
| List Component | `<LibraryCatalogGenresList />` |
| Composable | `useLibraryCatalogGenres()` |

## File Structure

```
collections/genres/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useLibraryCatalogGenres.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/library-catalog-genres/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [genreId].patch.ts    # UPDATE
│   │   └── [genreId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| description | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/library-catalog-genres

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/library-catalog-genres`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/library-catalog-genres?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/library-catalog-genres

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/library-catalog-genres`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/library-catalog-genres/{genreId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/library-catalog-genres/${genreId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/library-catalog-genres/{genreId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/library-catalog-genres/${genreId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: genres, pending } = await useCollectionQuery('libraryCatalogGenres')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('libraryCatalogGenres')
</script>

<template>
  <LibraryCatalogGenresList />
</template>
```

### Schema & Validation

```typescript
import { libraryCatalogGenreSchema } from './app/composables/useLibraryCatalogGenres'

// Validate data
const result = libraryCatalogGenreSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only genres`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useLibraryCatalogGenres.ts`:
```typescript
export const libraryCatalogGenreSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/library-catalog-genres/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
