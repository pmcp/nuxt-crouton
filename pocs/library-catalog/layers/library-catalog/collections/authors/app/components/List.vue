<!--
  @crouton-generated
  @collection authors
  @layer library-catalog
  @generated 2026-06-19

  ## AI Context
  - List component for authors collection
  - Collection name: libraryCatalogAuthors
  - API endpoint: /api/teams/[id]/library-catalog-authors
  - Columns composable: useLibraryCatalogAuthors()
  - Column fields: collection, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="libraryCatalogAuthors"
    :columns="columns"
    :rows="authors || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="LibraryCatalogAuthors"
        :collection="'libraryCatalogAuthors'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useLibraryCatalogAuthors from '../composables/useLibraryCatalogAuthors'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useLibraryCatalogAuthors()

const { items: authors, pending } = await useCollectionQuery(
  'libraryCatalogAuthors'
)

</script>