# LibraryCatalogAuthors Collection

> Generated by @fyit/crouton-cli on 2026-06-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `libraryCatalogAuthors` |
| Layer | `library-catalog` |
| API Base | `/api/teams/[teamId]/library-catalog-authors` |
| Form Component | `<LibraryCatalogAuthorsForm />` |
| List Component | `<LibraryCatalogAuthorsList />` |
| Composable | `useLibraryCatalogAuthors()` |

## File Structure

```
collections/authors/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useLibraryCatalogAuthors.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/library-catalog-authors/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [authorId].patch.ts    # UPDATE
│   │   └── [authorId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| bio | text | No |
| bornYear | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/library-catalog-authors

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/library-catalog-authors`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/library-catalog-authors?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/library-catalog-authors

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/library-catalog-authors`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/library-catalog-authors/{authorId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/library-catalog-authors/${authorId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/library-catalog-authors/{authorId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/library-catalog-authors/${authorId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: authors, pending } = await useCollectionQuery('libraryCatalogAuthors')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('libraryCatalogAuthors')
</script>

<template>
  <LibraryCatalogAuthorsList />
</template>
```

### Schema & Validation

```typescript
import { libraryCatalogAuthorSchema } from './app/composables/useLibraryCatalogAuthors'

// Validate data
const result = libraryCatalogAuthorSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only authors`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useLibraryCatalogAuthors.ts`:
```typescript
export const libraryCatalogAuthorSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/library-catalog-authors/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
