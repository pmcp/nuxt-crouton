# LibraryCatalogBooks Collection

> Generated by @fyit/crouton-cli on 2026-06-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `libraryCatalogBooks` |
| Layer | `library-catalog` |
| API Base | `/api/teams/[teamId]/library-catalog-books` |
| Form Component | `<LibraryCatalogBooksForm />` |
| List Component | `<LibraryCatalogBooksList />` |
| Composable | `useLibraryCatalogBooks()` |

## Features

- References: authorId → authors, genreId → genres

## File Structure

```
collections/books/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useLibraryCatalogBooks.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/library-catalog-books/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [bookId].patch.ts    # UPDATE
│   │   └── [bookId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| isbn | string | No |
| publishedYear | number | No |
| coverImage | image | No |
| description | text | No |
| authorId | string (→ authors) | No |
| genreId | string (→ genres) | No |
| copiesTotal | number | No |
| copiesAvailable | number | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/library-catalog-books

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/library-catalog-books`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/library-catalog-books?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/library-catalog-books

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/library-catalog-books`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/library-catalog-books/{bookId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/library-catalog-books/${bookId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/library-catalog-books/{bookId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/library-catalog-books/${bookId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: books, pending } = await useCollectionQuery('libraryCatalogBooks')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('libraryCatalogBooks')
</script>

<template>
  <LibraryCatalogBooksList />
</template>
```

### Schema & Validation

```typescript
import { libraryCatalogBookSchema } from './app/composables/useLibraryCatalogBooks'

// Validate data
const result = libraryCatalogBookSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only books`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useLibraryCatalogBooks.ts`:
```typescript
export const libraryCatalogBookSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/library-catalog-books/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
