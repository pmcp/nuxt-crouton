<!--
  @crouton-generated
  @collection books
  @layer library-catalog
  @generated 2026-06-19

  ## AI Context
  - List component for books collection
  - Collection name: libraryCatalogBooks
  - API endpoint: /api/teams/[id]/library-catalog-books
  - Columns composable: useLibraryCatalogBooks()
  - Column fields: collection, fields

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="libraryCatalogBooks"
    :columns="columns"
    :rows="books || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="LibraryCatalogBooks"
        :collection="'libraryCatalogBooks'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useLibraryCatalogBooks from '../composables/useLibraryCatalogBooks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useLibraryCatalogBooks()

const { items: books, pending } = await useCollectionQuery(
  'libraryCatalogBooks'
)

</script>