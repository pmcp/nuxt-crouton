# LibraryCatalogLoans Collection

> Generated by @fyit/crouton-cli on 2026-06-19

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `libraryCatalogLoans` |
| Layer | `library-catalog` |
| API Base | `/api/teams/[teamId]/library-catalog-loans` |
| Form Component | `<LibraryCatalogLoansForm />` |
| List Component | `<LibraryCatalogLoansList />` |
| Composable | `useLibraryCatalogLoans()` |

## File Structure

```
collections/loans/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useLibraryCatalogLoans.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/library-catalog-loans/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [loanId].patch.ts    # UPDATE
│   │   └── [loanId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| collection | string | No |
| fields | string | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/library-catalog-loans

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/library-catalog-loans`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/library-catalog-loans?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/library-catalog-loans

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/library-catalog-loans`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/library-catalog-loans/{loanId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/library-catalog-loans/${loanId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/library-catalog-loans/{loanId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/library-catalog-loans/${loanId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: loans, pending } = await useCollectionQuery('libraryCatalogLoans')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('libraryCatalogLoans')
</script>

<template>
  <LibraryCatalogLoansList />
</template>
```

### Schema & Validation

```typescript
import { libraryCatalogLoanSchema } from './app/composables/useLibraryCatalogLoans'

// Validate data
const result = libraryCatalogLoanSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only loans`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useLibraryCatalogLoans.ts`:
```typescript
export const libraryCatalogLoanSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/library-catalog-loans/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
