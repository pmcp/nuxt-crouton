<!--
  @crouton-generated
  @collection loans
  @layer library-catalog
  @generated 2026-06-21

  ## AI Context
  - Form component for loans collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/library-catalog-loans
  - Zod schema: useLibraryCatalogLoans() composable
  - Fields: bookId, borrowerName, borrowedAt, dueAt, returnedAt

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField :label="t('library-catalog.loans.fields.bookId', 'Book')" name="bookId" class="not-last:pb-4">
          <CroutonFormReferenceSelect
            v-model="state.bookId"
            collection="libraryCatalogBooks"
            :label="t('library-catalog.loans.fields.bookId', 'Book')"
          />
        </UFormField>
        <UFormField :label="t('library-catalog.loans.fields.borrowerName', 'Borrower Name')" name="borrowerName" class="not-last:pb-4">
          <UInput v-model="state.borrowerName" class="w-full" size="xl" />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
      </div>
      <div class="flex flex-col gap-4 p-1">
        <UFormField :label="t('library-catalog.loans.fields.borrowedAt', 'Borrowed At')" name="borrowedAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.borrowedAt" />
        </UFormField>
        <UFormField :label="t('library-catalog.loans.fields.dueAt', 'Due At')" name="dueAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.dueAt" />
        </UFormField>
        <UFormField :label="t('library-catalog.loans.fields.returnedAt', 'Returned At')" name="returnedAt" class="not-last:pb-4">
          <CroutonCalendar v-model:date="state.returnedAt" />
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { LibraryCatalogLoanFormProps, LibraryCatalogLoanFormData } from '../../types'
import useLibraryCatalogLoans from '../composables/useLibraryCatalogLoans'

const props = defineProps<LibraryCatalogLoanFormProps>()
const { defaultValue, schema, collection } = useLibraryCatalogLoans()

// Field labels resolve through translations (team override → system → fallback)
const { t } = useT()

// Form layout configuration
const tabs = ref(false)



// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Convert date strings to Date objects for date fields during editing
if (props.action === 'update' && props.activeItem?.id) {
  if (initialValues.borrowedAt) {
    initialValues.borrowedAt = new Date(initialValues.borrowedAt)
  }
  if (initialValues.dueAt) {
    initialValues.dueAt = new Date(initialValues.dueAt)
  }
  if (initialValues.returnedAt) {
    initialValues.returnedAt = new Date(initialValues.returnedAt)
  }
}

// Draft state: seeded from defaults (required fields may start null/empty until
// the user fills them; the zod schema validates on submit), so cast the initial
// values to the validated shape.
const state = ref<LibraryCatalogLoanFormData & { id?: string | null }>(initialValues as LibraryCatalogLoanFormData & { id?: string | null })

const handleSubmit = async () => {
  try {
    // Serialize Date objects to ISO strings for API submission
    const serializedData: Record<string, any> = { ...state.value }
    if (serializedData.borrowedAt instanceof Date) {
      serializedData.borrowedAt = serializedData.borrowedAt.toISOString()
    }
    if (serializedData.dueAt instanceof Date) {
      serializedData.dueAt = serializedData.dueAt.toISOString()
    }
    if (serializedData.returnedAt instanceof Date) {
      serializedData.returnedAt = serializedData.returnedAt.toISOString()
    }

    if (props.action === 'create') {
      await create(serializedData)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, serializedData)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
    // You can add toast notification here if available
    // toast.add({ title: 'Error', description: 'Failed to submit form', color: 'red' })
  }
}
</script>