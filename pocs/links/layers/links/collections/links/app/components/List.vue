<!--
  @crouton-generated
  @collection links
  @layer links
  @generated 2026-06-22

  ## AI Context
  - List component for links collection
  - Collection name: links
  - API endpoint: /api/teams/[id]/links
  - Columns composable: useLinks()
  - Column fields: title, url, tags

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="links"
    :columns="columns"
    :rows="items || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="Links"
        :collection="'links'"
        createButton
      />
    </template>
    <template #url-cell="{ row }">
      <a
        :href="row.original.url"
        target="_blank"
        rel="noopener noreferrer"
        class="text-(--ui-primary) hover:underline"
      >
        {{ row.original.url }}
      </a>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useLinks from '../composables/useLinks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useLinks()

const { items, pending } = await useCollectionQuery(
  'links'
)

</script>
