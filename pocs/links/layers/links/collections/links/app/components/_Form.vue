<!--
  @crouton-generated
  @collection links
  @layer links
  @generated 2026-06-22

  ## AI Context
  - Form component for links collection
  - Handles: create, update, delete actions
  - API endpoint: /api/teams/[id]/links
  - Zod schema: useLinks() composable
  - Fields: title, url, tags

  ## Common Modifications
  - Add field: Add UFormField in template, update schema in composable
  - Custom validation: Modify schema in composable
  - Custom field component: Use meta.component in schema JSON
  - Change layout: Modify CroutonFormLayout template slots

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonFormActionButton
    v-if="action === 'delete'"
    :action="action"
    :collection="collection"
    :items="items"
    :loading="loading"
    @click="handleSubmit"
  />

  <UForm
    v-else
    :schema="schema"
    :state="state"
    @submit="handleSubmit"
  >
    <CroutonFormLayout>
      <template #main>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Title" name="title" class="not-last:pb-4">
          <UInput v-model="state.title" class="w-full" size="xl" placeholder="My awesome link" />
        </UFormField>
        <UFormField label="URL" name="url" class="not-last:pb-4">
          <UInput v-model="state.url" class="w-full" size="xl" type="url" placeholder="https://example.com" />
        </UFormField>
      </div>
      </template>

      <template #sidebar>
      <div class="flex flex-col gap-4 p-1">
        <UFormField label="Tags" name="tags" class="not-last:pb-4">
          <UInput v-model="state.tags" class="w-full" size="xl" placeholder="news, frontend, tools" />
          <p class="text-sm text-(--ui-text-muted) mt-1.5">Comma-separated tags</p>
        </UFormField>
      </div>
      </template>

      <template #footer>
        <CroutonFormActionButton
          :action="action"
          :collection="collection"
          :items="items"
          :loading="loading"
        />
      </template>
    </CroutonFormLayout>
  </UForm>
</template>

<script setup lang="ts">
import type { LinkFormProps, LinkFormData } from '../../types'
import useLinks from '../composables/useLinks'

const props = defineProps<LinkFormProps>()
const { defaultValue, schema, collection } = useLinks()

// Use new mutation composable for data operations
const { create, update, deleteItems } = useCollectionMutation(collection)

// useCrouton still manages modal state
const { close, loading } = useCrouton()

// Initialize form state with proper values (no watch needed!)
const initialValues = props.action === 'update' && props.activeItem?.id
  ? { ...defaultValue, ...props.activeItem }
  : { ...defaultValue }

// Draft state: seeded from defaults (required fields may start null/empty until
// the user fills them; the zod schema validates on submit), so cast the initial
// values to the validated shape.
const state = ref<LinkFormData & { id?: string | null }>(initialValues as LinkFormData & { id?: string | null })

const handleSubmit = async () => {
  try {
    if (props.action === 'create') {
      await create(state.value)
    } else if (props.action === 'update' && state.value.id) {
      await update(state.value.id, state.value)
    } else if (props.action === 'delete') {
      await deleteItems(props.items)
    }

    close()

  } catch (error) {
    console.error('Form submission failed:', error)
  }
}
</script>
