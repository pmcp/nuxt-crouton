/**
 * @crouton-generated
 * @collection links
 * @layer links
 * @generated 2026-06-22
 *
 * ## AI Context
 * - Composable: useLinks
 * - Collection name: links
 * - API endpoint: /api/teams/[id]/links
 * - Form component: LinksForm
 * - List component: LinksList
 * - Fields: title, url, tags
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const linkSchema = z.object({
  title: z.string().min(1, 'title is required'),
  url: z.string().min(1, 'url is required'),
  tags: z.string().optional()
})

export const linksColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'url', header: 'URL' },
  { accessorKey: 'tags', header: 'Tags' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _linksConfig = {
  name: 'links',
  layer: 'links',
  apiPath: 'links',
  componentName: 'LinksForm',
  defaultValues: {
    title: '',
    url: '',
    tags: ''
  },
  columns: linksColumns,
  kind: 'content',
  fields: [
    {
      "name": "title",
      "type": "string",
      "label": "Title",
      "area": "main"
    },
    {
      "name": "url",
      "type": "string",
      "label": "URL",
      "area": "main"
    },
    {
      "name": "tags",
      "type": "string",
      "label": "Tags",
      "area": "sidebar"
    }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_linksConfig, 'schema', {
  value: linkSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const linksConfig = _linksConfig as typeof _linksConfig & { schema: typeof linkSchema }

export const useLinks = () => linksConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: linksConfig.defaultValues,
    schema: linkSchema,
    columns: linksConfig.columns,
    collection: linksConfig.name
  }
}
