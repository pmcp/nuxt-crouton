<!--
  @crouton-generated
  @collection assets
  @layer crouton
  @generated 2026-03-10

  ## AI Context
  - List component for assets collection
  - Collection name: croutonAssets
  - API endpoint: /api/teams/[id]/crouton-assets
  - Columns composable: useCroutonAssets()
  - Column fields: userId, filename, pathname, contentType, size, category, width, height, alt, uploadedAt, updatedBy

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="croutonAssets"
    :columns="columns"
    :rows="assets || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="CroutonAssets"
        :collection="'croutonAssets'"
        createButton
      />
    </template>
    <template #alt-cell="{ row }">
      {{ t(row.original, 'alt') }}
    </template>
    <template #uploadedAt-cell="{ row }">
      <CroutonDate :date="row.original.uploadedAt"></CroutonDate>
    </template>
    <template #translations-cell="{ row }">
      <CroutonI18nListCards :item="row.original" :fields="['alt']" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useCroutonAssets from '../composables/useCroutonAssets'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { t } = useEntityTranslations()
const { locale } = useI18n()
const { columns } = useCroutonAssets()

const { items: assets, pending } = await useCollectionQuery(
  'croutonAssets'
)

</script>