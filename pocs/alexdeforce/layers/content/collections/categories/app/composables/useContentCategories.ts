/**
 * @crouton-generated
 * @collection categories
 * @layer content
 * @generated 2026-03-10
 *
 * ## AI Context
 * - Composable: useContentCategories
 * - Collection name: contentCategories
 * - API endpoint: /api/teams/[id]/content-categories
 * - Form component: ContentCategoriesForm
 * - List component: ContentCategoriesList
 * - Fields: title, icon
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const contentCategorieSchema = z.object({
  title: z.string().min(1, 'title is required'),
  icon: z.string().optional()
})

export const contentCategoriesColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'icon', header: 'Icon' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _contentCategoriesConfig = {
  name: 'contentCategories',
  layer: 'content',
  apiPath: 'content-categories',
  componentName: 'ContentCategoriesForm',
  defaultValues: {
    title: '',
    icon: ''
  },
  columns: contentCategoriesColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "icon",
          "type": "string",
          "label": "Icon",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_contentCategoriesConfig, 'schema', {
  value: contentCategorieSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const contentCategoriesConfig = _contentCategoriesConfig as typeof _contentCategoriesConfig & { schema: typeof contentCategorieSchema }

export const useContentCategories = () => contentCategoriesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: contentCategoriesConfig.defaultValues,
    schema: contentCategorieSchema,
    columns: contentCategoriesConfig.columns,
    collection: contentCategoriesConfig.name
  }
}