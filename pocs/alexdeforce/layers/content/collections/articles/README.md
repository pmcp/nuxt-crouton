# ContentArticles Collection

> Generated by @fyit/crouton-cli on 2026-03-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `contentArticles` |
| Layer | `content` |
| API Base | `/api/teams/[teamId]/content-articles` |
| Form Component | `<ContentArticlesForm />` |
| List Component | `<ContentArticlesList />` |
| Composable | `useContentArticles()` |

## Features

- References: category → categories, tags → tags

## File Structure

```
collections/articles/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useContentArticles.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/content-articles/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [articleId].patch.ts    # UPDATE
│   │   └── [articleId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| date | date | Yes |
| category | string (→ categories) | Yes |
| content | text | No |
| imageUrl | string | No |
| tags | array (→ tags) | No |
| featured | boolean | No |
| status | string | Yes |
| publishedAt | date | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/content-articles

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/content-articles`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/content-articles?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/content-articles

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/content-articles`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/content-articles/{articleId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/content-articles/${articleId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/content-articles/{articleId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/content-articles/${articleId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: articles, pending } = await useCollectionQuery('contentArticles')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('contentArticles')
</script>

<template>
  <ContentArticlesList />
</template>
```

### Schema & Validation

```typescript
import { contentArticleSchema } from './app/composables/useContentArticles'

// Validate data
const result = contentArticleSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only articles`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useContentArticles.ts`:
```typescript
export const contentArticleSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/content-articles/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
