{
  "name": "pocs-skillissue",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare 2>/dev/null || true",
    "typecheck": "nuxt typecheck",
    "cf:staging": "NITRO_PRESET=cloudflare_module nuxt build && npx wrangler deploy --config .output/server/wrangler.json --env staging",
    "cf:deploy": "NITRO_PRESET=cloudflare_module nuxt build && npx wrangler deploy --config .output/server/wrangler.json"
  },
  "dependencies": {
    "@fyit/crouton-themes": "workspace:*",
    "@nuxt/ui": "catalog:",
    "eruda": "^3.4.3",
    "nuxt": "catalog:",
    "vue": "catalog:",
    "vue-router": "catalog:"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
