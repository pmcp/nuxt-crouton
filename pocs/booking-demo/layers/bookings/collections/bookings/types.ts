/**
 * @crouton-generated
 * @collection bookings
 * @layer bookings
 * @generated 2026-06-23
 *
 * ## AI Context
 * - Main interface: BookingsBooking
 * - Form data type: BookingsBookingFormData
 * - New item type: NewBookingsBooking
 * - Form props: BookingsBookingFormProps
 * - Fields: location, date, slot, group, quantity, status
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { bookingsBookingSchema } from './app/composables/useBookingsBookings'

export interface BookingsBooking {
  id: string
  teamId: string
  owner: string
  location: string
  date: Date
  slot: string[]
  group?: string
  quantity: number
  status: string
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type BookingsBookingFormData = z.infer<typeof bookingsBookingSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewBookingsBooking = Omit<BookingsBooking, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface BookingsBookingFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: BookingsBooking | Record<string, never> // BookingsBooking for update, empty object for create
  collection: string
  loading: string
  action: 'create' | 'update' | 'delete'
}