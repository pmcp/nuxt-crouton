<!--
  @crouton-generated
  @collection nodes
  @layer thinkgraph
  @generated 2026-03-19

  ## AI Context
  - List component for nodes collection
  - Collection name: thinkgraphNodes
  - API endpoint: /api/teams/[id]/thinkgraph-nodes
  - Columns composable: useThinkgraphNodes()
  - Column fields: canvasId, parentId, nodeType, status, title, brief, output, handoffType, handoffMeta, contextScope, contextNodeIds, notionTaskId, worktree, sendTarget, sendMode, injectMode, origin, stepIndex, skillVersion, tokenCount, userId

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="thinkgraphNodes"
    :columns="columns"
    :rows="nodes || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ThinkgraphNodes"
        :collection="'thinkgraphNodes'"
        createButton
      />
    </template>
    <template #canvasId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.canvasId"
        :id="row.original.canvasId"
        collection="thinkgraphCanvases"
      />
    </template>
    <template #parentId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.parentId"
        :id="row.original.parentId"
        collection="thinkgraphNodes"
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useThinkgraphNodes from '../composables/useThinkgraphNodes'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useThinkgraphNodes()

const { items: nodes, pending } = await useCollectionQuery(
  'thinkgraphNodes'
)

</script>