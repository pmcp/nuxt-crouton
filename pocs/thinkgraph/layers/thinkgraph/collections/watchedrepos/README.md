# ThinkgraphWatchedRepos Collection

> Generated by @fyit/crouton-cli on 2026-04-07

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `thinkgraphWatchedRepos` |
| Layer | `thinkgraph` |
| API Base | `/api/teams/[teamId]/thinkgraph-watchedrepos` |
| Form Component | `<ThinkgraphWatchedReposForm />` |
| List Component | `<ThinkgraphWatchedReposList />` |
| Composable | `useThinkgraphWatchedRepos()` |

## File Structure

```
collections/watchedrepos/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useThinkgraphWatchedRepos.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/thinkgraph-watchedrepos/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [watchedrepoId].patch.ts    # UPDATE
│   │   └── [watchedrepoId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| repo | string | Yes |
| branch | string | No |
| lastCheckedSha | string | No |
| notes | text | No |
| active | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/thinkgraph-watchedrepos

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-watchedrepos`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/thinkgraph-watchedrepos?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/thinkgraph-watchedrepos

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/thinkgraph-watchedrepos`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/thinkgraph-watchedrepos/{watchedrepoId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/thinkgraph-watchedrepos/${watchedrepoId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/thinkgraph-watchedrepos/{watchedrepoId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/thinkgraph-watchedrepos/${watchedrepoId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: watchedRepos, pending } = await useCollectionQuery('thinkgraphWatchedRepos')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('thinkgraphWatchedRepos')
</script>

<template>
  <ThinkgraphWatchedReposList />
</template>
```

### Schema & Validation

```typescript
import { thinkgraphWatchedRepoSchema } from './app/composables/useThinkgraphWatchedRepos'

// Validate data
const result = thinkgraphWatchedRepoSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only watchedrepos`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useThinkgraphWatchedRepos.ts`:
```typescript
export const thinkgraphWatchedRepoSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/thinkgraph-watchedrepos/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
