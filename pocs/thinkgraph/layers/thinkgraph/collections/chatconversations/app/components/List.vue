<!--
  @crouton-generated
  @collection chatconversations
  @layer thinkgraph
  @generated 2026-03-12

  ## AI Context
  - List component for chatconversations collection
  - Collection name: thinkgraphChatConversations
  - API endpoint: /api/teams/[id]/thinkgraph-chatconversations
  - Columns composable: useThinkgraphChatConversations()
  - Column fields: nodeId, title, messages, provider, model, systemPrompt, metadata, messageCount, lastMessageAt

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="thinkgraphChatConversations"
    :columns="columns"
    :rows="chatconversations || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ThinkgraphChatConversations"
        :collection="'thinkgraphChatConversations'"
        createButton
      />
    </template>
    <template #lastMessageAt-cell="{ row }">
      <CroutonDate :date="row.original.lastMessageAt"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useThinkgraphChatConversations from '../composables/useThinkgraphChatConversations'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useThinkgraphChatConversations()

const { items: chatConversations, pending } = await useCollectionQuery(
  'thinkgraphChatConversations'
)

</script>